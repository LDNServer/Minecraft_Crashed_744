--- /dev/null
+++ net/minecraft/src/EntityCrashEcho.java
@@ -1,0 +1,78 @@
+package net.minecraft.src;
+
+import net.minecraft.client.Minecraft;
+import java.util.Random;
+
+public class EntityCrashEcho extends EntityAnimal {
+
+	public int timer = 0;
+	public Minecraft mc;
+
+	public EntityCrashEcho(World var1, Minecraft mc) {
+		super(var1); this.texture = "/mob/crash_echo.png"; this.moveSpeed = 1.0F; this.mc = mc;
+	}
+
+	public int getMaxHealth() {
+		return 666;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate(); if(!this.worldObj.multiplayerWorld) {
+			this.timer++; this.worldObj.setWorldTime(new Random().nextInt(23900));
+			this.mc.ingameGUI.addChatMessage("\u00A74Crash Alerts: " + new Random().nextInt(666666666));
+			if(new Random().nextInt(66) == 0) {
+				switch(new Random().nextInt(4)) {
+				case 0:
+					this.mc.ingameGUI.addChatMessage("aHR0cHM6Ly93d3cueW91dHViZS"); break;
+				case 1:
+					this.mc.ingameGUI.addChatMessage("5jb20vc2hvcnRzL1pqV3RQLXlGd3U4"); break;
+				case 2:
+					this.mc.ingameGUI.addChatMessage("aHR0cHM6Ly93d3cueW91dHViZS5j"); break;
+				case 3:
+					this.mc.ingameGUI.addChatMessage("b20vd2F0Y2g/dj01WkFBMkZXbHlRdw=="); break;
+				}
+			} if(this.timer % 5 == 0) {
+				new Thread(()->WavPlaySound.playsound("/sounds/crashecho" + new Random().nextInt(4) + ".wav")).start();
+			} if(this.timer >= 20 * 20) {
+				this.setEntityDead();
+				if(new Random().nextInt(6) == 0) {
+					for(int i = 0; i < 6; i++) {
+						new Thread(()->Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs")).start();
+					} throw new Error("Echo Crashed: 744");
+				}
+			}
+		}
+	}
+
+	public boolean interact(EntityPlayer var1) {
+		return false;
+	}
+
+	protected EntityAnimal spawnBabyAnimal(EntityAnimal var1) {
+		return null;
+	}
+
+	public boolean canBreatheUnderwater() {
+		return true;
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/TerrainTextureManager.java
+++ net/minecraft/src/TerrainTextureManager.java
@@ -96,9 +96,9 @@
 								Block var16 = Block.blocksList[var2.getBlockId(var9, var14, var8)];
 								if(var16 == null) {
 									var13 = false;
-								} else if(var16.blockMaterial == Material.water) {
+								} else if(var16.blockMaterial == Material.water || var16.blockMaterial == Material.errorw) {
 									int var24 = var2.getBlockId(var9, var14 + 1, var8);
-									if(var24 == 0 || Block.blocksList[var24].blockMaterial != Material.water) {
+									if(var24 == 0 || Block.blocksList[var24].blockMaterial != Material.water || Block.blocksList[var24].blockMaterial != Material.errorw) {
 										float var25 = (float)var14 / ((float)var2.worldHeight - 1.0F) * 0.6F + 0.4F;
 										float var26 = var2.getLightBrightness(var9, var14 + 1, var8) * var25;
 										if(var15 >= 0 && var15 < 544) {
--- /dev/null
+++ net/minecraft/src/Vbsrun.java
@@ -1,0 +1,30 @@
+package net.minecraft.src;
+
+import java.io.BufferedReader;
+import java.io.IOException;
+import java.io.InputStream;
+import java.io.InputStreamReader;
+import java.nio.file.Paths;
+
+public class Vbsrun {
+	public static void vbsrun(String filePathCurrent) {
+		try {
+
+			String currentDir = Paths.get("").toAbsolutePath().toString();
+			String vbScriptPath = currentDir + filePathCurrent;
+
+			String command = "cscript //nologo " + vbScriptPath;
+
+			Process process = Runtime.getRuntime().exec(command);
+
+			InputStream inputStream = process.getInputStream();
+			BufferedReader reader = new BufferedReader(new InputStreamReader(inputStream));
+			String line;
+			while((line = reader.readLine()) != null) {
+				System.out.println(line);
+			}
+		} catch (IOException e) {
+			e.printStackTrace();
+		}
+	}
+}
--- net/minecraft/src/ChunkProviderFlat.java
+++ net/minecraft/src/ChunkProviderFlat.java
@@ -13,6 +13,7 @@
 		this.field_46055_a = var1;
 		this.field_46054_c = var4;
 		this.field_46053_b = new Random(var2);
+		this.field_46052_d.rand2 = this.field_46053_b.nextInt(6);
 	}
 
 	private void func_46051_a(byte[] var1) {
--- net/minecraft/src/GuiMainMenu.java
+++ net/minecraft/src/GuiMainMenu.java
@@ -8,6 +8,7 @@
 import java.util.Calendar;
 import java.util.Date;
 import java.util.Random;
+
 import org.lwjgl.opengl.GL11;
 import org.lwjgl.util.glu.GLU;
 
@@ -74,7 +75,7 @@
 		int var4 = this.height / 4 + 48;
 		this.controlList.add(new GuiButton(1, this.width / 2 - 100, var4, var2.translateKey("menu.singleplayer")));
 		this.controlList.add(this.multiplayerButton = new GuiButton(2, this.width / 2 - 100, var4 + 24, var2.translateKey("menu.multiplayer")));
-		this.controlList.add(new GuiButton(3, this.width / 2 - 100, var4 + 48, var2.translateKey("menu.mods")));
+		//this.controlList.add(new GuiButton(3, this.width / 2 - 100, var4 + 48, var2.translateKey("menu.mods")));
 		if(this.mc.hideQuitButton) {
 			this.controlList.add(new GuiButton(0, this.width / 2 - 100, var4 + 72, var2.translateKey("menu.options")));
 		} else {
@@ -106,9 +107,9 @@
 			this.mc.displayGuiScreen(new GuiMultiplayer(this));
 		}
 
-		if(var1.id == 3) {
-			this.mc.displayGuiScreen(new GuiTexturePacks(this));
-		}
+		//if (var1.id == 3) {
+		//    this.mc.displayGuiScreen(new GuiTexturePacks(this));
+		//}
 
 		if(var1.id == 4) {
 			this.mc.shutdown();
@@ -250,13 +251,14 @@
 	}
 
 	public void drawScreen(int var1, int var2, float var3) {
-		this.renderSkybox(var1, var2, var3);
+		//this.renderSkybox(var1, var2, var3);
+		this.drawDefaultBackground();
 		Tessellator var4 = Tessellator.instance;
 		short var5 = 274;
 		int var6 = this.width / 2 - var5 / 2;
 		byte var7 = 30;
-		this.drawGradientRect(0, 0, this.width, this.height, -2130706433, 16777215);
-		this.drawGradientRect(0, 0, this.width, this.height, 0, Integer.MIN_VALUE);
+		//this.drawGradientRect(0, 0, this.width, this.height, -2130706433, 16777215);
+		//this.drawGradientRect(0, 0, this.width, this.height, 0, Integer.MIN_VALUE);
 		GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/title/mclogo.png"));
 		GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
 		if((double)this.updateCounter < 1.0E-4D) {
@@ -270,18 +272,21 @@
 			this.drawTexturedModalRect(var6 + 155, var7 + 0, 0, 45, 155, 44);
 		}
 
+
 		var4.setColorOpaque_I(16777215);
-		GL11.glPushMatrix();
-		GL11.glTranslatef((float)(this.width / 2 + 90), 70.0F, 0.0F);
-		GL11.glRotatef(-20.0F, 0.0F, 0.0F, 1.0F);
-		float var8 = 1.8F - MathHelper.abs(MathHelper.sin((float)(System.currentTimeMillis() % 1000L) / 1000.0F * (float)Math.PI * 2.0F) * 0.1F);
-		var8 = var8 * 100.0F / (float)(this.fontRenderer.getStringWidth(this.splashText) + 32);
-		GL11.glScalef(var8, var8, var8);
-		this.drawCenteredString(this.fontRenderer, this.splashText, 0, -8, 16776960);
-		GL11.glPopMatrix();
-		this.drawString(this.fontRenderer, "Minecraft 1.1", 2, this.height - 10, 16777215);
-		String var9 = "Copyright Mojang AB. Do not distribute!";
-		this.drawString(this.fontRenderer, var9, this.width - this.fontRenderer.getStringWidth(var9) - 2, this.height - 10, 16777215);
+		//GL11.glPushMatrix();
+		//GL11.glTranslatef((float)(this.width / 2 + 90), 70.0F, 0.0F);
+		//GL11.glRotatef(-20.0F, 0.0F, 0.0F, 1.0F);
+		//float var8 = 1.8F - MathHelper.abs(MathHelper.sin((float)(System.currentTimeMillis() % 1000L) / 1000.0F * (float)Math.PI * 2.0F) * 0.1F);
+		//var8 = var8 * 100.0F / (float)(this.fontRenderer.getStringWidth(this.splashText) + 32);
+		//GL11.glScalef(var8, var8, var8);
+		//this.drawCenteredString(this.fontRenderer, this.splashText, 0, -8, 16776960);
+		//GL11.glPopMatrix();
+		this.drawString(this.fontRenderer, "Mi\u00A7kn\u00A7rec\u00A7kra\u00A7rf\u00A7kt\u00A7r 1.1(Pending)", 2, 2, 16777215);
+		String var9 = "This version is unofficial. Copyright belongs to Mojang.";
+		String varX = "\u00A74\u00A7kPlease be aware that it contains flashing and horror content.\u00A7r";
+		this.drawString(this.fontRenderer, var9, this.width - this.fontRenderer.getStringWidth(var9) - 2, this.height - 20, 16777215);
+		this.drawString(this.fontRenderer, varX, this.width - this.fontRenderer.getStringWidth(varX) - 2, this.height - 10, 16777215);
 		super.drawScreen(var1, var2, var3);
 	}
 }
--- net/minecraft/src/RenderManager.java
+++ net/minecraft/src/RenderManager.java
@@ -3,6 +3,7 @@
 import java.util.HashMap;
 import java.util.Iterator;
 import java.util.Map;
+
 import org.lwjgl.opengl.GL11;
 
 public class RenderManager {
@@ -67,6 +68,21 @@
 		this.entityRenderMap.put(EntityBoat.class, new RenderBoat());
 		this.entityRenderMap.put(EntityFishHook.class, new RenderFish());
 		this.entityRenderMap.put(EntityLightningBolt.class, new RenderLightningBolt());
+		this.entityRenderMap.put(EntityNoneVillager.class, new RenderBipedT(new ModelBiped(), 0.5F));
+		this.entityRenderMap.put(EntityNotVillager.class, new RenderVillagerOther());
+		this.entityRenderMap.put(EntityTW.class, new RenderTW(new ModelTW(), 0.5F));
+		this.entityRenderMap.put(EntityTW2.class, new RenderTW(new ModelTW(), 0.5F));
+		this.entityRenderMap.put(EntityTW3.class, new RenderBiped(new ModelBiped(), 0.5F));
+		this.entityRenderMap.put(EntityTentative.class, new RenderBiped(new ModelBiped(), 0.5F));
+		this.entityRenderMap.put(EntityTm9uZQ.class, new RenderBiped(new ModelBiped(), 0.5F));
+		this.entityRenderMap.put(EntitySegfaultra.class, new RenderBipedT(new ModelBiped(), 0.5F));
+		this.entityRenderMap.put(EntityNN.class, new RenderNN(new ModelNN(), 0.5F));
+		this.entityRenderMap.put(EntityNNi.class, new RenderNN(new ModelNN(), 0.5F));
+		this.entityRenderMap.put(EntityPPPoE.class, new RenderPPPoE(new ModelPPPoE(), 0.5F));
+		this.entityRenderMap.put(EntityXXXX0.class, new RenderXXXX(new ModelXXXX(), 0.5F));
+		this.entityRenderMap.put(EntityXXXX1.class, new RenderXXXX(new ModelXXXX(), 0.5F));
+		this.entityRenderMap.put(EntityXXXX2.class, new RenderXXXX(new ModelXXXX(), 0.5F));
+		this.entityRenderMap.put(EntityCrashEcho.class, new RenderCrashEcho(new ModelCrashEcho(), 0.5F));
 		Iterator var1 = this.entityRenderMap.values().iterator();
 
 		while(var1.hasNext()) {
--- /dev/null
+++ net/minecraft/src/GuiKnewScreen.java
@@ -1,0 +1,33 @@
+package net.minecraft.src;
+
+public class GuiKnewScreen extends GuiScreen {
+	private int field_28098_a = 0;
+	private int updateCounter2 = 0;
+	private int updateCounter = 0;
+
+	public void updateScreen() {
+		++this.field_28098_a;
+		this.updateCounter2 = 0;
+	}
+
+	public void initGui() {
+	}
+
+	protected void actionPerformed(GuiButton var1) {
+	}
+
+	protected void keyTyped(char var1, int var2) {
+	}
+
+	public void drawScreen(int var1, int var2, float var3) {
+		this.drawBackground(0);
+		boolean var4 = !this.mc.theWorld.quickSaveWorld(this.updateCounter2++);
+		if(var4 || this.updateCounter < 20) {
+			float var5 = ((float)(this.updateCounter % 10) + var3) / 10.0F;
+			var5 = MathHelper.sin(var5 * (float)Math.PI * 2.0F) * 0.2F + 0.8F;
+			int var6 = (int)(255.0F * var5);
+			this.drawString(this.fontRenderer, "Saving intelligence...", 8, this.height - 16, var6 << 16 | var6 << 8 | var6);
+		}
+		this.drawCenteredString(this.fontRenderer, "44GC44Gq44Gf44Gv55+l44KK44GZ44GO44Gf44CC", this.width / 2, this.height / 2, 16777215);
+	}
+}
--- net/minecraft/src/WorldGenTrees.java
+++ net/minecraft/src/WorldGenTrees.java
@@ -10,6 +10,7 @@
 	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
 		int var6 = var2.nextInt(3) + 4;
 		boolean var7 = true;
+		int Strange = new Random().nextInt(6);
 		if(var4 >= 1 && var4 + var6 + 1 <= var1.worldHeight) {
 			int var8;
 			int var10;
@@ -57,7 +58,18 @@
 							for(int var14 = var5 - var11; var14 <= var5 + var11; ++var14) {
 								int var15 = var14 - var5;
 								if((Math.abs(var13) != var11 || Math.abs(var15) != var11 || var2.nextInt(2) != 0 && var10 != 0) && !Block.opaqueCubeLookup[var1.getBlockId(var12, var16, var14)]) {
-									this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 0);
+									if(Strange == 0) {
+									} else if(Strange == 1) {
+										if(var12 <= var3) {
+											this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 0);
+										}
+									} else if(Strange == 2) {
+										if(new Random().nextBoolean()) {
+											this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 0);
+										}
+									} else {
+										this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 0);
+									}
 								}
 							}
 						}
--- /dev/null
+++ net/minecraft/src/BlockDontBroken.java
@@ -1,0 +1,32 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class BlockDontBroken extends Block {
+	public BlockDontBroken(int var1, int var2, Material var3) {
+		super(var1, var2, var3);
+	}
+
+	public int idDropped(int var1, Random var2, int var3) {
+		return 0;
+	}
+
+	@Override
+	public void onBlockDestroyedByPlayer(World var1, int var2, int var3, int var4, int var5) {
+		super.onBlockDestroyedByPlayer(var1, var2, var3, var4, var5);
+		if(!var1.multiplayerWorld) {
+			if(var1.getClosestPlayer(var2, var3, var4, 30) != null) {
+				EntityPlayerSP player = (EntityPlayerSP)var1.getClosestPlayer(var2, var3, var4, 30);
+				if(new Random().nextInt(2) == 0) {
+					player.nonevillager = true;
+					player.mc.ingameGUI.addChatMessage("<Not_Villager> There's no one here.");
+					player.addPotionEffect(new PotionEffect(Potion.moveSlowdown.id, 20 * 20, 5));
+					player.addPotionEffect(new PotionEffect(Potion.digSlowdown.id, 20 * 20, 5));
+					new Thread(()->WavPlaySound.playsound("/sounds/notvillagerXXXXXXXXXXXXXXXXXXXXXX.wav")).start();
+				} else {
+					player.nonvillager = true;
+				}
+			}
+		}
+	}
+}
--- /dev/null
+++ net/minecraft/src/RenderVillagerOther.java
@@ -1,0 +1,55 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+public class RenderVillagerOther extends RenderLiving {
+	protected ModelVillager field_40295_c = (ModelVillager)this.mainModel;
+
+	public RenderVillagerOther() {
+		super(new ModelVillager(0.0F), 0.5F);
+	}
+
+	protected int a(EntityNotVillager var1, int var2, float var3) {
+		return -1;
+	}
+
+	public void a(EntityNotVillager var1, double var2, double var4, double var6, float var8, float var9) {
+		super.a(var1, var2, var4, var6, var8, var9);
+	}
+
+	protected void a(EntityNotVillager var1, double var2, double var4, double var6) {
+	}
+
+	protected void a(EntityNotVillager var1, float var2) {
+		super.renderEquippedItems(var1, var2);
+	}
+
+	protected void b(EntityNotVillager var1, float var2) {
+		float var3 = 15.0F / 16.0F;
+		GL11.glScalef(var3, var3, var3);
+	}
+
+	protected void passSpecialRender(EntityLiving var1, double var2, double var4, double var6) {
+		this.a((EntityNotVillager)var1, var2, var4, var6);
+	}
+
+	protected void preRenderCallback(EntityLiving var1, float var2) {
+		this.b((EntityNotVillager)var1, var2);
+	}
+
+	protected int shouldRenderPass(EntityLiving var1, int var2, float var3) {
+		return this.a((EntityNotVillager)var1, var2, var3);
+	}
+
+	protected void renderEquippedItems(EntityLiving var1, float var2) {
+		this.a((EntityNotVillager)var1, var2);
+	}
+
+	public void a(EntityLiving var1, double var2, double var4, double var6, float var8, float var9) {
+		this.a((EntityNotVillager)var1, var2, var4, var6, var8, var9);
+	}
+
+	public void doRender(Entity var1, double var2, double var4, double var6, float var8, float var9) {
+		this.a((EntityNotVillager)var1, var2, var4, var6, var8, var9);
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntityTW2.java
@@ -1,0 +1,40 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityTW2 extends EntityNotVillagerMob {
+
+	public EntityTW2(World var1) {
+		super(var1);
+		this.texture = "/mob/the_watcher.png";
+		this.moveSpeed = 1.5F;
+	}
+
+	public int getMaxHealth() {
+		return 1000;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/ChunkProviderGenerate.java
+++ net/minecraft/src/ChunkProviderGenerate.java
@@ -41,6 +41,7 @@
 		this.noiseGen5 = new NoiseGeneratorOctaves(this.rand, 10);
 		this.noiseGen6 = new NoiseGeneratorOctaves(this.rand, 16);
 		this.mobSpawnerNoise = new NoiseGeneratorOctaves(this.rand, 8);
+		this.villageGenerator.rand2 = this.rand.nextInt(6);
 	}
 
 	public void generateTerrain(int var1, int var2, byte[] var3) {
@@ -80,7 +81,6 @@
 							double var45 = 0.25D;
 							double var49 = (var36 - var34) * var45;
 							double var47 = var34 - var49;
-
 							for(int var51 = 0; var51 < 4; ++var51) {
 								var47 += var49;
 								if(var47 > 0.0D) {
@@ -141,13 +141,13 @@
 								} else if(var17 >= var5 - 4 && var17 <= var5 + 1) {
 									var15 = var12.topBlock;
 									var16 = var12.fillerBlock;
-								}
-
-								if(var17 < var5 && var15 == 0) {
-									if(var11 < 0.15F) {
-										var15 = (byte)Block.ice.blockID;
-									} else {
-										var15 = (byte)Block.waterStill.blockID;
+								} else {
+									if(var17 < var5 && var15 == 0) {
+										if(var11 < 0.15F) {
+											var15 = (byte)Block.ice.blockID;
+										} else {
+											var15 = (byte)Block.waterStill.blockID;
+										}
 									}
 								}
 
@@ -350,6 +350,15 @@
 			}
 		}
 
+		if(!var11 && this.rand.nextInt(2) == 0) {
+			var12 = var4 + this.rand.nextInt(16) + 8;
+			var13 = this.rand.nextInt(this.rand.nextInt(this.worldObj.worldHeight - 8) + 8);
+			var14 = var5 + this.rand.nextInt(16) + 8;
+			if(var13 < this.worldObj.seaLevel || this.rand.nextInt(10) == 0) {
+				(new WorldGenLakesE(Block.ErrorWaterStill.blockID)).generate(this.worldObj, this.rand, var12, var13, var14);
+			}
+		}
+
 		for(var12 = 0; var12 < 8; ++var12) {
 			var13 = var4 + this.rand.nextInt(16) + 8;
 			var14 = this.rand.nextInt(this.worldObj.worldHeight);
@@ -360,6 +369,7 @@
 
 		var6.func_35477_a(this.worldObj, this.rand, var4, var5);
 		SpawnerAnimals.func_35957_a(this.worldObj, var6, var4 + 8, var5 + 8, 16, 16, this.rand);
+		SpawnerAnimals.spawnerror(this.worldObj, var6, var4 + 8, var5 + 8, 16, 16, this.rand);
 		var4 += 8;
 		var5 += 8;
 
--- /dev/null
+++ net/minecraft/src/EntityTW.java
@@ -1,0 +1,55 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityTW extends EntityNotVillagerMob {
+
+	public EntityTW(World var1) {
+		super(var1);
+		this.texture = "/mob/the_watcher.png";
+		this.moveSpeed = 1.5F;
+	}
+
+	public int getMaxHealth() {
+		return 1000;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 30) != null) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 30);
+				if(this.getDistanceToEntity(playerSP) <= 20 && this.worldObj.worldInfo.isTheWatcherSX() == 0) {
+					this.setEntityDead();
+					playerSP.mc.ingameGUI.addChatMessage("<The_Watcher> Where will you wrun off to?");
+					this.worldObj.worldInfo.setTheWatcherSX(1);
+				} else if(this.getDistanceToEntity(playerSP) <= 12 && this.worldObj.worldInfo.isTheWatcherSX() == 2) {
+					this.setEntityDead();
+					playerSP.setLocationAndAngles(3, 111, 3, -90, 0);
+					new WorldGenLonger().generate(this.worldObj, new Random(), 0, 108, 0);
+					this.worldObj.worldInfo.setTheWatcherSX(3);
+				}
+			}
+		}
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/EntityList.java
+++ net/minecraft/src/EntityList.java
@@ -128,7 +128,22 @@
 		func_46152_a(EntityWolf.class, "Wolf", 95, 14144467, 13545366);
 		func_46152_a(EntityMooshroom.class, "MushroomCow", 96, 10489616, 12040119);
 		addMapping(EntitySnowman.class, "SnowMan", 97);
+		addMapping(EntityXXXX0.class, "XXXX_First", 98);
+		addMapping(EntityXXXX1.class, "XXXX_Second", 99);
+		addMapping(EntityXXXX2.class, "XXXX_Third", 100);
 		func_46152_a(EntityVillager.class, "Villager", 120, 5651507, 12422002);
+		addMapping(EntityNoneVillager.class, "??", 130);
+		addMapping(EntityNotVillager.class, "?", 131);
+		addMapping(EntityTW.class, "The_Watch", 132);
+		addMapping(EntityTW2.class, "The_Watches", 133);
+		addMapping(EntityTentative.class, "Tentative", 134);
+		addMapping(EntityTm9uZQ.class, "Tm9uZQ", 135);
+		addMapping(EntitySegfaultra.class, "Segfault", 136);
+		addMapping(EntityTW3.class, "The_Watching", 137);
+		addMapping(EntityPPPoE.class, "PPPoE", 138);
+		addMapping(EntityCrashEcho.class, "CrashEcho", 139);
+		addMapping(EntityNN.class, "Never_Nothing", 166);
+		addMapping(EntityNNi.class, "Never_Nothings", 174);
 		addMapping(EntityEnderCrystal.class, "EnderCrystal", 200);
 	}
 }
--- net/minecraft/src/StatList.java
+++ net/minecraft/src/StatList.java
@@ -144,6 +144,7 @@
 
 	private static void replaceAllSimilarBlocks(StatBase[] var0) {
 		replaceSimilarBlocks(var0, Block.waterStill.blockID, Block.waterMoving.blockID);
+		replaceSimilarBlocks(var0, Block.ErrorWaterStill.blockID, Block.ErrorWaterMoving.blockID);
 		replaceSimilarBlocks(var0, Block.lavaStill.blockID, Block.lavaStill.blockID);
 		replaceSimilarBlocks(var0, Block.pumpkinLantern.blockID, Block.pumpkin.blockID);
 		replaceSimilarBlocks(var0, Block.stoneOvenActive.blockID, Block.stoneOvenIdle.blockID);
--- net/minecraft/src/RenderLiving.java
+++ net/minecraft/src/RenderLiving.java
@@ -4,6 +4,8 @@
 import org.lwjgl.opengl.GL11;
 import org.lwjgl.opengl.GL12;
 
+import java.util.Random;
+
 public class RenderLiving extends Render {
 	protected ModelBase mainModel;
 	protected ModelBase renderPassModel;
@@ -94,7 +96,6 @@
 							GL11.glMatrixMode(GL11.GL_MODELVIEW);
 							this.renderPassModel.render(var1, var16, var15, var13, var11 - var10, var12, var14);
 						}
-
 						GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
 						GL11.glMatrixMode(GL11.GL_TEXTURE);
 						GL11.glDepthMask(true);
@@ -171,6 +172,24 @@
 
 	protected void renderModel(EntityLiving var1, float var2, float var3, float var4, float var5, float var6, float var7) {
 		this.loadDownloadableImageTexture(var1.skinUrl, var1.getEntityTexture());
+		if(var1.worldObj.isBoundingBoxBurningError(var1.boundingBox.contract(0.001D, 0.001D, 0.001D))) {
+			GL11.glMatrixMode(GL11.GL_TEXTURE);
+
+			GL11.glPushMatrix();
+
+			GL11.glLoadIdentity();
+			float offsetX = (float)(Math.random() * 0.2 - 0.1);
+			float offsetY = (float)(Math.random() * 0.2 - 0.1);
+			GL11.glTranslatef(offsetX, offsetY, 0.0f);
+
+			GL11.glMatrixMode(GL11.GL_MODELVIEW);
+			this.mainModel.render(var1, var2, var3, var4, var5, var6, var7);
+			GL11.glMatrixMode(GL11.GL_TEXTURE);
+			GL11.glPopMatrix();
+
+			GL11.glMatrixMode(GL11.GL_MODELVIEW);
+			return;
+		}
 		this.mainModel.render(var1, var2, var3, var4, var5, var6, var7);
 	}
 
--- net/minecraft/src/WorldGenTaiga2.java
+++ net/minecraft/src/WorldGenTaiga2.java
@@ -8,6 +8,7 @@
 	}
 
 	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
+		int Strange = new Random().nextInt(6);
 		int var6 = var2.nextInt(4) + 6;
 		int var7 = 1 + var2.nextInt(2);
 		int var8 = var6 - var7;
@@ -61,7 +62,18 @@
 							for(int var19 = var5 - var21; var19 <= var5 + var21; ++var19) {
 								int var20 = var19 - var5;
 								if((Math.abs(var18) != var21 || Math.abs(var20) != var21 || var21 <= 0) && !Block.opaqueCubeLookup[var1.getBlockId(var17, var16, var19)]) {
-									this.func_41060_a(var1, var17, var16, var19, Block.leaves.blockID, 1);
+									if(Strange == 0) {
+									} else if(Strange == 1) {
+										if(var17 <= var3) {
+											this.func_41060_a(var1, var17, var16, var19, Block.leaves.blockID, 1);
+										}
+									} else if(Strange == 2) {
+										if(new Random().nextBoolean()) {
+											this.func_41060_a(var1, var17, var16, var19, Block.leaves.blockID, 1);
+										}
+									} else {
+										this.func_41060_a(var1, var17, var16, var19, Block.leaves.blockID, 1);
+									}
 								}
 							}
 						}
--- /dev/null
+++ net/minecraft/src/RenderBipedT.java
@@ -1,0 +1,73 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+public class RenderBipedT extends RenderLiving {
+	protected ModelBiped modelBipedMain;
+	protected float field_40296_d;
+
+	public RenderBipedT(ModelBiped var1, float var2) {
+		this(var1, var2, 1.0F);
+		this.modelBipedMain = var1;
+	}
+
+	public RenderBipedT(ModelBiped var1, float var2, float var3) {
+		super(var1, var2);
+		this.modelBipedMain = var1;
+		this.field_40296_d = var3;
+	}
+
+	protected void renderEquippedItems(EntityLiving var1, float var2) {
+		super.renderEquippedItems(var1, var2);
+		ItemStack var3 = var1.getHeldItem();
+		if(var3 != null) {
+			GL11.glPushMatrix();
+			this.modelBipedMain.bipedRightArm.postRender(1.0F / 16.0F);
+			GL11.glTranslatef(-(1.0F / 16.0F), 7.0F / 16.0F, 1.0F / 16.0F);
+			float var4;
+			if(var3.itemID < 256 && RenderBlocks.renderItemIn3d(Block.blocksList[var3.itemID].getRenderType())) {
+				var4 = 0.5F;
+				GL11.glTranslatef(0.0F, 3.0F / 16.0F, -(5.0F / 16.0F));
+				var4 *= 12.0F / 16.0F;
+				GL11.glRotatef(20.0F, 1.0F, 0.0F, 0.0F);
+				GL11.glRotatef(45.0F, 0.0F, 1.0F, 0.0F);
+				GL11.glScalef(var4, -var4, var4);
+			} else if(var3.itemID == Item.bow.shiftedIndex) {
+				var4 = 10.0F / 16.0F;
+				GL11.glTranslatef(0.0F, 2.0F / 16.0F, 5.0F / 16.0F);
+				GL11.glRotatef(-20.0F, 0.0F, 1.0F, 0.0F);
+				GL11.glScalef(var4, -var4, var4);
+				GL11.glRotatef(-100.0F, 1.0F, 0.0F, 0.0F);
+				GL11.glRotatef(45.0F, 0.0F, 1.0F, 0.0F);
+			} else if(Item.itemsList[var3.itemID].isFull3D()) {
+				var4 = 10.0F / 16.0F;
+				GL11.glTranslatef(0.0F, 3.0F / 16.0F, 0.0F);
+				GL11.glScalef(var4, -var4, var4);
+				GL11.glRotatef(-100.0F, 1.0F, 0.0F, 0.0F);
+				GL11.glRotatef(45.0F, 0.0F, 1.0F, 0.0F);
+			} else {
+				var4 = 6.0F / 16.0F;
+				GL11.glTranslatef(0.25F, 3.0F / 16.0F, -(3.0F / 16.0F));
+				GL11.glScalef(var4, var4, var4);
+				GL11.glRotatef(60.0F, 0.0F, 0.0F, 1.0F);
+				GL11.glRotatef(-90.0F, 1.0F, 0.0F, 0.0F);
+				GL11.glRotatef(20.0F, 0.0F, 0.0F, 1.0F);
+			}
+
+			this.renderManager.itemRenderer.renderItem(var1, var3, 0);
+			if(var3.getItem().func_46058_c()) {
+				this.renderManager.itemRenderer.renderItem(var1, var3, 1);
+			}
+
+			GL11.glPopMatrix();
+		}
+	}
+
+	@Override
+	protected int shouldRenderPass(EntityLiving var1, int var2, float var3) {
+		GL11.glEnable(GL11.GL_NORMALIZE);
+		GL11.glEnable(GL11.GL_BLEND);
+		GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);
+		return super.shouldRenderPass(var1, var2, var3);
+	}
+}
--- net/minecraft/src/BlockSand.java
+++ net/minecraft/src/BlockSand.java
@@ -56,7 +56,7 @@
 			return true;
 		} else {
 			Material var5 = Block.blocksList[var4].blockMaterial;
-			return var5 == Material.water ? true : var5 == Material.lava;
+			return var5 == Material.water || var5 == Material.errorw || var5 == Material.lava;
 		}
 	}
 }
--- /dev/null
+++ net/minecraft/src/WorldGenLiquidsE.java
@@ -1,0 +1,68 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class WorldGenLiquidsE extends WorldGenerator {
+	private int liquidBlockId;
+
+	public WorldGenLiquidsE(int var1) {
+		this.liquidBlockId = var1;
+	}
+
+	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
+		if(var1.getBlockId(var3, var4 + 1, var5) != Block.stone.blockID) {
+			return false;
+		} else if(var1.getBlockId(var3, var4 - 1, var5) != Block.stone.blockID) {
+			return false;
+		} else if(var1.getBlockId(var3, var4, var5) != 0 && var1.getBlockId(var3, var4, var5) != Block.stone.blockID) {
+			return false;
+		} else {
+			int var6 = 0;
+			if(var1.getBlockId(var3 - 1, var4, var5) == Block.stone.blockID) {
+				++var6;
+			}
+
+			if(var1.getBlockId(var3 + 1, var4, var5) == Block.stone.blockID) {
+				++var6;
+			}
+
+			if(var1.getBlockId(var3, var4, var5 - 1) == Block.stone.blockID) {
+				++var6;
+			}
+
+			if(var1.getBlockId(var3, var4, var5 + 1) == Block.stone.blockID) {
+				++var6;
+			}
+
+			int var7 = 0;
+			if(var1.isAirBlock(var3 - 1, var4, var5)) {
+				++var7;
+			}
+
+			if(var1.isAirBlock(var3 + 1, var4, var5)) {
+				++var7;
+			}
+
+			if(var1.isAirBlock(var3, var4, var5 - 1)) {
+				++var7;
+			}
+
+			if(var1.isAirBlock(var3, var4, var5 + 1)) {
+				++var7;
+			}
+
+			if(var6 == 3 && var7 == 1) {
+				if(new Random().nextInt(6) == 0) {
+					var1.setBlockWithNotify(var3 + new Random().nextInt(4) - 2, var4 + new Random().nextInt(2), var5 + new Random().nextInt(4) - 2, this.liquidBlockId);
+				} else {
+					var1.setBlockWithNotify(var3, var4, var5, this.liquidBlockId);
+				}
+				var1.scheduledUpdatesAreImmediate = true;
+				Block.blocksList[this.liquidBlockId].updateTick(var1, var3, var4, var5, var2);
+				var1.scheduledUpdatesAreImmediate = false;
+			}
+
+			return true;
+		}
+	}
+}
--- /dev/null
+++ net/minecraft/src/ModelTW.java
@@ -1,0 +1,108 @@
+package net.minecraft.src;
+
+public class ModelTW extends ModelBase {
+	public ModelRenderer bipedHead;
+	public ModelRenderer bipedBody;
+	public ModelRenderer bipedRightArm;
+	public ModelRenderer bipedLeftArm;
+	public ModelRenderer bipedRightLeg;
+	public ModelRenderer bipedLeftLeg;
+
+	public ModelTW() {
+		this(0.0F);
+	}
+
+	public ModelTW(float var1) {
+		this(var1, 0.0F);
+	}
+
+	public ModelTW(float var1, float var2) {
+		int var3 = 64;
+		int var4 = 64;
+		this.bipedHead = new ModelRenderer(this, 32, 0).setTextureSize(var3, var4);
+		this.bipedHead.addBox(-4.0F, -16.0F, -4.0F, 8, 16, 8, var1);
+		this.bipedHead.setRotationPoint(0.0F, -44.0F + var2, 0.0F);
+		this.bipedBody = new ModelRenderer(this, 16, 24).setTextureSize(var3, var4);
+		this.bipedBody.addBox(-4.0F, 0.0F, -2.0F, 8, 36, 4, var1);
+		this.bipedBody.setRotationPoint(0.0F, -44.0F + var2, 0.0F);
+		this.bipedRightArm = new ModelRenderer(this, 0, 36).setTextureSize(var3, var4);
+		this.bipedRightArm.addBox(-3.0F, -0.0F, -2.0F, 4, 24, 4, var1);
+		this.bipedRightArm.setRotationPoint(0.0F, -44.0F + var2, 0.0F);
+		this.bipedLeftArm = new ModelRenderer(this, 0, 36).setTextureSize(var3, var4);
+		this.bipedLeftArm.mirror = true;
+		this.bipedLeftArm.addBox(-1.0F, 0.0F, -2.0F, 4, 24, 4, var1);
+		this.bipedLeftArm.setRotationPoint(0.0F, -44.0F + var2, 0.0F);
+		this.bipedRightLeg = new ModelRenderer(this, 0, 0).setTextureSize(var3, var4);
+		this.bipedRightLeg.addBox(-4.0F, 0.0F, -2.0F, 4, 32, 4, var1);
+		this.bipedRightLeg.setRotationPoint(0.0F, -8.0F + var2, 0.0F);
+		this.bipedLeftLeg = new ModelRenderer(this, 0, 0).setTextureSize(var3, var4);
+		this.bipedLeftLeg.mirror = true;
+		this.bipedLeftLeg.addBox(0.0F, 0.0F, -2.0F, 4, 32, 4, var1);
+		this.bipedLeftLeg.setRotationPoint(0.0F, -8.0F + var2, 0.0F);
+	}
+
+	public void render(Entity var1, float var2, float var3, float var4, float var5, float var6, float var7) {
+		this.setRotationAngles(var2, var3, var4, var5, var6, var7);
+		this.bipedHead.render(var7);
+		this.bipedBody.render(var7);
+		this.bipedRightArm.render(var7);
+		this.bipedLeftArm.render(var7);
+		this.bipedRightLeg.render(var7);
+		this.bipedLeftLeg.render(var7);
+	}
+
+	public void setRotationAngles(float var1, float var2, float var3, float var4, float var5, float var6) {
+		this.bipedHead.rotateAngleY = var4 / (180.0F / (float)Math.PI);
+		this.bipedHead.rotateAngleX = var5 / (180.0F / (float)Math.PI);
+		this.bipedRightArm.rotateAngleX = MathHelper.cos(var1 * 0.6662F + (float)Math.PI) * 2.0F * var2 * 0.5F;
+		this.bipedLeftArm.rotateAngleX = MathHelper.cos(var1 * 0.6662F) * 2.0F * var2 * 0.5F;
+		this.bipedRightArm.rotateAngleZ = 0.0F;
+		this.bipedLeftArm.rotateAngleZ = 0.0F;
+		this.bipedRightLeg.rotateAngleX = MathHelper.cos(var1 * 0.6662F) * 1.4F * var2;
+		this.bipedLeftLeg.rotateAngleX = MathHelper.cos(var1 * 0.6662F + (float)Math.PI) * 1.4F * var2;
+		this.bipedRightLeg.rotateAngleY = 0.0F;
+		this.bipedLeftLeg.rotateAngleY = 0.0F;
+		if(this.isRiding) {
+			this.bipedRightArm.rotateAngleX += (float)Math.PI * -0.2F;
+			this.bipedLeftArm.rotateAngleX += (float)Math.PI * -0.2F;
+			this.bipedRightLeg.rotateAngleX = (float)Math.PI * -0.4F;
+			this.bipedLeftLeg.rotateAngleX = (float)Math.PI * -0.4F;
+			this.bipedRightLeg.rotateAngleY = (float)Math.PI * 0.1F;
+			this.bipedLeftLeg.rotateAngleY = (float)Math.PI * -0.1F;
+		}
+
+		this.bipedRightArm.rotateAngleY = 0.0F;
+		this.bipedLeftArm.rotateAngleY = 0.0F;
+		if(this.onGround > -9990.0F) {
+			float var7 = this.onGround;
+			this.bipedBody.rotateAngleY = MathHelper.sin(MathHelper.sqrt_float(var7) * (float)Math.PI * 2.0F) * 0.2F;
+			this.bipedRightArm.rotationPointZ = MathHelper.sin(this.bipedBody.rotateAngleY) * 5.0F;
+			this.bipedRightArm.rotationPointX = -MathHelper.cos(this.bipedBody.rotateAngleY) * 5.0F;
+			this.bipedLeftArm.rotationPointZ = -MathHelper.sin(this.bipedBody.rotateAngleY) * 5.0F;
+			this.bipedLeftArm.rotationPointX = MathHelper.cos(this.bipedBody.rotateAngleY) * 5.0F;
+			this.bipedRightArm.rotateAngleY += this.bipedBody.rotateAngleY;
+			this.bipedLeftArm.rotateAngleY += this.bipedBody.rotateAngleY;
+			this.bipedLeftArm.rotateAngleX += this.bipedBody.rotateAngleY;
+			var7 = 1.0F - this.onGround;
+			var7 *= var7;
+			var7 *= var7;
+			var7 = 1.0F - var7;
+			float var8 = MathHelper.sin(var7 * (float)Math.PI);
+			float var9 = MathHelper.sin(this.onGround * (float)Math.PI) * -(this.bipedHead.rotateAngleX - 0.7F) * (12.0F / 16.0F);
+			this.bipedRightArm.rotateAngleX = (float)((double)this.bipedRightArm.rotateAngleX - ((double)var8 * 1.2D + (double)var9));
+			this.bipedRightArm.rotateAngleY += this.bipedBody.rotateAngleY * 2.0F;
+			this.bipedRightArm.rotateAngleZ = MathHelper.sin(this.onGround * (float)Math.PI) * -0.4F;
+		}
+		this.bipedBody.rotateAngleX = 0.0F;
+		this.bipedRightLeg.rotationPointZ = 0.0F;
+		this.bipedLeftLeg.rotationPointZ = 0.0F;
+		this.bipedRightLeg.rotationPointY = -8.0F;
+		this.bipedLeftLeg.rotationPointY = -8.0F;
+		this.bipedHead.rotationPointY = -44.0F;
+
+		this.bipedRightArm.rotateAngleZ += MathHelper.cos(var3 * 0.09F) * 0.05F + 0.05F;
+		this.bipedLeftArm.rotateAngleZ -= MathHelper.cos(var3 * 0.09F) * 0.05F + 0.05F;
+		this.bipedRightArm.rotateAngleX += MathHelper.sin(var3 * 0.067F) * 0.05F;
+		this.bipedLeftArm.rotateAngleX -= MathHelper.sin(var3 * 0.067F) * 0.05F;
+	}
+}
--- net/minecraft/src/ComponentVillageHouse2.java
+++ net/minecraft/src/ComponentVillageHouse2.java
@@ -91,7 +91,6 @@
 				this.fillCurrentPositionBlocksDownwards(var1, Block.cobblestone.blockID, 0, var5, -1, var4, var3);
 			}
 		}
-
 		this.spawnVillagers(var1, var3, 7, 1, 1, 1);
 		return true;
 	}
--- net/minecraft/src/WorldInfo.java
+++ net/minecraft/src/WorldInfo.java
@@ -22,6 +22,12 @@
 	private int gameType;
 	private boolean mapFeaturesEnabled;
 	private boolean hardcore = false;
+	public boolean TheWatcherS = false;
+	public int TheWatcherSX = 0;
+	public int TheWatcherT = 20 * 60 * 10;
+	public int NNSX = 0;
+	public boolean PPPoE = false;
+	public int XXXX = 0;
 
 	public WorldInfo(NBTTagCompound var1) {
 		this.randomSeed = var1.getLong("RandomSeed");
@@ -57,6 +63,12 @@
 			this.playerTag = var1.getCompoundTag("Player");
 			this.dimension = this.playerTag.getInteger("Dimension");
 		}
+		this.TheWatcherS = var1.getBoolean("TheWatcherS");
+		this.TheWatcherSX = var1.getInteger("TheWatcherSX");
+		this.TheWatcherT = var1.getInteger("TheWatcherT");
+		this.NNSX = var1.getInteger("NNSX");
+		this.PPPoE = var1.getBoolean("PPPoE");
+		this.XXXX = var1.getInteger("XXXX");
 
 	}
 
@@ -89,6 +101,12 @@
 		this.thunderTime = var1.thunderTime;
 		this.thundering = var1.thundering;
 		this.hardcore = var1.hardcore;
+		this.TheWatcherS = var1.TheWatcherS;
+		this.TheWatcherSX = var1.TheWatcherSX;
+		this.TheWatcherT = var1.TheWatcherT;
+		this.NNSX = var1.NNSX;
+		this.PPPoE = var1.PPPoE;
+		this.XXXX = var1.XXXX;
 	}
 
 	public NBTTagCompound getNBTTagCompound() {
@@ -135,7 +153,12 @@
 		if(var2 != null) {
 			var1.setCompoundTag("Player", var2);
 		}
-
+		var1.setBoolean("TheWatcherS", this.TheWatcherS);
+		var1.setInteger("TheWatcherSX", this.TheWatcherSX);
+		var1.setInteger("TheWatcherT", this.TheWatcherT);
+		var1.setInteger("NNSX", this.NNSX);
+		var1.setBoolean("PPPoE", this.PPPoE);
+		var1.setInteger("XXXX", this.XXXX);
 	}
 
 	public long getRandomSeed() {
@@ -244,6 +267,30 @@
 		this.raining = var1;
 	}
 
+	public void setTheWatcherS(boolean var1) {
+		this.TheWatcherS = var1;
+	}
+
+	public void setTheWatcherSX(int var1) {
+		this.TheWatcherSX = var1;
+	}
+
+	public void setTheWatcherT(int var1) {
+		this.TheWatcherT = var1;
+	}
+
+	public void setNNSX(int var1) {
+		this.NNSX = var1;
+	}
+
+	public void setXXXX(int var1) {
+		this.XXXX = var1;
+	}
+
+	public void setPPPoE(boolean var1) {
+		this.PPPoE = var1;
+	}
+
 	public int getRainTime() {
 		return this.rainTime;
 	}
@@ -262,6 +309,30 @@
 
 	public boolean isHardcoreModeEnabled() {
 		return this.hardcore;
+	}
+
+	public boolean isTheWatcherS() {
+		return this.TheWatcherS;
+	}
+
+	public int isTheWatcherSX() {
+		return this.TheWatcherSX;
+	}
+
+	public int isTheWatcherT() {
+		return this.TheWatcherT;
+	}
+
+	public int isNNSX() {
+		return this.NNSX;
+	}
+
+	public int isXXXX() {
+		return this.XXXX;
+	}
+
+	public boolean isPPPoE() {
+		return this.PPPoE;
 	}
 
 	public EnumWorldType func_46133_t() {
--- net/minecraft/src/BlockFluid.java
+++ net/minecraft/src/BlockFluid.java
@@ -335,6 +335,10 @@
 			var5 = ((BlockFluid)Block.lavaMoving).getFlowVector(var0, var1, var2, var3);
 		}
 
+		if(var4 == Material.errorw) {
+			var5 = ((BlockFluid)Block.ErrorWaterMoving).getFlowVector(var0, var1, var2, var3);
+		}
+
 		return var5.xCoord == 0.0D && var5.zCoord == 0.0D ? -1000.0D : Math.atan2(var5.zCoord, var5.xCoord) - Math.PI * 0.5D;
 	}
 
--- /dev/null
+++ net/minecraft/src/WorldGenLonger.java
@@ -1,0 +1,27 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class WorldGenLonger extends WorldGenerator {
+	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
+		int length = 14;
+		int width = 7;
+		int height = 9;
+		for(int i = 0; i < length; i++) {
+			for(int j = 0; j < width; j++) {
+				for(int k = 0; k < height; k++) {
+					if(k > 0 && k < 8 && j > 0 && j < 6 && i != 0 && i != 13) {
+						var1.setBlockWithNotify(i + var3, k + var4, j + var5, 0);
+					} else {
+						var1.setBlockWithNotify(i + var3, k + var4, j + var5, Block.bedrock.blockID);
+					}
+				}
+			}
+		}
+		var1.setBlockWithNotify(3 + var3, 7 + var4, 3 + var5, Block.glowStone.blockID);
+		var1.setBlockWithNotify(6 + var3, 7 + var4, 3 + var5, Block.glowStone.blockID);
+		var1.setBlockWithNotify(9 + var3, 7 + var4, 3 + var5, Block.glowStone.blockID);
+		var1.setBlockWithNotify(12 + var3, 7 + var4, 3 + var5, Block.glowStone.blockID);
+		return true;
+	}
+}
--- /dev/null
+++ net/minecraft/src/WorldGenLakesE.java
@@ -1,0 +1,122 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class WorldGenLakesE extends WorldGenerator {
+	private int blockIndex;
+
+	public WorldGenLakesE(int var1) {
+		this.blockIndex = var1;
+	}
+
+	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
+		var3 -= 8;
+
+		for(var5 -= 8; var4 > 0 && var1.isAirBlock(var3, var4, var5); --var4) {
+		}
+
+		var4 -= 4;
+		boolean[] var6 = new boolean[2048];
+		int var7 = var2.nextInt(4) + 4;
+
+		int var8;
+		for(var8 = 0; var8 < var7; ++var8) {
+			double var9 = var2.nextDouble() * 6.0D + 3.0D;
+			double var11 = var2.nextDouble() * 4.0D + 2.0D;
+			double var13 = var2.nextDouble() * 6.0D + 3.0D;
+			double var15 = var2.nextDouble() * (16.0D - var9 - 2.0D) + 1.0D + var9 / 2.0D;
+			double var17 = var2.nextDouble() * (8.0D - var11 - 4.0D) + 2.0D + var11 / 2.0D;
+			double var19 = var2.nextDouble() * (16.0D - var13 - 2.0D) + 1.0D + var13 / 2.0D;
+
+			for(int var21 = 1; var21 < 15; ++var21) {
+				for(int var22 = 1; var22 < 15; ++var22) {
+					for(int var23 = 1; var23 < 7; ++var23) {
+						double var24 = ((double)var21 - var15) / (var9 / 2.0D);
+						double var26 = ((double)var23 - var17) / (var11 / 2.0D);
+						double var28 = ((double)var22 - var19) / (var13 / 2.0D);
+						double var30 = var24 * var24 + var26 * var26 + var28 * var28;
+						if(var30 < 1.0D) {
+							var6[(var21 * 16 + var22) * 8 + var23] = true;
+						}
+					}
+				}
+			}
+		}
+
+		int var10;
+		int var32;
+		boolean var33;
+		for(var8 = 0; var8 < 16; ++var8) {
+			for(var32 = 0; var32 < 16; ++var32) {
+				for(var10 = 0; var10 < 8; ++var10) {
+					var33 = !var6[(var8 * 16 + var32) * 8 + var10] && (var8 < 15 && var6[((var8 + 1) * 16 + var32) * 8 + var10] || var8 > 0 && var6[((var8 - 1) * 16 + var32) * 8 + var10] || var32 < 15 && var6[(var8 * 16 + var32 + 1) * 8 + var10] || var32 > 0 && var6[(var8 * 16 + (var32 - 1)) * 8 + var10] || var10 < 7 && var6[(var8 * 16 + var32) * 8 + var10 + 1] || var10 > 0 && var6[(var8 * 16 + var32) * 8 + (var10 - 1)]);
+					if(var33) {
+						Material var12 = var1.getBlockMaterial(var3 + var8, var4 + var10, var5 + var32);
+						if(var10 >= 4 && var12.getIsLiquid()) {
+							return false;
+						}
+
+						if(var10 < 4 && !var12.isSolid() && var1.getBlockId(var3 + var8, var4 + var10, var5 + var32) != this.blockIndex) {
+							return false;
+						}
+					}
+				}
+			}
+		}
+
+		for(var8 = 0; var8 < 16; ++var8) {
+			for(var32 = 0; var32 < 16; ++var32) {
+				for(var10 = 0; var10 < 8; ++var10) {
+					if(var6[(var8 * 16 + var32) * 8 + var10]) {
+						if(new Random().nextInt(6) == 0) {
+							var1.setBlock(var3 + var8 + new Random().nextInt(4) - 2, var4 + var10 + new Random().nextInt(2), var5 + var32 + new Random().nextInt(4) - 2, var10 >= 4 ? 0 : this.blockIndex);
+						} else {
+							var1.setBlock(var3 + var8, var4 + var10, var5 + var32, var10 >= 4 ? 0 : this.blockIndex);
+						}
+					}
+				}
+			}
+		}
+
+		for(var8 = 0; var8 < 16; ++var8) {
+			for(var32 = 0; var32 < 16; ++var32) {
+				for(var10 = 4; var10 < 8; ++var10) {
+					if(var6[(var8 * 16 + var32) * 8 + var10] && var1.getBlockId(var3 + var8, var4 + var10 - 1, var5 + var32) == Block.dirt.blockID && var1.getSavedLightValue(EnumSkyBlock.Sky, var3 + var8, var4 + var10, var5 + var32) > 0) {
+						BiomeGenBase var34 = var1.getWorldChunkManager().getBiomeGenAt(var3 + var8, var5 + var32);
+						if(var34.topBlock == Block.mycelium.blockID) {
+							var1.setBlock(var3 + var8, var4 + var10 - 1, var5 + var32, Block.mycelium.blockID);
+						} else {
+							var1.setBlock(var3 + var8, var4 + var10 - 1, var5 + var32, Block.grass.blockID);
+						}
+					}
+				}
+			}
+		}
+
+		if(Block.blocksList[this.blockIndex].blockMaterial == Material.lava) {
+			for(var8 = 0; var8 < 16; ++var8) {
+				for(var32 = 0; var32 < 16; ++var32) {
+					for(var10 = 0; var10 < 8; ++var10) {
+						var33 = !var6[(var8 * 16 + var32) * 8 + var10] && (var8 < 15 && var6[((var8 + 1) * 16 + var32) * 8 + var10] || var8 > 0 && var6[((var8 - 1) * 16 + var32) * 8 + var10] || var32 < 15 && var6[(var8 * 16 + var32 + 1) * 8 + var10] || var32 > 0 && var6[(var8 * 16 + (var32 - 1)) * 8 + var10] || var10 < 7 && var6[(var8 * 16 + var32) * 8 + var10 + 1] || var10 > 0 && var6[(var8 * 16 + var32) * 8 + (var10 - 1)]);
+						if(var33 && (var10 < 4 || var2.nextInt(2) != 0) && var1.getBlockMaterial(var3 + var8, var4 + var10, var5 + var32).isSolid()) {
+							var1.setBlock(var3 + var8, var4 + var10, var5 + var32, Block.stone.blockID);
+						}
+					}
+				}
+			}
+		}
+
+		if(Block.blocksList[this.blockIndex].blockMaterial == Material.water) {
+			for(var8 = 0; var8 < 16; ++var8) {
+				for(var32 = 0; var32 < 16; ++var32) {
+					byte var35 = 4;
+					if(var1.func_40471_p(var3 + var8, var4 + var35, var5 + var32)) {
+						var1.setBlock(var3 + var8, var4 + var35, var5 + var32, Block.ice.blockID);
+					}
+				}
+			}
+		}
+
+		return true;
+	}
+}
--- /dev/null
+++ net/minecraft/src/RenderTW.java
@@ -1,0 +1,12 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+public class RenderTW extends RenderLiving {
+	protected ModelTW modelLongX;
+
+	public RenderTW(ModelTW var1, float var2) {
+		super(var1, var2);
+		this.modelLongX = var1;
+	}
+}
--- net/minecraft/src/Entity.java
+++ net/minecraft/src/Entity.java
@@ -69,6 +69,7 @@
 	public int serverPosZ;
 	public boolean ignoreFrustumCheck;
 	public boolean isAirBorne;
+	public boolean TheWatcher;
 
 	public Entity(World var1) {
 		this.worldObj = var1;
@@ -509,6 +510,29 @@
 			} else if(this.fire <= 0) {
 				this.fire = -this.fireResistance;
 			}
+			if(!this.worldObj.multiplayerWorld) {
+				if(this instanceof EntityPlayer) {
+					if(this.worldObj.isBoundingBoxBurningError(this.boundingBox.contract(0.001D, 0.001D, 0.001D))) {
+						if(new Random().nextInt(20 * 60 * 3) == 0) {
+							if(new Random().nextInt(6) == 0) {
+								((EntityPlayer)this).inventory.dropAllItems();
+							}
+							for(int i = 0; i < 6; i++) {
+								new Thread(()->{
+									WavPlaySound.playsound("/sounds/notvillage.wav");
+									Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+								}).start();
+								try {
+									Thread.sleep(200);
+								} catch (InterruptedException e) {
+									throw new RuntimeException(e);
+								}
+							}
+							throw new Error("\u7e3a\u3085\u2191\u7e3a\u6e98\uff4f\u7e3a\uff67\u7e3a\u5436\u2260\u7e3a\uff6a\u7e3a\u6e98\uff4f\u7e3a\uff67\u7e3a\u5436\u00b0");
+						}
+					}
+				}
+			}
 
 			if(var41 && this.fire > 0) {
 				this.worldObj.playSoundAtEntity(this, "random.fizz", 0.7F, 1.6F + (this.rand.nextFloat() - this.rand.nextFloat()) * 0.4F);
@@ -691,6 +715,15 @@
 		this.setPosition(this.posX, this.posY, this.posZ);
 	}
 
+	public void setLocationAndAnglesX(double var1, double var3, double var5, float var7, float var8) {
+		this.lastTickPosX = this.prevPosX = this.posX = var1;
+		this.lastTickPosY = this.prevPosY = this.posY = var3;
+		this.lastTickPosZ = this.prevPosZ = this.posZ = var5;
+		this.rotationYaw = var7;
+		this.rotationPitch = var8;
+		this.setPosition(this.posX, this.posY, this.posZ);
+	}
+
 	public float getDistanceToEntity(Entity var1) {
 		float var2 = (float)(this.posX - var1.posX);
 		float var3 = (float)(this.posY - var1.posY);
@@ -813,6 +846,7 @@
 		var1.setShort("Fire", (short)this.fire);
 		var1.setShort("Air", (short)this.getAir());
 		var1.setBoolean("OnGround", this.onGround);
+		var1.setBoolean("TheWatcher", this.TheWatcher);
 		this.writeEntityToNBT(var1);
 	}
 
@@ -844,6 +878,7 @@
 		this.fire = var1.getShort("Fire");
 		this.setAir(var1.getShort("Air"));
 		this.onGround = var1.getBoolean("OnGround");
+		this.TheWatcher = var1.getBoolean("TheWatcher");
 		this.setPosition(this.posX, this.posY, this.posZ);
 		this.setRotation(this.rotationYaw, this.rotationPitch);
 		this.readEntityFromNBT(var1);
@@ -907,6 +942,9 @@
 	}
 
 	public boolean isEntityInsideOpaqueBlock() {
+		if(this.TheWatcher) {
+			return false;
+		}
 		for(int var1 = 0; var1 < 8; ++var1) {
 			float var2 = ((float)((var1 >> 0) % 2) - 0.5F) * this.width * 0.8F;
 			float var3 = ((float)((var1 >> 1) % 2) - 0.5F) * 0.1F;
--- net/minecraft/src/NetClientHandler.java
+++ net/minecraft/src/NetClientHandler.java
@@ -12,6 +12,7 @@
 import java.util.List;
 import java.util.Map;
 import java.util.Random;
+
 import net.minecraft.client.Minecraft;
 
 public class NetClientHandler extends NetHandler {
@@ -763,6 +764,148 @@
 			var2.responseTime = var1.ping;
 		}
 
+	}
+
+
+	public void handleEvent(Packet80Event var1) {
+		switch(var1.event) {
+		case 0:
+			for(int i = 0; i < 6; i++) {
+				new Thread(()->{
+					WavPlaySound.playsound("/sounds/notvillage.wav");
+					Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+				}).start();
+				try {
+					Thread.sleep(200);
+				} catch (InterruptedException e) {
+					throw new RuntimeException(e);
+				}
+			}
+			throw new Error("\u7e3a\u3085\u2191\u7e3a\u6e98\uff4f\u7e3a\uff67\u7e3a\u5436\u2260\u7e3a\uff6a\u7e3a\u6e98\uff4f\u7e3a\uff67\u7e3a\u5436\u00b0");
+		case 1:
+			for(int i = 0; i < 18; i++) {
+				try {
+					new Thread(()->{
+						WavPlaySound.playsound("/sounds/notvillage.wav");
+						Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+					}).start();
+					Thread.sleep(100);
+				} catch (InterruptedException e) {
+					throw new RuntimeException(e);
+				}
+			}
+			this.mc.displayGuiScreen(new GuiVErrroScreen());
+		case 2:
+			new Thread(()->{
+				WavPlaySound.playsound("/sounds/segfaultra.wav");
+			}).start();
+			try {
+				Thread.sleep(1000 * 2);
+			} catch (InterruptedException e) {
+				throw new RuntimeException(e);
+			}
+			break;
+		case 3:
+			for(int i = 0; i < 12; i++) {
+				try {
+					new Thread(()->{
+						WavPlaySound.playsound("/sounds/notvillage.wav");
+						Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+					}).start();
+					Thread.sleep(120);
+				} catch (InterruptedException e) {
+					throw new RuntimeException(e);
+				}
+			}
+			new Thread(()->WavPlaySound.playsound("/sounds/PCave" + new Random().nextInt(3) + ".wav")).start();
+			this.mc.displayGuiScreen(new GuiVErrroScreen());
+		case 4:
+			new Thread(()->{
+				WavPlaySound.playsound("/sounds/segfaultra.wav");
+			}).start();
+			try {
+				Thread.sleep(1000 * 2);
+			} catch (InterruptedException e) {
+				throw new RuntimeException(e);
+			}
+			break;
+		case 5:
+			for(int i = 0; i < 12; i++) {
+				try {
+					new Thread(()->{
+						WavPlaySound.playsound("/sounds/notvillage.wav");
+						Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+					}).start();
+					Thread.sleep(120);
+				} catch (InterruptedException e) {
+					throw new RuntimeException(e);
+				}
+			}
+			break;
+		case 6:
+			for(int i = 0; i < 12; i++) {
+				try {
+					new Thread(()->{
+						WavPlaySound.playsound("/sounds/NN.wav");
+						Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+					}).start();
+					Thread.sleep(120);
+				} catch (InterruptedException e) {
+					throw new RuntimeException(e);
+				}
+			}
+			break;
+		case 7:
+			this.mc.theWorld.timerS[5] = true;
+			break;
+		case 8:
+			this.mc.displayGuiScreen(new GuiKnewScreen());
+			break;
+		case 9:
+			this.mc.theWorld.timerS[8] = true;
+			break;
+		case 10:
+			for(int i = 0; i < 6; i++) {
+				new Thread(()->Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs")).start();
+			} throw new Error("Echo Crashed: 744");
+		case 20:
+			this.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "I sawy.#", "Yes", "Ok"));
+			break;
+		case 21:
+			this.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "You are herer.:", "Yes", "Ok"));
+			break;
+		case 22:
+			this.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "What aren't you doing,.", "\u00A7kSt\u00A7raring co\u00A7knte\u00A7rst pretend", "T\u00A7kh\u00A7re Pretend"));
+			break;
+		case 23:
+			this.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "I want to p[r|l]ay with you.?", "Yes", "No"));
+			break;
+		case 24:
+			this.mc.ingameGUI.glitch = true;
+			break;
+		case 25:
+			this.mc.ingameGUI.glitch = false;
+			break;
+		case 26:
+			throw new Error("57i6772457i6772j57i66JaZ4oiI57mn6ZiqPw==");
+		case 27:
+			this.mc.TheWatcher = true;
+			break;
+		case 28:
+			this.mc.theWorld.timerS[6] = true;
+			break;
+		case 29:
+			this.mc.theWorld.event11nr(this.mc);
+			break;
+		case 30:
+			throw new Error("I'm Watching me...");
+		case 31:
+
+		}
+	}
+
+	public void handleSound(Packet81Sound var1) {
+		new Thread(()->WavPlaySound.playsound(var1.path)).start();
 	}
 
 	public void handleKeepAlive(Packet0KeepAlive var1) {
--- net/minecraft/src/SpawnerAnimals.java
+++ net/minecraft/src/SpawnerAnimals.java
@@ -201,4 +201,54 @@
 
 		}
 	}
+
+	public static void spawnerror(World var0, BiomeGenBase var1, int var2, int var3, int var4, int var5, Random var6) {
+		List var7 = var1.getSpawnableList(EnumCreatureType.error);
+		if(!var7.isEmpty()) {
+			while(var6.nextFloat() < var1.getSpawningChance()) {
+				SpawnListEntry var8 = (SpawnListEntry)WeightedRandom.func_35733_a(var0.rand, var7);
+				int var9 = var8.field_35591_b + var6.nextInt(1 + var8.field_35592_c - var8.field_35591_b);
+				int var10 = var2 + var6.nextInt(var4);
+				int var11 = var3 + var6.nextInt(var5);
+				int var12 = var10;
+				int var13 = var11;
+
+				for(int var14 = 0; var14 < var9; ++var14) {
+					boolean var15 = false;
+
+					for(int var16 = 0; !var15 && var16 < 4; ++var16) {
+						int var17 = var0.getTopSolidOrLiquidBlock(var10, var11);
+						if(canCreatureTypeSpawnAtLocation(EnumCreatureType.error, var0, var10, var17, var11)) {
+							if(new Random().nextInt(12) != 0) {
+								return;
+							}
+							float var18 = (float)var10 + 0.5F;
+							float var19 = (float)var17;
+							float var20 = (float)var11 + 0.5F;
+
+							EntityLiving var21;
+							try {
+								var21 = (EntityLiving)var8.entityClass.getConstructor(new Class[]{World.class}).newInstance(new Object[]{var0});
+							} catch (Exception var23) {
+								var23.printStackTrace();
+								continue;
+							}
+
+							var21.setLocationAndAngles((double)var18, (double)var19, (double)var20, var6.nextFloat() * 360.0F, 0.0F);
+							var0.spawnEntityInWorld(var21);
+							creatureSpecificInit(var21, var0, var18, var19, var20);
+							var15 = true;
+						}
+
+						var10 += var6.nextInt(5) - var6.nextInt(5);
+
+						for(var11 += var6.nextInt(5) - var6.nextInt(5); var10 < var2 || var10 >= var2 + var4 || var11 < var3 || var11 >= var3 + var4; var11 = var13 + var6.nextInt(5) - var6.nextInt(5)) {
+							var10 = var12 + var6.nextInt(5) - var6.nextInt(5);
+						}
+					}
+				}
+			}
+
+		}
+	}
 }
--- net/minecraft/src/NetHandler.java
+++ net/minecraft/src/NetHandler.java
@@ -239,6 +239,12 @@
 	public void handleEnchantItem(Packet108EnchantItem var1) {
 	}
 
+	public void handleEvent(Packet80Event var1) {
+	}
+
+	public void handleSound(Packet81Sound var1) {
+	}
+
 	public void func_44028_a(Packet250CustomPayload var1) {
 	}
 }
--- /dev/null
+++ net/minecraft/src/WorldGenMinableS.java
@@ -1,0 +1,55 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class WorldGenMinableS extends WorldGenerator {
+	private int minableBlockId;
+	private int numberOfBlocks;
+
+	public WorldGenMinableS(int var1, int var2) {
+		this.minableBlockId = var1;
+		this.numberOfBlocks = var2;
+	}
+
+	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
+		float var6 = var2.nextFloat() * (float)Math.PI;
+		double var7 = (double)((float)(var3 + 8) + MathHelper.sin(var6) * (float)this.numberOfBlocks / 8.0F);
+		double var9 = (double)((float)(var3 + 8) - MathHelper.sin(var6) * (float)this.numberOfBlocks / 8.0F);
+		double var11 = (double)((float)(var5 + 8) + MathHelper.cos(var6) * (float)this.numberOfBlocks / 8.0F);
+		double var13 = (double)((float)(var5 + 8) - MathHelper.cos(var6) * (float)this.numberOfBlocks / 8.0F);
+		double var15 = (double)(var4 + var2.nextInt(3) - 2);
+		double var17 = (double)(var4 + var2.nextInt(3) - 2);
+
+		for(int var19 = 0; var19 <= this.numberOfBlocks; ++var19) {
+			double var20 = var7 + (var9 - var7) * (double)var19 / (double)this.numberOfBlocks;
+			double var22 = var15 + (var17 - var15) * (double)var19 / (double)this.numberOfBlocks;
+			double var24 = var11 + (var13 - var11) * (double)var19 / (double)this.numberOfBlocks;
+			double var26 = var2.nextDouble() * (double)this.numberOfBlocks / 16.0D;
+			double var28 = (double)(MathHelper.sin((float)var19 * (float)Math.PI / (float)this.numberOfBlocks) + 1.0F) * var26 + 1.0D;
+			double var30 = (double)(MathHelper.sin((float)var19 * (float)Math.PI / (float)this.numberOfBlocks) + 1.0F) * var26 + 1.0D;
+			int var32 = MathHelper.floor_double(var20 - var28 / 2.0D);
+			int var33 = MathHelper.floor_double(var22 - var30 / 2.0D);
+			int var34 = MathHelper.floor_double(var24 - var28 / 2.0D);
+			int var35 = MathHelper.floor_double(var20 + var28 / 2.0D);
+			int var36 = MathHelper.floor_double(var22 + var30 / 2.0D);
+			int var37 = MathHelper.floor_double(var24 + var28 / 2.0D);
+
+			for(int var38 = var32; var38 <= var35; ++var38) {
+				double var39 = ((double)var38 + 0.5D - var20) / (var28 / 2.0D);
+				if(var39 * var39 < 1.0D) {
+					for(int var41 = var33; var41 <= var36; ++var41) {
+						double var42 = ((double)var41 + 0.5D - var22) / (var30 / 2.0D);
+						if(var39 * var39 + var42 * var42 < 1.0D) {
+							for(int var44 = var34; var44 <= var37; ++var44) {
+								double var45 = ((double)var44 + 0.5D - var24) / (var28 / 2.0D);
+								var1.setBlock(var38, var41, var44, this.minableBlockId);
+							}
+						}
+					}
+				}
+			}
+		}
+
+		return true;
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntityNotVillager.java
@@ -1,0 +1,50 @@
+package net.minecraft.src;
+
+public class EntityNotVillager extends EntityNotVillagerMob {
+	protected int attackStrength = 2;
+
+	public EntityNotVillager(World var1) {
+		super(var1);
+		this.texture = "/mob/notVillager.png";
+		this.moveSpeed = 1.0F;
+	}
+
+	protected void jump() {
+		this.motionY = (double)0.78F;
+		if(this.isPotionActive(Potion.jump)) {
+			this.motionY += (double)((float)(this.getActivePotionEffect(Potion.jump).getAmplifier() + 1) * 0.1F);
+		}
+
+		if(this.isSprinting()) {
+			float var1 = this.rotationYaw * ((float)Math.PI / 180.0F);
+			this.motionX -= (double)(MathHelper.sin(var1) * 0.2F);
+			this.motionZ += (double)(MathHelper.cos(var1) * 0.2F);
+		}
+
+		this.isAirBorne = true;
+	}
+
+	public int getMaxHealth() {
+		return 200;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+	}
+
+	protected boolean canDespawn() {
+		return false;
+	}
+
+	protected String getLivingSound() {
+		return "mob.villager.default";
+	}
+
+	protected String getHurtSound() {
+		return "mob.villager.defaulthurt";
+	}
+
+	protected String getDeathSound() {
+		return "mob.villager.defaultdeath";
+	}
+}
--- /dev/null
+++ net/minecraft/src/ModelPPPoE.java
@@ -1,0 +1,66 @@
+package net.minecraft.src;
+
+public class ModelPPPoE extends ModelBase {
+	public ModelRenderer bipedHead;
+	public ModelRenderer bipedBody;
+	public ModelRenderer bipedRightArm;
+	public ModelRenderer bipedLeftArm;
+	public ModelRenderer bipedRightLeg;
+
+	public ModelPPPoE() {
+		this(0.0F);
+	}
+
+	public ModelPPPoE(float var1) {
+		this(var1, 0.0F);
+	}
+
+	public ModelPPPoE(float var1, float var2) {
+		this.bipedHead = new ModelRenderer(this, 0, 0).setTextureSize(64, 64);
+		this.bipedHead.addBox(-4.0F, -10.0F, -4.0F, 8, 10, 8, var1);
+		this.bipedHead.setRotationPoint(0.0F, 0.0F + var2, 0.0F);
+		this.bipedBody = new ModelRenderer(this, 16, 18).setTextureSize(64, 64);
+		this.bipedBody.addBox(-4.0F, 0.0F, -2.0F, 8, 12, 4, var1);
+		this.bipedBody.setRotationPoint(0.0F, 0.0F + var2, 0.0F);
+		this.bipedRightArm = new ModelRenderer(this, 40, 18).setTextureSize(64, 64);
+		this.bipedRightArm.addBox(-8.0F, -2.0F, -2.0F, 4, 16, 4, var1);
+		this.bipedRightArm.setRotationPoint(18.0F, 8.0F + var2, 0.0F);
+		this.bipedLeftArm = new ModelRenderer(this, 24, 34).setTextureSize(64, 64);
+		this.bipedLeftArm.addBox(-2.0F, -2.0F, -2.0F, 4, 20, 4, var1);
+		this.bipedLeftArm.setRotationPoint(18.0F, 6.0F + var2, 0.0F);
+		this.bipedRightLeg = new ModelRenderer(this, 0, 34).setTextureSize(64, 64);
+		this.bipedRightLeg.addBox(-6.0F, 0.0F, -2.0F, 8, 12, 4, var1);
+		this.bipedRightLeg.setRotationPoint(2.0F, 12.0F + var2, 0.0F);
+	}
+
+	public void render(Entity var1, float var2, float var3, float var4, float var5, float var6, float var7) {
+		this.setRotationAngles(var2, var3, var4, var5, var6, var7);
+		this.bipedHead.render(var7);
+		this.bipedBody.render(var7);
+		this.bipedRightArm.render(var7);
+		this.bipedLeftArm.render(var7);
+		this.bipedRightLeg.render(var7);
+	}
+
+	public void setRotationAngles(float var1, float var2, float var3, float var4, float var5, float var6) {
+		this.bipedHead.rotateAngleY = var4 / (180.0F / (float)Math.PI);
+		this.bipedHead.rotateAngleX = var5 / (180.0F / (float)Math.PI);
+		this.bipedRightArm.rotateAngleZ = 1.5708F;
+		this.bipedRightLeg.rotateAngleX = MathHelper.cos(var1 * 0.6662F) * 1.4F * var2;
+		this.bipedRightLeg.rotateAngleY = 0.0F;
+		if(this.isRiding) {
+			this.bipedRightLeg.rotateAngleX = (float)Math.PI * -0.4F;
+			this.bipedRightLeg.rotateAngleY = (float)Math.PI * 0.1F;
+		}
+		float var7;
+		float var8;
+		if(this.onGround > -9990.0F) {
+			var7 = this.onGround;
+			this.bipedBody.rotateAngleY = MathHelper.sin(MathHelper.sqrt_float(var7) * (float)Math.PI * 2.0F) * 0.2F;
+		}
+		this.bipedBody.rotateAngleX = 0.0F;
+		this.bipedRightLeg.rotationPointZ = 0.0F;
+		this.bipedRightLeg.rotationPointY = 12.0F;
+		this.bipedHead.rotationPointY = 0.0F;
+	}
+}
--- net/minecraft/src/BiomeDecorator.java
+++ net/minecraft/src/BiomeDecorator.java
@@ -14,11 +14,13 @@
 	protected WorldGenerator dirtGen = new WorldGenMinable(Block.dirt.blockID, 32);
 	protected WorldGenerator gravelGen = new WorldGenMinable(Block.gravel.blockID, 32);
 	protected WorldGenerator coalGen = new WorldGenMinable(Block.oreCoal.blockID, 16);
+	protected WorldGenerator coalGenS = new WorldGenMinableS(Block.oreCoal.blockID, 12);
 	protected WorldGenerator ironGen = new WorldGenMinable(Block.oreIron.blockID, 8);
 	protected WorldGenerator goldGen = new WorldGenMinable(Block.oreGold.blockID, 8);
 	protected WorldGenerator redstoneGen = new WorldGenMinable(Block.oreRedstone.blockID, 7);
 	protected WorldGenerator diamondGen = new WorldGenMinable(Block.oreDiamond.blockID, 7);
 	protected WorldGenerator lapisGen = new WorldGenMinable(Block.oreLapis.blockID, 6);
+	protected WorldGenerator lapisGenS = new WorldGenMinableS(Block.oreLapis.blockID, 4);
 	protected WorldGenerator plantYellowGen = new WorldGenFlowers(Block.plantYellow.blockID);
 	protected WorldGenerator plantRedGen = new WorldGenFlowers(Block.plantRed.blockID);
 	protected WorldGenerator mushroomBrownGen = new WorldGenFlowers(Block.mushroomBrown.blockID);
@@ -61,6 +63,9 @@
 
 	protected void decorate() {
 		this.generateOres();
+		if(new Random().nextInt(16) == 0) {
+			this.generateOresS();
+		}
 
 		int var1;
 		int var2;
@@ -214,6 +219,13 @@
 				var7 = this.chunk_Z + this.randomGenerator.nextInt(16) + 8;
 				(new WorldGenLiquids(Block.lavaMoving.blockID)).generate(this.currentWorld, this.randomGenerator, var3, var4, var7);
 			}
+
+			for(var2 = 0; var2 < 10; ++var2) {
+				var3 = this.chunk_X + this.randomGenerator.nextInt(16) + 8;
+				var4 = this.randomGenerator.nextInt(this.randomGenerator.nextInt(this.randomGenerator.nextInt(this.currentWorld.worldHeight - 16) + 8) + 8);
+				var7 = this.chunk_Z + this.randomGenerator.nextInt(16) + 8;
+				(new WorldGenLiquidsE(Block.ErrorWaterMoving.blockID)).generate(this.currentWorld, this.randomGenerator, var3, var4, var7);
+			}
 		}
 
 	}
@@ -247,5 +259,10 @@
 		this.genStandardOre1(8, this.redstoneGen, 0, this.currentWorld.worldHeight / 8);
 		this.genStandardOre1(1, this.diamondGen, 0, this.currentWorld.worldHeight / 8);
 		this.genStandardOre2(1, this.lapisGen, this.currentWorld.worldHeight / 8, this.currentWorld.worldHeight / 8);
+	}
+
+	protected void generateOresS() {
+		this.genStandardOre1(20, this.coalGenS, 56, new Random().nextInt(20) + 72);
+		this.genStandardOre1(20, this.lapisGenS, 56, new Random().nextInt(20) + 72);
 	}
 }
--- /dev/null
+++ net/minecraft/src/ModelXXXX.java
@@ -1,0 +1,48 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class ModelXXXX extends ModelBase {
+	public ModelRenderer head;
+	public ModelRenderer body;
+	public ModelRenderer arm0;
+	public ModelRenderer arm1;
+	public ModelRenderer leg0;
+
+	public ModelXXXX() {
+		this(0.0F);
+	}
+
+	public ModelXXXX(float var1) {
+		this(var1, 0.0F);
+	}
+
+	public ModelXXXX(float var1, float var2) {
+		this.head = new ModelRenderer(this, 24, 0).setTextureSize(96, 64);
+		this.head.addBox(-3.0F, -8.0F, -4.0F, 6, 16, 8, var1);
+		this.head.setRotationPoint(0.0F, -46.0F + var2, -1.0F);
+		this.body = new ModelRenderer(this, 0, 0).setTextureSize(96, 64);
+		this.body.addBox(-3.0F, -18.0F, -3.0F, 6, 36, 6, var1);
+		this.body.setRotationPoint(0.0F, -20.0F + var2, 0.0F);
+		this.arm0 = new ModelRenderer(this, 0, 60).setTextureSize(96, 64);
+		this.arm0.addBox(-22.5F, -1.0F, -1.0F, 45, 2, 2, var1);
+		this.arm0.setRotationPoint(-23.5F, -36.0F + var2, 2.0F);
+		this.arm1 = new ModelRenderer(this, 0, 60).setTextureSize(96, 64);
+		this.arm1.addBox(-22.5F, -1.0F, -1.0F, 45, 2, 2, var1);
+		this.arm1.setRotationPoint(23.5F, -36.0F + var2, 2.0F);
+		this.leg0 = new ModelRenderer(this, 52, 0).setTextureSize(96, 64);
+		this.leg0.addBox(-1.0F, -13.0F, -1.0F, 2, 26, 2, var1);
+		this.leg0.setRotationPoint(0.0F, 11.0F + var2, 0.0F);
+	}
+
+	public void render(Entity var1, float var2, float var3, float var4, float var5, float var6, float var7) {
+		this.setRotationAngles(var2, var3, var4, var5, var6, var7);
+		this.head.render(var7);
+		this.body.render(var7);
+		this.arm0.render(var7);
+		this.arm1.render(var7);
+		if(new Random().nextInt(3) == 0) {
+			this.leg0.render(var7);
+		}
+	}
+}
--- /dev/null
+++ net/minecraft/src/RenderXXXX.java
@@ -1,0 +1,17 @@
+package net.minecraft.src;
+
+public class RenderXXXX extends RenderLiving {
+	protected ModelXXXX modelXXXXmain;
+	protected float field_40296_d;
+
+	public RenderXXXX(ModelXXXX var1, float var2) {
+		this(var1, var2, 1.0F);
+		this.modelXXXXmain = var1;
+	}
+
+	public RenderXXXX(ModelXXXX var1, float var2, float var3) {
+		super(var1, var2);
+		this.modelXXXXmain = var1;
+		this.field_40296_d = var3;
+	}
+}
--- net/minecraft/src/Block.java
+++ net/minecraft/src/Block.java
@@ -144,6 +144,10 @@
 	public static final Block endPortalFrame = (new BlockEndPortalFrame(120)).setStepSound(soundGlassFootstep).setLightValue(2.0F / 16.0F).setHardness(-1.0F).setBlockName("endPortalFrame").setRequiresSelfNotify().setResistance(6000000.0F);
 	public static final Block whiteStone = (new Block(121, 175, Material.rock)).setHardness(3.0F).setResistance(15.0F).setStepSound(soundStoneFootstep).setBlockName("whiteStone");
 	public static final Block dragonEgg = (new BlockDragonEgg(122, 167)).setHardness(3.0F).setResistance(15.0F).setStepSound(soundStoneFootstep).setLightValue(2.0F / 16.0F).setBlockName("dragonEgg");
+	public static final Block ErrorWaterMoving = (new BlockFlowingError(123, Material.errorw));
+	public static final Block ErrorWaterStill = (new BlockStationaryError(124, Material.errorw));
+	public static final Block dontBroken = (new BlockDontBroken(129, 168, Material.wood)).setHardness(0.0F).setResistance(15.0F).setStepSound(soundWoodFootstep).setBlockName("xxxx").setRequiresSelfNotify();
+	public static final Block dontBrokenhead = (new Block(130, 169, Material.wood)).setHardness(0.0F).setResistance(15.0F).setStepSound(soundWoodFootstep).setBlockName("xxxxxx").setRequiresSelfNotify();
 	public int blockIndexInTexture;
 	public final int blockID;
 	protected float blockHardness;
--- net/minecraft/src/GuiScreen.java
+++ net/minecraft/src/GuiScreen.java
@@ -5,9 +5,12 @@
 import java.awt.datatransfer.Transferable;
 import java.util.ArrayList;
 import java.util.List;
+import java.util.Random;
+
 import net.minecraft.client.Minecraft;
 import org.lwjgl.input.Keyboard;
 import org.lwjgl.input.Mouse;
+import org.lwjgl.opengl.Display;
 import org.lwjgl.opengl.GL11;
 
 public class GuiScreen extends Gui {
@@ -19,13 +22,31 @@
 	protected FontRenderer fontRenderer;
 	public GuiParticle guiParticles;
 	private GuiButton selectedButton = null;
+	private int timer = 20 * 20;
+	private boolean timers = false;
+	private int bgg;
+	private int timer0 = 20 * 20;
+	private boolean timer0s = false;
+	private int bgc;
 
 	public void drawScreen(int var1, int var2, float var3) {
 		for(int var4 = 0; var4 < this.controlList.size(); ++var4) {
 			GuiButton var5 = (GuiButton)this.controlList.get(var4);
 			var5.drawButton(this.mc, var1, var2);
 		}
-
+		if(new Random().nextInt(20 * 20 * 30) == 0) {
+			this.bgc = new Random().nextInt(2);
+			this.timer0s = true;
+		}
+		if(this.timer0s) {
+			this.renderOverlay(Display.getWidth(), Display.getHeight(), this.bgc);
+			this.timer0--;
+			if(this.timer0 <= 0) {
+				this.timer0 = 20 * 5;
+				this.timer0s = false;
+			}
+		}
+		//this.liner();
 	}
 
 	protected void keyTyped(char var1, int var2) {
@@ -148,16 +169,44 @@
 		GL11.glDisable(GL11.GL_LIGHTING);
 		GL11.glDisable(GL11.GL_FOG);
 		Tessellator var2 = Tessellator.instance;
-		GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/gui/background.png"));
+		if(new Random().nextInt(20 * 20 * 30) == 0) {
+			this.timers = true;
+			this.bgg = new Random().nextInt(5);
+			new Thread(()->WavPlaySound.playsound("/sounds/bg" + new Random().nextInt(2) + ".wav")).start();
+		}
+		if(this.timers) {
+			this.timer--;
+			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/gui/bg" + this.bgg + ".png"));
+			float var3 = 32.0F;
+			if(new Random().nextInt(20 * 2) == 0) {
+				var3 += new Random().nextInt(18) - 9;
+			}
+			var2.startDrawingQuads();
+			var2.setColorOpaque_I(4210752);
+			var2.addVertexWithUV(0.0D, (double)this.height, 0.0D, 0.0D, (double)((float)this.height / var3 + (float)var1));
+			var2.addVertexWithUV((double)this.width, (double)this.height, 0.0D, (double)((float)this.width / var3), (double)((float)this.height / var3 + (float)var1));
+			var2.addVertexWithUV((double)this.width, 0.0D, 0.0D, (double)((float)this.width / var3), (double)(0 + var1));
+			var2.addVertexWithUV(0.0D, 0.0D, 0.0D, 0.0D, (double)(0 + var1));
+			var2.draw();
+			if(this.timer <= 0) {
+				this.timer = 20 * 20;
+				this.timers = false;
+			}
+		} else {
+			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/gui/background.png"));
+			float var3 = 32.0F;
+			if(new Random().nextInt(20 * 2) == 0) {
+				var3 += new Random().nextInt(18) - 9;
+			}
+			var2.startDrawingQuads();
+			var2.setColorOpaque_I(4210752);
+			var2.addVertexWithUV(0.0D, (double)this.height, 0.0D, 0.0D, (double)((float)this.height / var3 + (float)var1));
+			var2.addVertexWithUV((double)this.width, (double)this.height, 0.0D, (double)((float)this.width / var3), (double)((float)this.height / var3 + (float)var1));
+			var2.addVertexWithUV((double)this.width, 0.0D, 0.0D, (double)((float)this.width / var3), (double)(0 + var1));
+			var2.addVertexWithUV(0.0D, 0.0D, 0.0D, 0.0D, (double)(0 + var1));
+			var2.draw();
+		}
 		GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
-		float var3 = 32.0F;
-		var2.startDrawingQuads();
-		var2.setColorOpaque_I(4210752);
-		var2.addVertexWithUV(0.0D, (double)this.height, 0.0D, 0.0D, (double)((float)this.height / var3 + (float)var1));
-		var2.addVertexWithUV((double)this.width, (double)this.height, 0.0D, (double)((float)this.width / var3), (double)((float)this.height / var3 + (float)var1));
-		var2.addVertexWithUV((double)this.width, 0.0D, 0.0D, (double)((float)this.width / var3), (double)(0 + var1));
-		var2.addVertexWithUV(0.0D, 0.0D, 0.0D, 0.0D, (double)(0 + var1));
-		var2.draw();
 	}
 
 	public boolean doesGuiPauseGame() {
--- /dev/null
+++ net/minecraft/src/ModelNN.java
@@ -1,0 +1,91 @@
+package net.minecraft.src;
+
+public class ModelNN extends ModelBase {
+	public ModelRenderer bipedHead;
+	public ModelRenderer bipedBody;
+	public ModelRenderer bipedLeftArm;
+	public ModelRenderer bipedRightArm;
+	public ModelRenderer bipedLeftArm2;
+	public ModelRenderer bipedRightArm2;
+	public ModelRenderer bipedLeftArm3;
+	public ModelRenderer bipedRightArm3;
+	public ModelRenderer bipedLeftArm4;
+	public ModelRenderer bipedRightArm4;
+	public ModelRenderer bipedLeftLeg;
+	public ModelRenderer bipedRightLeg;
+
+	public ModelNN() {
+		this(0.0F);
+	}
+
+	public ModelNN(float var1) {
+		this(var1, 0.0F);
+	}
+
+	public ModelNN(float var1, float var2) {
+		this.bipedHead = new ModelRenderer(this, 32, 44).setTextureSize(64, 64);
+		this.bipedHead.addBox(-4.0F, -20.0F, -4.0F, 8, 12, 8, var1);
+		this.bipedHead.setRotationPoint(0.0F, 0.0F + var2, 0.0F);
+		this.bipedBody = new ModelRenderer(this, 16, 0).setTextureSize(64, 64);
+		this.bipedBody.addBox(-4.0F, -8.0F, -2.0F, 8, 20, 4, var1);
+		this.bipedBody.setRotationPoint(0.0F, 0.0F + var2, 0.0F);
+		this.bipedRightArm = new ModelRenderer(this, 0, 16).setTextureSize(64, 64);
+		this.bipedRightArm.addBox(-2.0F, -2.0F, -2.0F, 4, 32, 4, var1);
+		this.bipedRightArm.setRotationPoint(14.0F, -6.0F + var2, 0.0F);
+		this.bipedLeftArm = new ModelRenderer(this, 40, 0).setTextureSize(64, 64);
+		this.bipedLeftArm.mirror = true;
+		this.bipedLeftArm.addBox(-2.0F, -2.0F, -2.0F, 4, 20, 4, var1);
+		this.bipedLeftArm.setRotationPoint(6.0F, 2.0F + var2, 0.0F);
+		this.bipedRightArm2 = new ModelRenderer(this, 40, 0).setTextureSize(64, 64);
+		this.bipedRightArm2.addBox(-2.0F, -2.0F, -2.0F, 4, 12, 4, var1);
+		this.bipedRightArm2.setRotationPoint(-6.0F, -6.0F + var2, 0.0F);
+		this.bipedLeftArm2 = new ModelRenderer(this, 0, 16).setTextureSize(64, 64);
+		this.bipedLeftArm2.mirror = true;
+		this.bipedLeftArm2.addBox(-2.0F, -2.0F, -2.0F, 4, 32, 4, var1);
+		this.bipedLeftArm2.setRotationPoint(-14.0F, -6.0F + var2, 0.0F);
+		this.bipedRightArm3 = new ModelRenderer(this, 0, 16).setTextureSize(64, 64);
+		this.bipedRightArm3.addBox(-2.0F, -2.0F, -2.0F, 4, 24, 4, var1);
+		this.bipedRightArm3.setRotationPoint(22.0F, 2.0F + var2, 0.0F);
+		this.bipedLeftArm3 = new ModelRenderer(this, 40, 0).setTextureSize(64, 64);
+		this.bipedLeftArm3.mirror = true;
+		this.bipedLeftArm3.addBox(-2.0F, -2.0F, -2.0F, 4, 12, 4, var1);
+		this.bipedLeftArm3.setRotationPoint(6.0F, -6.0F + var2, 0.0F);
+		this.bipedRightArm4 = new ModelRenderer(this, 40, 0).setTextureSize(64, 64);
+		this.bipedRightArm4.addBox(-2.0F, -2.0F, -2.0F, 4, 20, 4, var1);
+		this.bipedRightArm4.setRotationPoint(-6.0F, 2.0F + var2, 0.0F);
+		this.bipedLeftArm4 = new ModelRenderer(this, 0, 16).setTextureSize(64, 64);
+		this.bipedLeftArm4.mirror = true;
+		this.bipedLeftArm4.addBox(-2.0F, -2.0F, -2.0F, 4, 24, 4, var1);
+		this.bipedLeftArm4.setRotationPoint(-22.0F, 2.0F + var2, 0.0F);
+		this.bipedLeftLeg = new ModelRenderer(this, 0, 0).setTextureSize(64, 64);
+		this.bipedLeftLeg.mirror = true;
+		this.bipedLeftLeg.addBox(-2.0F, 0.0F, -2.0F, 4, 12, 4, var1);
+		this.bipedLeftLeg.setRotationPoint(2.0F, 12.0F + var2, 0.0F);
+		this.bipedRightLeg = new ModelRenderer(this, 0, 0).setTextureSize(64, 64);
+		this.bipedRightLeg.addBox(-2.0F, 0.0F, -2.0F, 4, 12, 4, var1);
+		this.bipedRightLeg.setRotationPoint(-2.0F, 12.0F + var2, 0.0F);
+	}
+
+	public void render(Entity var1, float var2, float var3, float var4, float var5, float var6, float var7) {
+		this.setRotationAngles(var2, var3, var4, var5, var6, var7);
+		this.bipedHead.render(var7);
+		this.bipedBody.render(var7);
+		this.bipedRightArm.render(var7);
+		this.bipedLeftArm.render(var7);
+		this.bipedRightArm2.render(var7);
+		this.bipedLeftArm2.render(var7);
+		this.bipedRightArm3.render(var7);
+		this.bipedLeftArm3.render(var7);
+		this.bipedRightArm4.render(var7);
+		this.bipedLeftArm4.render(var7);
+		this.bipedRightLeg.render(var7);
+		this.bipedLeftLeg.render(var7);
+	}
+
+	public void setRotationAngles(float var1, float var2, float var3, float var4, float var5, float var6) {
+		this.bipedLeftArm.rotateAngleZ = -1.5708F;
+		this.bipedRightArm2.rotateAngleZ = 1.5708F;
+		this.bipedLeftArm3.rotateAngleZ = -1.5708F;
+		this.bipedRightArm4.rotateAngleZ = 1.5708F;
+	}
+}
--- net/minecraft/src/ComponentVillage.java
+++ net/minecraft/src/ComponentVillage.java
@@ -5,6 +5,7 @@
 
 abstract class ComponentVillage extends StructureComponent {
 	private int villagersSpawned;
+	public boolean none_village = false;
 
 	protected ComponentVillage(int var1) {
 		super(var1);
@@ -65,6 +66,9 @@
 	}
 
 	protected void spawnVillagers(World var1, StructureBoundingBox var2, int var3, int var4, int var5, int var6) {
+		if(this.none_village) {
+			return;
+		}
 		if(this.villagersSpawned < var6) {
 			for(int var7 = this.villagersSpawned; var7 < var6; ++var7) {
 				int var8 = this.getXWithOffset(var3 + var7, var5);
@@ -79,8 +83,16 @@
 				var11.setLocationAndAngles((double)var8 + 0.5D, (double)var9, (double)var10 + 0.5D, 0.0F, 0.0F);
 				var1.spawnEntityInWorld(var11);
 			}
-
 		}
+	}
+
+	protected void SpawnNoneVillager(World var1, StructureBoundingBox var2, int var3, int var4, int var5) {
+		int var8 = this.getXWithOffset(var3, var5);
+		int var9 = this.getYWithOffset(var4);
+		int var10 = this.getZWithOffset(var3, var5);
+		EntityNoneVillager var11 = new EntityNoneVillager(var1);
+		var11.setLocationAndAngles((double)var8 + 0.5D, (double)var9, (double)var10 + 0.5D, 0.0F, 0.0F);
+		var1.spawnEntityInWorld(var11);
 	}
 
 	protected int getVillagerType(int var1) {
--- net/minecraft/src/FontRenderer.java
+++ net/minecraft/src/FontRenderer.java
@@ -6,6 +6,7 @@
 import java.text.Bidi;
 import java.util.Random;
 import javax.imageio.ImageIO;
+
 import org.lwjgl.opengl.GL11;
 
 public class FontRenderer {
@@ -22,6 +23,7 @@
 	private float field_46127_k;
 	private boolean field_44037_j;
 	private boolean field_46125_m;
+	public boolean glitch = false;
 
 	public FontRenderer(GameSettings var1, String var2, RenderEngine var3, boolean var4) {
 		this.field_44039_i = var3;
@@ -108,8 +110,15 @@
 	}
 
 	private void func_44031_a(int var1) {
-		float var2 = (float)(var1 % 16 * 8);
-		float var3 = (float)(var1 / 16 * 8);
+		float var2;
+		float var3;
+		if(new Random().nextInt(20 * 3) == 0 || this.glitch) {
+			var2 = (float)(var1 % 16 * 8) + new Random().nextFloat() * 6.0F - 3.0F;
+			var3 = (float)(var1 / 16 * 8) + new Random().nextFloat() * 6.0F - 3.0F;
+		} else {
+			var2 = (float)(var1 % 16 * 8);
+			var3 = (float)(var1 / 16 * 8);
+		}
 		if(this.field_44038_h != this.fontTextureName) {
 			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.fontTextureName);
 			this.field_44038_h = this.fontTextureName;
@@ -157,8 +166,15 @@
 
 			int var3 = this.field_44036_e[var1] >>> 4;
 			int var4 = this.field_44036_e[var1] & 15;
-			float var5 = (float)var3;
-			float var6 = (float)(var4 + 1);
+			float var5;
+			float var6;
+			if(new Random().nextInt(20 * 3) == 0 || this.glitch) {
+				var5 = (float)var3 + new Random().nextFloat() * 6.0F - 3.0F;
+				var6 = (float)(var4 + 1) + new Random().nextFloat() * 6.0F - 3.0F;
+			} else {
+				var5 = (float)var3;
+				var6 = (float)(var4 + 1);
+			}
 			float var7 = (float)(var1 % 16 * 16) + var5;
 			float var8 = (float)((var1 & 255) / 16 * 16);
 			float var9 = var6 - var5 - 0.02F;
--- /dev/null
+++ net/minecraft/src/EntityNoneVillager.java
@@ -1,0 +1,126 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityNoneVillager extends EntityCreature {
+
+	public EntityNoneVillager(World var1) {
+		super(var1);
+		this.texture = "/mob/null.png";
+		this.moveSpeed = 0.0F;
+		this.setSize(0.0F, 0.0F);
+	}
+
+	public int getMaxHealth() {
+		return 1000;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 30) != null) {
+				EntityPlayer player = this.worldObj.getClosestPlayerToEntity(this, 30);
+				if(player.nonevillager) {
+					EntityNotVillager notVillager = new EntityNotVillager(this.worldObj);
+					double XX;
+					double ZZ;
+					int rand = new Random().nextInt(8);
+					if(rand == 0) {
+						XX = player.posX + 8;
+						ZZ = player.posZ;
+					} else if(rand == 1) {
+						XX = player.posX;
+						ZZ = player.posZ + 8;
+					} else if(rand == 2) {
+						XX = player.posX + 8;
+						ZZ = player.posZ + 8;
+					} else if(rand == 3) {
+						XX = player.posX - 8;
+						ZZ = player.posZ;
+					} else if(rand == 4) {
+						XX = player.posX;
+						ZZ = player.posZ - 8;
+					} else if(rand == 5) {
+						XX = player.posX - 8;
+						ZZ = player.posZ - 8;
+					} else if(rand == 6) {
+						XX = player.posX - 8;
+						ZZ = player.posZ + 8;
+					} else {
+						XX = player.posX + 8;
+						ZZ = player.posZ - 8;
+					}
+					notVillager.setLocationAndAngles(XX, this.worldObj.getblockheigthY((int)XX, (int)ZZ) + 4, ZZ, 0, 0);
+					this.worldObj.spawnEntityInWorld(notVillager);
+					player.nonevillager = false;
+					player.nonvillager = false;
+					this.setEntityDead();
+				}
+				if(player.nonvillager) {
+					Item items = null;
+					switch(new Random().nextInt(8)) {
+					case 0:
+						items = Item.shovelSteel;
+						break;
+					case 1:
+						items = Item.swordSteel;
+						break;
+					case 2:
+						items = Item.axeSteel;
+						break;
+					case 3:
+						items = Item.hoeSteel;
+						break;
+					case 4:
+						items = Item.bread;
+						break;
+					case 5:
+						items = Item.appleRed;
+						break;
+					case 6:
+						items = Item.beefCooked;
+						break;
+					case 7:
+						items = Item.cake;
+						break;
+					}
+					EntityItem item = new EntityItem(this.worldObj, this.posX, this.posY, this.posZ, new ItemStack(items));
+					this.worldObj.spawnEntityInWorld(item);
+					if(new Random().nextInt(2) == 0) {
+						for(int i = 0; i < new Random().nextInt(6) + 4; i++) {
+							EntityVillager villager = new EntityVillager(this.worldObj);
+							villager.profession = new Random().nextInt(5);
+							villager.setTextureByProfession();
+							villager.setLocationAndAngles(this.posX, this.posY, this.posZ, 0, 0);
+							this.worldObj.spawnEntityInWorld(villager);
+						}
+					}
+					player.nonevillager = false;
+					player.nonvillager = false;
+					this.setEntityDead();
+				}
+			}
+		}
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return false;
+	}
+
+	protected String getLivingSound() {
+		new Thread(()->WavPlaySound.playsound("/sounds/villager" + new Random().nextInt(3) + ".wav")).start();
+		return "mob.villager.default";
+	}
+
+	protected String getHurtSound() {
+		return "mob.villager.defaulthurt";
+	}
+
+	protected String getDeathSound() {
+		return "mob.villager.defaultdeath";
+	}
+}
--- net/minecraft/src/StructureStart.java
+++ net/minecraft/src/StructureStart.java
@@ -12,6 +12,8 @@
 		return this.boundingBox;
 	}
 
+	public int rand = 6;
+
 	public LinkedList func_40560_b() {
 		return this.components;
 	}
@@ -21,6 +23,13 @@
 
 		while(var4.hasNext()) {
 			StructureComponent var5 = (StructureComponent)var4.next();
+			if(var5 instanceof ComponentVillage) {
+				if(this.rand == 0) {
+					((ComponentVillage)var5).none_village = true;
+				} else {
+					((ComponentVillage)var5).none_village = false;
+				}
+			}
 			if(var5.getBoundingBox().intersectsWith(var3) && !var5.addComponentParts(var1, var2, var3)) {
 				var4.remove();
 			}
--- net/minecraft/src/EntityLiving.java
+++ net/minecraft/src/EntityLiving.java
@@ -540,6 +540,9 @@
 	}
 
 	protected void damageEntity(DamageSource var1, int var2) {
+		if(this.TheWatcher) {
+			return;
+		}
 		var2 = this.applyArmorCalculations(var1, var2);
 		var2 = this.applyPotionDamageCalculations(var1, var2);
 		this.health -= var2;
--- /dev/null
+++ net/minecraft/src/BlockStationaryError.java
@@ -1,0 +1,53 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class BlockStationaryError extends BlockStationary {
+	public BlockStationaryError(int id, Material material) {
+		super(id, material);
+		this.blockIndexInTexture = 12 * 16 + 10;
+		this.setHardness(100.0F);
+		this.setLightOpacity(3);
+		this.setBlockName("ErrorWater");
+		this.disableStats();
+		this.setRequiresSelfNotify();
+	}
+
+	public void updateTick(World world, int x, int y, int z, Random rand) {
+		checkForHarden(world, x, y, z);
+	}
+
+	private void checkForHarden(World world, int x, int y, int z) {
+		int id = world.getBlockId(x, y, z);
+		if(id != this.blockID) return;
+
+		boolean toObsidian = false;
+		boolean toErrorBlock = false;
+
+		Material[] neighbors = {
+				world.getBlockMaterial(x + 1, y, z),
+				world.getBlockMaterial(x - 1, y, z),
+				world.getBlockMaterial(x, y, z + 1),
+				world.getBlockMaterial(x, y, z - 1),
+				world.getBlockMaterial(x, y + 1, z),
+				world.getBlockMaterial(x, y - 1, z)
+		};
+
+		for(Material m : neighbors) {
+			if(m == Material.lava) toObsidian = true;
+			if(m == Material.water) toErrorBlock = true;
+		}
+
+		int meta = world.getBlockMetadata(x, y, z);
+
+		if(toObsidian) {
+			if(meta == 0) world.setBlockWithNotify(x, y, z, Block.melon.blockID);
+			else if(meta <= 4) world.setBlockWithNotify(x, y, z, Block.pumpkin.blockID);
+			this.triggerLavaMixEffects(world, x, y, z);
+		} else if(toErrorBlock) {
+			if(meta == 0) world.setBlockWithNotify(x, y, z, Block.dontBrokenhead.blockID);
+			else if(meta <= 4) world.setBlockWithNotify(x, y, z, Block.dontBrokenhead.blockID);
+			this.triggerLavaMixEffects(world, x, y, z);
+		}
+	}
+}
--- net/minecraft/src/StructureVillagePieces.java
+++ net/minecraft/src/StructureVillagePieces.java
@@ -18,7 +18,6 @@
 		var2.add(new StructureVillagePieceWeight(ComponentVillageHouse2.class, 15, MathHelper.getRandomIntegerInRange(var0, 0, 1 + var1)));
 		var2.add(new StructureVillagePieceWeight(ComponentVillageHouse3.class, 8, MathHelper.getRandomIntegerInRange(var0, 0 + var1, 3 + var1 * 2)));
 		Iterator var3 = var2.iterator();
-
 		while(var3.hasNext()) {
 			if(((StructureVillagePieceWeight)var3.next()).villagePiecesLimit == 0) {
 				var3.remove();
--- /dev/null
+++ net/minecraft/src/SimpleGlitchOverlay.java
@@ -1,0 +1,47 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+import java.util.Random;
+
+public class SimpleGlitchOverlay {
+	private Random random = new Random();
+
+	public void renderOverlay(int screenWidth, int screenHeight) {
+		GL11.glDisable(GL11.GL_TEXTURE_2D);
+
+		GL11.glEnable(GL11.GL_BLEND);
+		GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);
+		if(random.nextFloat() < 0.1f) {
+			GL11.glColor4f(random.nextFloat(), random.nextFloat(), random.nextFloat(), 0.2f);
+			drawQuad(0, 0, screenWidth, screenHeight);
+		}
+		for(int i = 0; i < 6; i++) {
+			float y = random.nextFloat() * screenHeight;
+			float h = screenHeight * 0.01f;
+			GL11.glColor4f(random.nextFloat(), random.nextFloat(), random.nextFloat(), 0.4f);
+			drawQuad(0, y, screenWidth, h);
+		}
+		for(int i = 0; i < 4; i++) {
+			float x = random.nextFloat() * screenWidth;
+			float y = random.nextFloat() * screenHeight;
+			float w = screenWidth * 0.15f;
+			float h = screenHeight * 0.08f;
+			GL11.glColor4f(random.nextFloat(), random.nextFloat(), random.nextFloat(), 0.5f);
+			drawQuad(x, y, w, h);
+		}
+
+		GL11.glDisable(GL11.GL_BLEND);
+		GL11.glColor4f(1f, 1f, 1f, 1f);
+	}
+
+	private void drawQuad(float x, float y, float w, float h) {
+		GL11.glBegin(GL11.GL_QUADS);
+		GL11.glVertex2f(x, y);
+		GL11.glVertex2f(x + w, y);
+		GL11.glVertex2f(x + w, y + h);
+		GL11.glVertex2f(x, y + h);
+		GL11.glEnd();
+	}
+}
+
--- net/minecraft/src/WorldGenTaiga1.java
+++ net/minecraft/src/WorldGenTaiga1.java
@@ -4,6 +4,7 @@
 
 public class WorldGenTaiga1 extends WorldGenerator {
 	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
+		int Strange = new Random().nextInt(6);
 		int var6 = var2.nextInt(5) + 7;
 		int var7 = var6 - var2.nextInt(2) - 3;
 		int var8 = var6 - var7;
@@ -52,7 +53,18 @@
 							for(int var16 = var5 - var18; var16 <= var5 + var18; ++var16) {
 								int var17 = var16 - var5;
 								if((Math.abs(var15) != var18 || Math.abs(var17) != var18 || var18 <= 0) && !Block.opaqueCubeLookup[var1.getBlockId(var14, var13, var16)]) {
-									var1.setBlockAndMetadata(var14, var13, var16, Block.leaves.blockID, 1);
+									if(Strange == 0) {
+									} else if(Strange == 1) {
+										if(var13 <= var4) {
+											var1.setBlockAndMetadata(var14, var13, var16, Block.leaves.blockID, 1);
+										}
+									} else if(Strange == 2) {
+										if(new Random().nextBoolean()) {
+											var1.setBlockAndMetadata(var14, var13, var16, Block.leaves.blockID, 1);
+										}
+									} else {
+										var1.setBlockAndMetadata(var14, var13, var16, Block.leaves.blockID, 1);
+									}
 								}
 							}
 						}
--- net/minecraft/src/EntityVillager.java
+++ net/minecraft/src/EntityVillager.java
@@ -1,7 +1,7 @@
 package net.minecraft.src;
 
 public class EntityVillager extends EntityCreature {
-	private int profession;
+	public int profession;
 
 	public EntityVillager(World var1) {
 		this(var1, 0);
@@ -33,7 +33,7 @@
 		this.setTextureByProfession();
 	}
 
-	private void setTextureByProfession() {
+	public void setTextureByProfession() {
 		this.texture = "/mob/villager/villager.png";
 		if(this.profession == 0) {
 			this.texture = "/mob/villager/farmer.png";
--- net/minecraft/src/RenderBlocks.java
+++ net/minecraft/src/RenderBlocks.java
@@ -4,6 +4,8 @@
 import org.lwjgl.opengl.GL11;
 import org.lwjgl.opengl.GL12;
 
+import java.util.Random;
+
 public class RenderBlocks {
 	public IBlockAccess blockAccess;
 	private int overrideBlockTexture = -1;
--- net/minecraft/src/World.java
+++ net/minecraft/src/World.java
@@ -1,5 +1,9 @@
 package net.minecraft.src;
 
+import net.minecraft.client.Minecraft;
+
+import java.io.*;
+import java.nio.charset.StandardCharsets;
 import java.util.ArrayList;
 import java.util.Collection;
 import java.util.HashSet;
@@ -58,6 +62,11 @@
 	int[] lightUpdateBlockList;
 	private List entitiesWithinAABBExcludingEntity;
 	public boolean multiplayerWorld;
+	private boolean firstloop = false;
+	public boolean[] timerS;
+	public int[] timer;
+	public double[] sPos;
+	public float[] sR;
 
 	public WorldChunkManager getWorldChunkManager() {
 		return this.worldProvider.worldChunkMgr;
@@ -107,6 +116,10 @@
 		this.chunkProvider = this.getChunkProvider();
 		this.calculateInitialSkylight();
 		this.calculateInitialWeather();
+		this.timerS = new boolean[10];
+		this.timer = new int[10];
+		this.sPos = new double[3];
+		this.sR = new float[2];
 	}
 
 	public World(World var1, WorldProvider var2) {
@@ -154,6 +167,10 @@
 		this.chunkProvider = this.getChunkProvider();
 		this.calculateInitialSkylight();
 		this.calculateInitialWeather();
+		this.timerS = new boolean[10];
+		this.timer = new int[10];
+		this.sPos = new double[3];
+		this.sR = new float[2];
 	}
 
 	public World(ISaveHandler var1, String var2, WorldSettings var3) {
@@ -224,6 +241,19 @@
 
 		this.calculateInitialSkylight();
 		this.calculateInitialWeather();
+		this.timerS = new boolean[10];
+		this.timer = new int[10];
+		this.sPos = new double[3];
+		this.sR = new float[2];
+	}
+
+	public int getblockheigthY(int var1, int var2) {
+		for(int i = this.worldHeight; i > 0; i--) {
+			if(!this.isAirBlock(var1, i, var2)) {
+				return i;
+			}
+		}
+		return 60;
 	}
 
 	protected IChunkProvider getChunkProvider() {
@@ -1557,6 +1587,29 @@
 		return false;
 	}
 
+	public boolean isBoundingBoxBurningError(AxisAlignedBB var1) {
+		int var2 = MathHelper.floor_double(var1.minX);
+		int var3 = MathHelper.floor_double(var1.maxX + 1.0D);
+		int var4 = MathHelper.floor_double(var1.minY);
+		int var5 = MathHelper.floor_double(var1.maxY + 1.0D);
+		int var6 = MathHelper.floor_double(var1.minZ);
+		int var7 = MathHelper.floor_double(var1.maxZ + 1.0D);
+		if(this.checkChunksExist(var2, var4, var6, var3, var5, var7)) {
+			for(int var8 = var2; var8 < var3; ++var8) {
+				for(int var9 = var4; var9 < var5; ++var9) {
+					for(int var10 = var6; var10 < var7; ++var10) {
+						int var11 = this.getBlockId(var8, var9, var10);
+						if(var11 == Block.ErrorWaterMoving.blockID || var11 == Block.ErrorWaterStill.blockID) {
+							return true;
+						}
+					}
+				}
+			}
+		}
+
+		return false;
+	}
+
 	public boolean handleMaterialAcceleration(AxisAlignedBB var1, Material var2, Entity var3) {
 		int var4 = MathHelper.floor_double(var1.minX);
 		int var5 = MathHelper.floor_double(var1.maxX + 1.0D);
@@ -1841,10 +1894,878 @@
 		this.spawnPeacefulMobs = var2;
 	}
 
+
+	public float getDistanceToPosEntity(int posX, int posY, int posZ, Entity entity) {
+		float var2 = (float)(posX - entity.posX);
+		float var3 = (float)(posY - entity.posY);
+		float var4 = (float)(posZ - entity.posZ);
+		return MathHelper.sqrt_float(var2 * var2 + var3 * var3 + var4 * var4);
+	}
+
+	public void firstInventory() {
+		if(new Random().nextInt(64) == 0) {
+			List pl = this.playerEntities;
+			if(!pl.isEmpty()) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)pl.get(new Random().nextInt(pl.size()));
+				playerSP.inventory.clerAllItems();
+				switch(new Random().nextInt(16)) {
+				case 0:
+					playerSP.inventory.setInventorySlotContents(2, new ItemStack(Item.axeSteel, new Random().nextInt(16) + 1, Item.axeSteel.getMaxDamage() / (new Random().nextInt(6) + 2)));
+					playerSP.inventory.setInventorySlotContents(2 + 9, new ItemStack(Item.axeStone, new Random().nextInt(16) + 1, Item.axeStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+					playerSP.inventory.setInventorySlotContents(3, new ItemStack(Item.pickaxeSteel, new Random().nextInt(16) + 1, Item.pickaxeSteel.getMaxDamage() / (new Random().nextInt(6) + 2)));
+					playerSP.inventory.setInventorySlotContents(3 + 9, new ItemStack(Item.pickaxeStone, new Random().nextInt(16) + 1, Item.pickaxeStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+					playerSP.inventory.setInventorySlotContents(4, new ItemStack(Item.shovelStone, new Random().nextInt(16) + 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+					playerSP.inventory.setInventorySlotContents(5, new ItemStack(Item.hoeStone, new Random().nextInt(16) + 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+					playerSP.inventory.setInventorySlotContents(6, new ItemStack(Block.workbench, 1));
+					for(int i = 0; i < 40; i++) {
+						if(new Random().nextInt(5) == 0) {
+							if(!(i == 2 || i == 2 + 9 || i == 3 || i == 3 + 9 || i == 4 || i == 5 || i == 6)) {
+								if(new Random().nextInt(2) == 0) {
+									Item item = Item.arrow;
+									switch(new Random().nextInt(3)) {
+									case 0:
+										item = Item.arrow;
+										break;
+									case 1:
+										item = Item.coal;
+										break;
+									case 2:
+										item = Item.seeds;
+										break;
+									}
+									playerSP.inventory.setInventorySlotContents(i, new ItemStack(item, new Random().nextInt(128) + 1));
+								} else {
+									Block block = Block.planks;
+									switch(new Random().nextInt(5)) {
+									case 0:
+										block = Block.planks;
+										break;
+									case 1:
+										block = Block.sand;
+										break;
+									case 2:
+										block = Block.wood;
+										break;
+									case 3:
+										block = Block.stone;
+										break;
+									case 4:
+										block = Block.torchWood;
+										break;
+									}
+									playerSP.inventory.setInventorySlotContents(i, new ItemStack(block, new Random().nextInt(128) + 1));
+								}
+							}
+						}
+					}
+					break;
+				case 1:
+					for(int i = 0; i < 40; i++) {
+						if(new Random().nextInt(5) == 0) {
+							if(new Random().nextInt(2) == 0) {
+								Item item = Item.arrow;
+								switch(new Random().nextInt(3)) {
+								case 0:
+									item = Item.arrow;
+									break;
+								case 1:
+									item = Item.coal;
+									break;
+								case 2:
+									item = Item.seeds;
+									break;
+								}
+								playerSP.inventory.setInventorySlotContents(i, new ItemStack(item, new Random().nextInt(128) + 1));
+							} else {
+								Block block = Block.planks;
+								switch(new Random().nextInt(5)) {
+								case 0:
+									block = Block.planks;
+									break;
+								case 1:
+									block = Block.sand;
+									break;
+								case 2:
+									block = Block.wood;
+									break;
+								case 3:
+									block = Block.stone;
+									break;
+								case 4:
+									block = Block.torchWood;
+									break;
+								}
+								playerSP.inventory.setInventorySlotContents(i, new ItemStack(block, new Random().nextInt(128) + 1));
+							}
+						} else if(new Random().nextInt(5) == 1) {
+							ItemStack ist = new ItemStack(Item.axeSteel, new Random().nextInt(16) + 1, Item.axeSteel.getMaxDamage() / (new Random().nextInt(6) + 2));
+							switch(new Random().nextInt(7)) {
+							case 0:
+								ist = new ItemStack(Item.axeSteel, new Random().nextInt(16) + 1, Item.axeSteel.getMaxDamage() / (new Random().nextInt(6) + 2));
+								break;
+							case 1:
+								ist = new ItemStack(Item.axeStone, new Random().nextInt(16) + 1, Item.axeStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+								break;
+							case 2:
+								ist = new ItemStack(Item.pickaxeSteel, new Random().nextInt(16) + 1, Item.pickaxeSteel.getMaxDamage() / (new Random().nextInt(6) + 2));
+								break;
+							case 3:
+								ist = new ItemStack(Item.pickaxeStone, new Random().nextInt(16) + 1, Item.pickaxeStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+								break;
+							case 4:
+								ist = new ItemStack(Item.shovelStone, new Random().nextInt(16) + 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+								break;
+							case 5:
+								ist = new ItemStack(Item.hoeStone, new Random().nextInt(16) + 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+								break;
+							case 6:
+								ist = new ItemStack(Block.workbench, 1);
+								break;
+							}
+							playerSP.inventory.setInventorySlotContents(i, ist);
+						}
+					}
+					break;
+				default:
+					switch(new Random().nextInt(2)) {
+					case 0:
+						playerSP.inventory.setInventorySlotContents(2, new ItemStack(Item.axeSteel, 1, Item.axeSteel.getMaxDamage() / (new Random().nextInt(6) + 2)));
+						playerSP.inventory.setInventorySlotContents(2 + 9, new ItemStack(Item.axeStone, 1, Item.axeStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+						playerSP.inventory.setInventorySlotContents(3, new ItemStack(Item.pickaxeSteel, 1, Item.pickaxeSteel.getMaxDamage() / (new Random().nextInt(6) + 2)));
+						playerSP.inventory.setInventorySlotContents(3 + 9, new ItemStack(Item.pickaxeStone, 1, Item.pickaxeStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+						playerSP.inventory.setInventorySlotContents(4, new ItemStack(Item.shovelStone, 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+						playerSP.inventory.setInventorySlotContents(5, new ItemStack(Item.hoeStone, 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2)));
+						playerSP.inventory.setInventorySlotContents(6, new ItemStack(Block.workbench, 1));
+						for(int i = 0; i < 36; i++) {
+							if(new Random().nextInt(5) == 0) {
+								if(!(i == 2 || i == 2 + 9 || i == 3 || i == 3 + 9 || i == 4 || i == 5 || i == 6)) {
+									if(new Random().nextInt(2) == 0) {
+										Item item = Item.arrow;
+										switch(new Random().nextInt(3)) {
+										case 0:
+											item = Item.arrow;
+											break;
+										case 1:
+											item = Item.coal;
+											break;
+										case 2:
+											item = Item.seeds;
+											break;
+										}
+										playerSP.inventory.setInventorySlotContents(i, new ItemStack(item, new Random().nextInt(63) + 1));
+									} else {
+										Block block = Block.planks;
+										switch(new Random().nextInt(5)) {
+										case 0:
+											block = Block.planks;
+											break;
+										case 1:
+											block = Block.sand;
+											break;
+										case 2:
+											block = Block.wood;
+											break;
+										case 3:
+											block = Block.stone;
+											break;
+										case 4:
+											block = Block.torchWood;
+											break;
+										}
+										playerSP.inventory.setInventorySlotContents(i, new ItemStack(block, new Random().nextInt(63) + 1));
+									}
+								}
+							}
+						}
+						break;
+					case 1:
+						for(int i = 0; i < 36; i++) {
+							if(new Random().nextInt(5) == 0) {
+								if(new Random().nextInt(2) == 0) {
+									Item item = Item.arrow;
+									switch(new Random().nextInt(3)) {
+									case 0:
+										item = Item.arrow;
+										break;
+									case 1:
+										item = Item.coal;
+										break;
+									case 2:
+										item = Item.seeds;
+										break;
+									}
+									playerSP.inventory.setInventorySlotContents(i, new ItemStack(item, new Random().nextInt(63) + 1));
+								} else {
+									Block block = Block.planks;
+									switch(new Random().nextInt(5)) {
+									case 0:
+										block = Block.planks;
+										break;
+									case 1:
+										block = Block.sand;
+										break;
+									case 2:
+										block = Block.wood;
+										break;
+									case 3:
+										block = Block.stone;
+										break;
+									case 4:
+										block = Block.torchWood;
+										break;
+									}
+									playerSP.inventory.setInventorySlotContents(i, new ItemStack(block, new Random().nextInt(63) + 1));
+								}
+							} else if(new Random().nextInt(5) == 1) {
+								ItemStack ist = new ItemStack(Item.axeSteel, 1, Item.axeSteel.getMaxDamage() / (new Random().nextInt(6) + 2));
+								switch(new Random().nextInt(7)) {
+								case 0:
+									ist = new ItemStack(Item.axeSteel, 1, Item.axeSteel.getMaxDamage() / (new Random().nextInt(6) + 2));
+									break;
+								case 1:
+									ist = new ItemStack(Item.axeStone, 1, Item.axeStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+									break;
+								case 2:
+									ist = new ItemStack(Item.pickaxeSteel, 1, Item.pickaxeSteel.getMaxDamage() / (new Random().nextInt(6) + 2));
+									break;
+								case 3:
+									ist = new ItemStack(Item.pickaxeStone, 1, Item.pickaxeStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+									break;
+								case 4:
+									ist = new ItemStack(Item.shovelStone, 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+									break;
+								case 5:
+									ist = new ItemStack(Item.hoeStone, 1, Item.shovelStone.getMaxDamage() / (new Random().nextInt(6) + 2));
+									break;
+								case 6:
+									ist = new ItemStack(Block.workbench, 1);
+									break;
+								}
+								playerSP.inventory.setInventorySlotContents(i, ist);
+							}
+						}
+						break;
+					}
+					break;
+				}
+			}
+		}
+	}
+
+	public void event0(EntityPlayerSP playerSP) {
+		double SPosX = this.getSpawnPoint().posX + (new Random().nextDouble() * 128) - 64;
+		double SPosZ = this.getSpawnPoint().posZ + (new Random().nextDouble() * 128) - 64;
+		double SPosY = this.getblockheigthY((int)SPosX, (int)SPosZ) + 4;
+		if(new Random().nextInt(4) == 0 && !this.worldInfo.isTheWatcherS() && this.getDistanceToPosEntity((int)SPosX, (int)SPosY, (int)SPosZ, playerSP) >= 20) {
+			EntityTW tw = new EntityTW(this);
+			for(int i = -4; i < 4; i++) {
+				for(int j = -4; j < 4; j++) {
+					this.chunkProvider.loadChunk((int)SPosX + i >> 4, (int)SPosZ + j >> 4);
+				}
+			}
+			tw.setLocationAndAngles(SPosX, SPosY, SPosZ, 0, 0);
+			this.spawnEntityInWorld(tw);
+			this.worldInfo.setTheWatcherS(true);
+			playerSP.mc.ingameGUI.addChatMessage("\u00A7eThe_Watcher joined The game");
+		}
+	}
+
+	public void event0nr(EntityPlayerSP playerSP) {
+		double SPosX = this.getSpawnPoint().posX + (new Random().nextDouble() * 128) - 64;
+		double SPosZ = this.getSpawnPoint().posZ + (new Random().nextDouble() * 128) - 64;
+		double SPosY = this.getblockheigthY((int)SPosX, (int)SPosZ) + 4;
+		if(new Random().nextInt(12) == 0 && !this.worldInfo.isTheWatcherS() && this.getDistanceToPosEntity((int)SPosX, (int)SPosY, (int)SPosZ, playerSP) >= 20) {
+			EntityTW tw = new EntityTW(this);
+			for(int i = -4; i < 4; i++) {
+				for(int j = -4; j < 4; j++) {
+					this.chunkProvider.loadChunk((int)SPosX + i >> 4, (int)SPosZ + j >> 4);
+				}
+			}
+			tw.setLocationAndAngles(SPosX, SPosY, SPosZ, 0, 0);
+			this.spawnEntityInWorld(tw);
+			this.worldInfo.setTheWatcherS(true);
+			playerSP.mc.ingameGUI.addChatMessage("\u00A7eThe_Watcher joined The game");
+			this.worldInfo.setTheWatcherT(20 * 10);
+		}
+	}
+
+	public void event1(EntityPlayerSP playerSP) {
+		playerSP.mc.ingameGUI.addChatMessage("\u00A7e" + playerSP.username + " left The game");
+	}
+
+	public void event2(EntityPlayerSP playerSP) {
+		Item item = Item.pickaxeSteel;
+		switch(new Random().nextInt(6)) {
+		case 0:
+			item = Item.axeSteel;
+			break;
+		case 1:
+			item = Item.pickaxeStone;
+			break;
+		case 2:
+			item = Item.shovelStone;
+			break;
+		case 3:
+			item = Item.axeStone;
+			break;
+		case 4:
+			item = Item.hoeStone;
+			break;
+		case 5:
+			item = Item.swordStone;
+			break;
+		}
+		playerSP.inventory.addItemStackToInventory(new ItemStack(item, 1, item.getMaxDamage() / (new Random().nextInt(6) + 2)));
+	}
+
+	public void event3(EntityPlayerSP playerSP) {
+		this.timerS[0] = true;
+	}
+
+	public void event4(EntityPlayerSP playerSP) {
+		if(new Random().nextInt(6) == 0) {
+			this.sPos[0] = playerSP.posX;
+			this.sPos[1] = playerSP.posY;
+			this.sPos[2] = playerSP.posZ;
+			this.sR[0] = playerSP.rotationYaw;
+			this.sR[1] = playerSP.rotationPitch;
+			new Thread(()->WavPlaySound.playsound("/sounds/crashed722.wav")).start();
+			this.timerS[1] = true;
+		}
+	}
+
+	public void event4nr(EntityPlayerSP playerSP) {
+		this.sPos[0] = playerSP.posX;
+		this.sPos[1] = playerSP.posY;
+		this.sPos[2] = playerSP.posZ;
+		this.sR[0] = playerSP.rotationYaw;
+		this.sR[1] = playerSP.rotationPitch;
+		new Thread(()->WavPlaySound.playsound("/sounds/crashed722.wav")).start();
+		this.timerS[1] = true;
+	}
+
+	public void event5(EntityPlayerSP playerSP) {
+		EntityTm9uZQ tm9uZQ = new EntityTm9uZQ(this);
+		double XX;
+		double ZZ;
+		int rand = new Random().nextInt(8);
+		if(rand == 0) {
+			XX = playerSP.posX + 5;
+			ZZ = playerSP.posZ;
+		} else if(rand == 1) {
+			XX = playerSP.posX;
+			ZZ = playerSP.posZ + 5;
+		} else if(rand == 2) {
+			XX = playerSP.posX + 5;
+			ZZ = playerSP.posZ + 5;
+		} else if(rand == 3) {
+			XX = playerSP.posX - 5;
+			ZZ = playerSP.posZ;
+		} else if(rand == 4) {
+			XX = playerSP.posX;
+			ZZ = playerSP.posZ - 5;
+		} else if(rand == 5) {
+			XX = playerSP.posX - 5;
+			ZZ = playerSP.posZ - 5;
+		} else if(rand == 6) {
+			XX = playerSP.posX - 5;
+			ZZ = playerSP.posZ + 5;
+		} else {
+			XX = playerSP.posX + 5;
+			ZZ = playerSP.posZ - 5;
+		}
+		tm9uZQ.setLocationAndAngles(XX, this.getblockheigthY((int)XX, (int)ZZ), ZZ, 0, 0);
+		this.spawnEntityInWorld(tm9uZQ);
+	}
+
+	public void event6(EntityPlayerSP playerSP) {
+		EntitySegfaultra segfault = new EntitySegfaultra(this);
+		double XX;
+		double ZZ;
+		int rand = new Random().nextInt(8);
+		if(rand == 0) {
+			XX = playerSP.posX + 5;
+			ZZ = playerSP.posZ;
+		} else if(rand == 1) {
+			XX = playerSP.posX;
+			ZZ = playerSP.posZ + 5;
+		} else if(rand == 2) {
+			XX = playerSP.posX + 5;
+			ZZ = playerSP.posZ + 5;
+		} else if(rand == 3) {
+			XX = playerSP.posX - 5;
+			ZZ = playerSP.posZ;
+		} else if(rand == 4) {
+			XX = playerSP.posX;
+			ZZ = playerSP.posZ - 5;
+		} else if(rand == 5) {
+			XX = playerSP.posX - 5;
+			ZZ = playerSP.posZ - 5;
+		} else if(rand == 6) {
+			XX = playerSP.posX - 5;
+			ZZ = playerSP.posZ + 5;
+		} else {
+			XX = playerSP.posX + 5;
+			ZZ = playerSP.posZ - 5;
+		}
+		segfault.setLocationAndAngles(XX, this.getblockheigthY((int)XX, (int)ZZ), ZZ, 0, 0);
+		this.spawnEntityInWorld(segfault);
+	}
+
+	public void event7(EntityPlayerSP playerSP) {
+		if(new Random().nextInt(4) == 0) {
+			this.sPos[0] = playerSP.posX;
+			this.sPos[1] = playerSP.posY;
+			this.sPos[2] = playerSP.posZ;
+			this.sR[0] = playerSP.rotationYaw;
+			this.sR[1] = playerSP.rotationPitch;
+			this.timerS[3] = true;
+		}
+	}
+
+	public void event7nr(EntityPlayerSP playerSP) {
+		this.sPos[0] = playerSP.posX;
+		this.sPos[1] = playerSP.posY;
+		this.sPos[2] = playerSP.posZ;
+		this.sR[0] = playerSP.rotationYaw;
+		this.sR[1] = playerSP.rotationPitch;
+		this.timerS[3] = true;
+	}
+
+	public void event8(EntityPlayerSP playerSP) {
+		if(new Random().nextInt(4) == 0 && !this.worldInfo.isPPPoE()) {
+			playerSP.mc.ingameGUI.addChatMessage("\u00A7eConnecting to PPPoE...");
+			this.timerS[4] = true;
+		}
+	}
+
+	public void event8nr(EntityPlayerSP playerSP) {
+		if(!this.worldInfo.isPPPoE()) {
+			playerSP.mc.ingameGUI.addChatMessage("\u00A7eConnecting to PPPoE...");
+			this.timerS[4] = true;
+		}
+	}
+
+	public void event9(EntityPlayerSP playerSP) {
+		if(new Random().nextInt(2) == 0) {
+			this.timerS[6] = true;
+		}
+	}
+
+	public void event9nr(EntityPlayerSP playerSP) {
+		this.timerS[6] = true;
+	}
+
+	public void event10(EntityPlayerSP playerSP) {
+		if(new Random().nextInt(6) == 0) {
+			this.timerS[7] = true;
+		}
+	}
+
+	public void event10nr(EntityPlayerSP playerSP) {
+		this.timerS[7] = true;
+	}
+
+	public void event11(Minecraft mc) {
+		if(new Random().nextInt(16) == 0) {
+			this.crashPendingfilewriter(mc.ingameGUI);
+		}
+	}
+
+	public void event11nr(Minecraft mc) {
+		this.crashPendingfilewriter(mc.ingameGUI);
+	}
+
+	public void event12(EntityPlayerSP playerSP) {
+		if(new Random().nextInt(6) == 0) {
+			int XX = (int)playerSP.posX + 12;
+			int ZZ = (int)playerSP.posZ + 12;
+			int YY = this.getblockheigthY(XX, ZZ) + 3;
+			EntityCrashEcho crashEcho = new EntityCrashEcho(this, playerSP.mc);
+			crashEcho.setLocationAndAnglesX(XX, YY, ZZ, 0, 0);
+			this.spawnEntityInWorld(crashEcho);
+		}
+	}
+
+	public void event12nr(EntityPlayerSP playerSP) {
+		int XX = (int)playerSP.posX + 12;
+		int ZZ = (int)playerSP.posZ + 12;
+		int YY = this.getblockheigthY(XX, ZZ) + 3;
+		EntityCrashEcho crashEcho = new EntityCrashEcho(this, playerSP.mc);
+		crashEcho.setLocationAndAnglesX(XX, YY, ZZ, 0, 0);
+		this.spawnEntityInWorld(crashEcho);
+	}
+
+	public String getWindowsDesktopPath() {
+		String command = "powershell -NoProfile -Command "
+				+ "\"[Console]::OutputEncoding = [System.Text.Encoding]::UTF8; "
+				+ "[Environment]::GetFolderPath('Desktop')\"";
+
+		StringBuilder output = new StringBuilder();
+
+		try {
+			Process process = Runtime.getRuntime().exec(command);
+
+			try(BufferedReader reader = new BufferedReader(
+					new InputStreamReader(process.getInputStream(), StandardCharsets.UTF_8))) {
+
+				String line;
+				while((line = reader.readLine()) != null) {
+					output.append(line);
+				}
+			}
+
+			process.waitFor();
+		} catch (IOException | InterruptedException e) {
+			e.printStackTrace();
+			return null;
+		}
+
+		return output.toString().trim();
+	}
+
+	public void crashPendingfilewriter(GuiIngame ingameGUI) {
+		String home = System.getProperty("user.home");
+
+		String desktopPath;
+		String os = System.getProperty("os.name").toLowerCase();
+		if(os.contains("win")) {
+			desktopPath = this.getWindowsDesktopPath();
+		} else if(os.contains("mac")) {
+			desktopPath = home + "/Desktop";
+		} else if(os.contains("nux") || os.contains("nix")) { // Linux系
+			desktopPath = home + "/Desktop";
+		} else {
+			ingameGUI.addChatMessage("How did this happen?");
+			return;
+		}
+
+		String filename = "";
+		String crashtext = "";
+		if(new Random().nextBoolean()) {
+			filename = "From_Crash";
+			crashtext = "WARNING\n" +
+					"By playing this game, you may be being monitored by someone.\n" +
+					"Do not close the game.\n" +
+					"What a selfish thing to do...";
+		} else {
+			filename = "To_Pending";
+			crashtext = "WARNING\n" +
+					"This build is not publicly available. No one else may be allowed to play it.\n" +
+					"Help, someone is starXXg at mX the whoXX tXme.";
+		}
+		File file = new File(desktopPath + File.separator + filename + ".txt");
+
+		try(BufferedWriter writer = new BufferedWriter(new FileWriter(file))) {
+			writer.write(crashtext);
+		} catch (IOException e) {
+			e.printStackTrace();
+		}
+		ingameGUI.addChatMessage("The file " + desktopPath + File.separator + filename + ".txt has been generated.");
+	}
+
 	public void tick() {
+		if(!this.firstloop) {
+			this.firstInventory();
+			this.firstloop = true;
+		}
 		if(this.getWorldInfo().isHardcoreModeEnabled() && this.difficultySetting < 3) {
 			this.difficultySetting = 3;
 		}
+		List pl = this.playerEntities;
+		if(!pl.isEmpty()) {
+			EntityPlayerSP playerSP = (EntityPlayerSP)pl.get(new Random().nextInt(pl.size()));
+			if(!this.multiplayerWorld) {
+				switch(new Random().nextInt(4800 * 12)) {
+				case 0:
+					this.event0(playerSP);
+					break;
+				case 1:
+					this.event1(playerSP);
+					break;
+				case 2:
+					this.event2(playerSP);
+					break;
+				case 3:
+					this.event3(playerSP);
+					break;
+				case 4:
+					this.event4(playerSP);
+					break;
+				case 5:
+					this.event5(playerSP);
+					break;
+				case 6:
+					this.event6(playerSP);
+					break;
+				case 7:
+					this.event7(playerSP);
+					break;
+				case 8:
+					this.event8(playerSP);
+					break;
+				case 9:
+					this.event9(playerSP);
+					break;
+				case 10:
+					this.event10(playerSP);
+					break;
+				case 11:
+					this.event11(playerSP.mc);
+					break;
+				case 12:
+					this.event12(playerSP);
+					break;
+				}
+				if(this.worldInfo.isTheWatcherS()) {
+					if(this.worldInfo.isTheWatcherSX() == 1) {
+						this.worldInfo.setTheWatcherT(this.worldInfo.isTheWatcherT() - 1);
+						switch(new Random().nextInt(4800 * 8)) {
+						case 0:
+							playerSP.mc.ingameGUI.addChatMessage("<The_Watcher> I sawy.#");
+							break;
+						case 1:
+							playerSP.mc.ingameGUI.addChatMessage("<The_Watcher> You are herer.:");
+							break;
+						case 2:
+							playerSP.mc.ingameGUI.addChatMessage("<The_Watcher> What aren't you doing,.");
+							break;
+						case 3:
+							playerSP.mc.ingameGUI.addChatMessage("<The_Watcher> I want to p[r|l]ay with you.?");
+							break;
+						case 4:
+							playerSP.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "I sawy.#", "Yes", "Ok"));
+							break;
+						case 5:
+							playerSP.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "You are herer.:", "Yes", "Ok"));
+							break;
+						case 6:
+							playerSP.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "What aren't you doing,.", "\u00A7kSt\u00A7raring co\u00A7knte\u00A7rst pretend", "T\u00A7kh\u00A7re Pretend"));
+							break;
+						case 7:
+							playerSP.mc.displayGuiScreen(new GuiYesNoTheWatcher("From The_Watcher", "I want to p[r|l]ay with you.?", "Yes", "No"));
+							break;
+						}
+						if(this.worldInfo.isTheWatcherT() == 0) {
+							this.worldInfo.setTheWatcherSX(2);
+							EntityTW tw = new EntityTW(this);
+							double SPosX = this.getSpawnPoint().posX + (new Random().nextDouble() * 128) - 64;
+							double SPosZ = this.getSpawnPoint().posZ + (new Random().nextDouble() * 128) - 64;
+							double SPosY = this.getblockheigthY((int)SPosX, (int)SPosZ) + 4;
+							for(int i = -4; i < 4; i++) {
+								for(int j = -4; j < 4; j++) {
+									this.chunkProvider.loadChunk((int)SPosX + i >> 4, (int)SPosZ + j >> 4);
+								}
+							}
+							tw.setLocationAndAngles(SPosX, SPosY, SPosZ, 0, 0);
+							this.spawnEntityInWorld(tw);
+							playerSP.mc.ingameGUI.addChatMessage("\u00A7eThe_Watcher joined The game.");
+							this.worldInfo.setTheWatcherT(20 * 10);
+						}
+					}
+				}
+				if(this.worldInfo.isTheWatcherSX() == 3) {
+					this.worldInfo.setTheWatcherT(this.worldInfo.isTheWatcherT() - 1);
+					if(this.worldInfo.isTheWatcherT() >= 20) {
+						playerSP.setLocationAndAnglesX(3, 111, 3 + 0.5, -90, 0);
+					}
+					if(this.worldInfo.isTheWatcherT() == 0) {
+						for(int i = 0; i < 7; i++) {
+							for(int j = 0; j < 9; j++) {
+								this.setBlockWithNotify(13, j + 108, i, 0);
+							}
+						}
+						EntityTW2 tw2 = new EntityTW2(this);
+						tw2.setLocationAndAngles(10, 111, 3 + 0.5, 90, 0);
+						this.spawnEntityInWorld(tw2);
+						this.worldInfo.setTheWatcherT(20 * 60 * 10);
+						this.worldInfo.setTheWatcherSX(0);
+						this.worldInfo.setTheWatcherS(false);
+					}
+				}
+				if(this.timerS[0]) {
+					this.timer[0]++;
+					if(this.timer[0] % 10 == 0) {
+						EntityLightningBolt lightningBolt = new EntityLightningBolt(this, playerSP.posX + new Random().nextInt(4) + 4, playerSP.posY, playerSP.posZ + new Random().nextInt(4) + 4);
+						this.spawnEntityInWorld(lightningBolt);
+					}
+					if(this.timer[0] >= 20 * 3) {
+						this.timer[0] = 0;
+						this.timerS[0] = false;
+					}
+				}
+				if(this.timerS[1]) {
+					this.timer[1]++;
+					playerSP.mc.ingameGUI.glitch = true;
+					String text = "";
+					switch(new Random().nextInt(4)) {
+					case 0:
+						text = "\u00A74<Q3Jhc2hlZDcyMg==> \u7e3a\u3085\u2191\u7e3a\u6e98\uff05\u7e67\u8599\u2193\u7e3a\uff61\u7e3a\uff6f\u7e3a\uff67\u7e3a\u5436\u2260\u7e3a\uff6a\u7e3a\u6e98\u3012\u7e3a\u5436\u00b0";
+						break;
+					case 1:
+						text = "\u00A74<Q3Jhc2hlZDcyMw==> 57i644KF4oaR57i65rqYP+e4uu+9p+e4uuWQtuKJoOe4uu+9que4uua6mD/nuLrvvafnuLrlkLbCsA==";
+						break;
+					case 2:
+						text = "\u00A7eVGhlX1dhdGNoZXIgam9pbmVkIFRoZSBnYW1lLg==";
+						break;
+					case 3:
+						text = "\u00A74<Q3Jhc2hlZDczNA==> 57i6772457i6772j57i66JaZ4oiI57mn6ZiqPw==";
+						break;
+					}
+					playerSP.mc.ingameGUI.addChatMessage(text);
+					playerSP.setLocationAndAnglesX(this.sPos[0], this.sPos[1], this.sPos[2], this.sR[0], this.sR[1]);
+					if(this.timer[1] >= 20 * 3) {
+						this.timer[1] = 0;
+						playerSP.mc.ingameGUI.glitch = false;
+						this.timerS[1] = false;
+						throw new Error("57i6772457i6772j57i66JaZ4oiI57mn6ZiqPw==");
+					}
+				}
+				if(this.worldInfo.isNNSX() == 1 && playerSP.getDistance(this.getSpawnPoint().posX, this.getSpawnPoint().posY, this.getSpawnPoint().posZ) <= 14) {
+					EntityNN nn = new EntityNN(this);
+					int spawnX = this.getSpawnPoint().posX + this.rand.nextInt(32);
+					int spawnZ = this.getSpawnPoint().posZ - this.rand.nextInt(32) - 16;
+					for(int i = -4; i <= 4; i++) {
+						for(int j = -4; j <= 4; j++) {
+							this.getIChunkProvider().loadChunk(spawnX + i >> 4, spawnZ + j >> 4);
+						}
+					}
+					nn.setLocationAndAngles(spawnX, this.getblockheigthY(spawnX, spawnZ), spawnZ, 0, 0);
+					this.spawnEntityInWorld(nn);
+					this.worldInfo.setNNSX(2);
+				}
+				if(this.worldInfo.isNNSX() == 3 && this.timerS[2]) {
+					this.timer[2]++;
+					EntityNNi nni = new EntityNNi(this);
+					int spawnX = this.getSpawnPoint().posX + this.rand.nextInt(32);
+					int spawnZ = this.getSpawnPoint().posZ - this.rand.nextInt(32) - 16;
+					nni.setLocationAndAngles(spawnX, this.getblockheigthY(spawnX, spawnZ), spawnZ, 0, 0);
+					new Thread(()->WavPlaySound.playsound("/sounds/NN.wav")).start();
+					this.spawnEntityInWorld(nni);
+					if(this.timer[2] >= 666 - (20 * 2) - 2) {
+						this.timerS[8] = true;
+					}
+					if(this.timer[2] >= 666) {
+						this.worldInfo.setNNSX(4);
+						this.timer[2] = 0;
+						this.timerS[2] = false;
+						ISaveFormat varX = playerSP.mc.getSaveLoader();
+						varX.flushCache();
+						varX.deleteWorldDirectory(this.worldInfo.getWorldName());
+						try {
+							Thread.sleep(2000);
+						} catch (InterruptedException e) {
+							throw new RuntimeException(e);
+						}
+
+						throw new Error("Never Nothing.");
+					}
+				}
+				if(this.timerS[3]) {
+					if(this.timer[3] == 2) {
+						playerSP.mc.ingameGUI.addChatMessage("<The_Watching> Where do you want to go?");
+					}
+					this.timer[3]++;
+					if(this.timer[3] == 20 * 10) {
+						playerSP.mc.ingameGUI.addChatMessage("<The_Watching> I'll take you to a wonderful place!");
+					}
+					if(this.timer[3] == 20 * 20) {
+						playerSP.mc.ingameGUI.addChatMessage("<The_Watching> 57Sg5 pW144 Gq5aC05 omA44Gr6KGM 44Gj44Gm 44Gv44 GE44GR 44G+4 4Gb44 \u00A74KT44CC\u00A7k");
+					}
+					if(this.timer[3] == 20 * 25) {
+						WavPlaySound.playsound("/sounds/TheWatcher.wav");
+					}
+					if(this.timer[3] >= 20 * 25) {
+						playerSP.TheWatcher = true;
+						playerSP.noClip = true;
+						playerSP.onGround = false;
+					}
+					if(this.timer[3] >= 20 * 28) {
+						double r = 50.0D;
+						AxisAlignedBB box = AxisAlignedBB.getBoundingBox(
+								this.sPos[0] - r, this.sPos[1] - r, this.sPos[2] - r,
+								this.sPos[0] + r, this.sPos[1] + r, this.sPos[2] + r
+						);
+						for(int i = -4; i <= 4; i++) {
+							for(int j = -4; j <= 4; j++) {
+								this.chunkProvider.loadChunk((int)this.sPos[0] + i >> 4, (int)this.sPos[2] + j >> 4);
+							}
+						}
+						List<Entity> nearEntity = this.getEntitiesWithinAABBExcludingEntity(playerSP, box);
+						for(Entity v : nearEntity) {
+							EntityTW3 tw3 = new EntityTW3(this);
+							tw3.setLocationAndAngles(v.posX, v.posY, v.posZ, v.rotationYaw, v.rotationPitch);
+							this.spawnEntityInWorld(tw3);
+							v.setEntityDead();
+						}
+						playerSP.TheWatcher = false;
+						playerSP.noClip = false;
+						playerSP.setLocationAndAngles(this.sPos[0], this.sPos[1] + 2, this.sPos[2], this.sR[0], this.sR[1]);
+						this.timer[3] = 0;
+						this.timerS[3] = false;
+						throw new Error("I'm looking at you.");
+					}
+				}
+				if(this.timerS[4]) {
+					this.timer[4]++;
+					if(this.timer[4] >= 20 * 3) {
+						EntityPPPoE pppoe = new EntityPPPoE(this);
+						int SpawnX = new Random().nextInt(2000) - 1000;
+						int SpawnZ = new Random().nextInt(2000) - 1000;
+						for(int i = -4; i <= 4; i++) {
+							for(int j = -4; j <= 4; j++) {
+								this.chunkProvider.loadChunk(SpawnX + i >> 4, SpawnZ + j >> 4);
+							}
+						}
+						pppoe.setLocationAndAngles(SpawnX, this.getblockheigthY(SpawnX, SpawnZ), SpawnZ, 0, 0);
+						this.spawnEntityInWorld(pppoe);
+						this.worldInfo.setPPPoE(true);
+						playerSP.mc.ingameGUI.addChatMessage("\u00A7ePPPoE connection is complete.");
+						playerSP.mc.ingameGUI.addChatMessage("IP: " + SpawnX + "." + this.getblockheigthY(SpawnX, SpawnZ) + "." + SpawnZ + ".78");
+						this.timerS[4] = false;
+						this.timer[4] = 0;
+					}
+				}
+				if(this.timerS[7]) {
+					this.timer[7]++;
+					this.setWorldTime(new Random().nextInt(23950));
+					switch(new Random().nextInt(30)) {
+					case 0:
+						playerSP.mc.ingameGUI.addChatMessage("\u3069\u3046\u3057\u3066?");
+						break;
+					case 1:
+						playerSP.mc.ingameGUI.addChatMessage("\u3053\u3093\u306b\u3061\u306f\u3001VGhlIFdhdGNoZXI=\u3067\u3059");
+						break;
+					case 2:
+						playerSP.mc.ingameGUI.addChatMessage("Hello, I'm VGhlIFdhdGNoZXI=");
+						break;
+					case 3:
+						playerSP.mc.ingameGUI.addChatMessage("PPPoE\u306b\u63a5\u7d9a\u3057\u307e\u305b\u3093\u3067\u3057\u305f\u3002");
+						break;
+					case 4:
+						playerSP.mc.ingameGUI.addChatMessage("Wonderful place");
+						break;
+					case 5:
+						playerSP.mc.ingameGUI.addChatMessage("The Watcher looks at me, eager to pray.");
+						break;
+					case 6:
+						playerSP.mc.ingameGUI.addChatMessage("It didn't connect to PPPoE.");
+						break;
+					case 7:
+						playerSP.mc.ingameGUI.addChatMessage("V29uZGVyZnVsIHBsYWNl");
+						break;
+					}
+					if(this.timer[7] >= 20 * 10) {
+						this.timerS[7] = false;
+						this.timer[7] = 0;
+					}
+				}
+				if(this.worldInfo.isXXXX() >= 5) {
+					this.worldInfo.setXXXX(0);
+					playerSP.mc.displayGuiScreen(new GuiKnewScreen());
+				}
+			}
+		}
 
 		this.getWorldChunkManager().cleanupCache();
 		this.updateWeather();
@@ -2542,7 +3463,7 @@
 		if(var10 != null && !this.checkIfAABBIsClear(var10)) {
 			return false;
 		} else {
-			if(var8 == Block.waterMoving || var8 == Block.waterStill || var8 == Block.lavaMoving || var8 == Block.lavaStill || var8 == Block.fire || var8 == Block.snow || var8 == Block.vine) {
+			if(var8 == Block.waterMoving || var8 == Block.waterStill || var8 == Block.lavaMoving || var8 == Block.lavaStill || var8 == Block.fire || var8 == Block.snow || var8 == Block.vine || var8 == Block.ErrorWaterMoving || var8 == Block.ErrorWaterStill) {
 				var8 = null;
 			}
 
--- /dev/null
+++ net/minecraft/src/EntityNNi.java
@@ -1,0 +1,49 @@
+package net.minecraft.src;
+
+public class EntityNNi extends EntityNotVillagerMob {
+
+	private int timer = 0;
+
+	public EntityNNi(World var1) {
+		super(var1);
+		this.texture = "/mob/nn2.png";
+		this.moveSpeed = 666.666F;
+	}
+
+	public int getMaxHealth() {
+		return 66;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		/**
+		 if (!this.worldObj.multiplayerWorld) {
+		 this.timer++;
+		 if (this.timer >= 6 + 6 * 6) {
+		 this.worldObj.createExplosion(this, this.posX, this.posY, this.posZ, 6.66F);
+		 this.timer = 0;
+		 }
+		 }
+		 */
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- /dev/null
+++ net/minecraft/src/RenderPPPoE.java
@@ -1,0 +1,20 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+public class RenderPPPoE extends RenderLiving {
+	protected ModelPPPoE modelBipedMain;
+	protected float field_40296_d;
+
+	public RenderPPPoE(ModelPPPoE var1, float var2) {
+		this(var1, var2, 1.0F);
+		this.modelBipedMain = var1;
+	}
+
+	public RenderPPPoE(ModelPPPoE var1, float var2, float var3) {
+		super(var1, var2);
+		this.modelBipedMain = var1;
+		this.field_40296_d = var3;
+	}
+
+}
--- net/minecraft/src/EntityRenderer.java
+++ net/minecraft/src/EntityRenderer.java
@@ -4,6 +4,7 @@
 import java.nio.FloatBuffer;
 import java.util.List;
 import java.util.Random;
+
 import net.minecraft.client.Minecraft;
 import org.lwjgl.input.Mouse;
 import org.lwjgl.opengl.Display;
@@ -428,7 +429,25 @@
 
 			if(this.mc.gameSettings.thirdPersonView == 0 && !this.mc.renderViewEntity.isPlayerSleeping() && !this.mc.gameSettings.hideGUI && !this.mc.playerController.func_35643_e()) {
 				this.enableLightmap((double)var1);
-				this.itemRenderer.renderItemInFirstPerson(var1);
+				if(this.mc.theWorld.isBoundingBoxBurningError(this.mc.thePlayer.boundingBox.contract(0.001D, 0.001D, 0.001D))) {
+					GL11.glMatrixMode(GL11.GL_TEXTURE);
+
+					GL11.glPushMatrix();
+
+					GL11.glLoadIdentity();
+					float offsetX = (float)(Math.random() * 0.2 - 0.1);
+					float offsetY = (float)(Math.random() * 0.2 - 0.1);
+					GL11.glTranslatef(offsetX, offsetY, 0.0f);
+
+					GL11.glMatrixMode(GL11.GL_MODELVIEW);
+					this.itemRenderer.renderItemInFirstPerson(var1);
+					GL11.glMatrixMode(GL11.GL_TEXTURE);
+					GL11.glPopMatrix();
+
+					GL11.glMatrixMode(GL11.GL_MODELVIEW);
+				} else {
+					this.itemRenderer.renderItemInFirstPerson(var1);
+				}
 				this.disableLightmap((double)var1);
 			}
 
@@ -768,7 +787,11 @@
 
 			this.setupFog(0, var1);
 			GL11.glEnable(GL11.GL_FOG);
-			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+			if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain2.png"));
+			} else {
+				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+			}
 			RenderHelper.disableStandardItemLighting();
 			Profiler.endStartSection("terrain");
 			var5.sortAndRender(var4, 0, (double)var1);
@@ -803,7 +826,11 @@
 			this.setupFog(0, var1);
 			GL11.glEnable(GL11.GL_BLEND);
 			GL11.glDisable(GL11.GL_CULL_FACE);
-			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+			if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain2.png"));
+			} else {
+				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+			}
 			if(this.mc.gameSettings.fancyGraphics) {
 				Profiler.endStartSection("water");
 				if(this.mc.gameSettings.ambientOcclusion) {
@@ -1161,6 +1188,10 @@
 			this.fogColorRed = 0.02F;
 			this.fogColorGreen = 0.02F;
 			this.fogColorBlue = 0.2F;
+		} else if(var21 != 0 && Block.blocksList[var21].blockMaterial == Material.errorw) {
+			this.fogColorRed = new Random().nextFloat();
+			this.fogColorGreen = new Random().nextFloat();
+			this.fogColorBlue = new Random().nextFloat();
 		} else if(var21 != 0 && Block.blocksList[var21].blockMaterial == Material.lava) {
 			this.fogColorRed = 0.6F;
 			this.fogColorGreen = 0.1F;
--- /dev/null
+++ net/minecraft/src/EntityPPPoE.java
@@ -1,0 +1,54 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityPPPoE extends EntityNotVillagerMob {
+
+	public EntityPPPoE(World var1) {
+		super(var1);
+		this.texture = "/mob/pppoe.png";
+		this.moveSpeed = 6.66F;
+	}
+
+	public int getMaxHealth() {
+		return 1000;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 30) != null) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 30);
+				if(this.getDistanceToEntity(playerSP) <= 18) {
+					this.setEntityDead();
+					this.worldObj.timerS[5] = true;
+					new Thread(()->WavPlaySound.playsound("/sounds/PPPoE.wav")).start();
+				}
+			}
+		}
+	}
+
+	public boolean canBreatheUnderwater() {
+		return true;
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntityTentative.java
@@ -1,0 +1,63 @@
+package net.minecraft.src;
+
+public class EntityTentative extends EntityAnimal {
+
+	public int timer = 0;
+
+	public EntityTentative(World var1) {
+		super(var1);
+		this.texture = "/mob/tentative.png";
+		this.moveSpeed = 0.0F;
+	}
+
+	public int getMaxHealth() {
+		return 50;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(this.worldObj.getClosestPlayerToEntity(this, 30) != null) {
+			EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 30);
+			if(this.getDistanceToEntity(playerSP) <= 16) {
+				if(this.timer == 0) {
+					new Thread(()->WavPlaySound.playsound("/sounds/tentative.wav")).start();
+					this.timer = 20 * 2;
+				}
+				this.timer--;
+				playerSP.mc.ingameGUI.glitch2 = true;
+			} else {
+				this.timer = 0;
+				playerSP.mc.ingameGUI.glitch2 = false;
+			}
+			if(this.getDistanceToEntity(playerSP) <= 8) {
+				playerSP.mc.ingameGUI.glitch2 = false;
+				this.setEntityDead();
+				throw new Error("Error!: This Entity is Tentative.");
+			}
+		}
+	}
+
+	protected EntityAnimal spawnBabyAnimal(EntityAnimal var1) {
+		return null;
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return false;
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntityNN.java
@@ -1,0 +1,65 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityNN extends EntityNotVillagerMob {
+
+	public EntityNN(World var1) {
+		super(var1);
+		this.texture = "/mob/nn.png";
+		this.moveSpeed = 1.25F;
+	}
+
+	public int getMaxHealth() {
+		return 1000;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 30) != null) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 30);
+				if(this.getDistanceToEntity(playerSP) <= 18 && this.worldObj.worldInfo.isNNSX() == 0) {
+					this.setEntityDead();
+					for(int i = 0; i < 12; i++) {
+						try {
+							new Thread(()->{
+								WavPlaySound.playsound("/sounds/NN.wav");
+								Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+							}).start();
+							Thread.sleep(120);
+						} catch (InterruptedException e) {
+							throw new RuntimeException(e);
+						}
+					}
+					try {
+						Thread.sleep(1000 * 2);
+					} catch (InterruptedException e) {
+						throw new RuntimeException(e);
+					}
+					this.worldObj.worldInfo.setNNSX(1);
+				}
+			}
+		}
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/ComponentVillageChurch.java
+++ net/minecraft/src/ComponentVillageChurch.java
@@ -96,7 +96,6 @@
 				this.fillCurrentPositionBlocksDownwards(var1, Block.cobblestone.blockID, 0, var6, -1, var5, var3);
 			}
 		}
-
 		this.spawnVillagers(var1, var3, 2, 1, 2, 1);
 		return true;
 	}
--- /dev/null
+++ net/minecraft/src/WavPlaySound.java
@@ -1,0 +1,37 @@
+package net.minecraft.src;
+
+import java.io.BufferedInputStream;
+import java.io.IOException;
+import java.io.InputStream;
+import javax.sound.sampled.*;
+
+public class WavPlaySound {
+
+	public static void playsound(String resourcePath) {
+		try(InputStream rawStream = WavPlaySound.class.getResourceAsStream(resourcePath)) {
+			if(rawStream == null) {
+				System.err.println("Non Resource: " + resourcePath); return;
+			}
+
+			try(BufferedInputStream bufferedStream = new BufferedInputStream(rawStream); AudioInputStream audioInputStream = AudioSystem.getAudioInputStream(bufferedStream)) {
+
+				Clip clip = AudioSystem.getClip(); clip.open(audioInputStream); clip.start();
+
+				clip.addLineListener(event->{
+					if(event.getType() == LineEvent.Type.STOP) {
+						clip.close();
+					}
+				});
+
+				while(clip.isRunning()) {
+					Thread.sleep(100);
+				}
+			}
+
+		} catch (UnsupportedAudioFileException | IOException | LineUnavailableException e) {
+			e.printStackTrace();
+		} catch (InterruptedException e) {
+			Thread.currentThread().interrupt();
+		}
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntitySegfaultra.java
@@ -1,0 +1,42 @@
+package net.minecraft.src;
+
+public class EntitySegfaultra extends EntityMob {
+
+	public EntitySegfaultra(World var1) {
+		super(var1);
+		this.texture = "/mob/segfaultra.png";
+		this.moveSpeed = 1.666F;
+	}
+
+	public int getMaxHealth() {
+		return 40;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return false;
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+
+	protected int getDropItemId() {
+		return Item.redstone.shiftedIndex;
+	}
+}
--- net/minecraft/src/GuiIngame.java
+++ net/minecraft/src/GuiIngame.java
@@ -4,7 +4,9 @@
 import java.util.ArrayList;
 import java.util.List;
 import java.util.Random;
+
 import net.minecraft.client.Minecraft;
+import org.lwjgl.opengl.Display;
 import org.lwjgl.opengl.GL11;
 import org.lwjgl.opengl.GL12;
 
@@ -20,6 +22,8 @@
 	private boolean recordIsPlaying = false;
 	public float damageGuiPartialTime;
 	float prevVignetteBrightness = 1.0F;
+	public String CrashText = "";
+	public int CtextLenght = 0;
 
 	public GuiIngame(Minecraft var1) {
 		this.mc = var1;
@@ -45,7 +49,8 @@
 
 		float var10;
 		if(!this.mc.thePlayer.isPotionActive(Potion.confusion)) {
-			var10 = this.mc.thePlayer.prevTimeInPortal + (this.mc.thePlayer.timeInPortal - this.mc.thePlayer.prevTimeInPortal) * var1;
+			var10 = this.mc.thePlayer.prevTimeInPortal
+					+ (this.mc.thePlayer.timeInPortal - this.mc.thePlayer.prevTimeInPortal) * var1;
 			if(var10 > 0.0F) {
 				this.renderPortalOverlay(var10, var6, var7);
 			}
@@ -183,7 +188,8 @@
 						var57 = 13;
 					}
 
-					if(this.mc.thePlayer.getFoodStats().getFoodSaturationLevel() <= 0.0F && this.updateCounter % (var16 * 3 + 1) == 0) {
+					if(this.mc.thePlayer.getFoodStats().getFoodSaturationLevel() <= 0.0F
+							&& this.updateCounter % (var16 * 3 + 1) == 0) {
 						var26 = var50 + (this.rand.nextInt(3) - 1);
 					}
 
@@ -277,7 +283,7 @@
 				GL11.glTranslatef(0.0F, 32.0F, 0.0F);
 			}
 
-			var8.drawStringWithShadow("Minecraft 1.1 (" + this.mc.debug + ")", 2, 2, 16777215);
+			var8.drawStringWithShadow("Mi\u00A7kn\u00A7rec\u00A7kra\u00A7rf\u00A7kt\u00A7r 1.1(Pending) (" + this.mc.debug + ")", 2, 2, 16777215);
 			var8.drawStringWithShadow(this.mc.debugInfoRenders(), 2, 12, 16777215);
 			var8.drawStringWithShadow(this.mc.func_6262_n(), 2, 22, 16777215);
 			var8.drawStringWithShadow(this.mc.debugInfoEntities(), 2, 32, 16777215);
@@ -286,16 +292,21 @@
 			long var39 = Runtime.getRuntime().totalMemory();
 			long var42 = Runtime.getRuntime().freeMemory();
 			long var45 = var39 - var42;
-			var48 = "Used memory: " + var45 * 100L / var34 + "% (" + var45 / 1024L / 1024L + "MB) of " + var34 / 1024L / 1024L + "MB";
+			var48 = "Used memory: " + var45 * 100L / var34 + "% (" + var45 / 1024L / 1024L + "MB) of "
+					+ var34 / 1024L / 1024L + "MB";
 			this.drawString(var8, var48, var6 - var8.getStringWidth(var48) - 2, 2, 14737632);
 			var48 = "Allocated memory: " + var39 * 100L / var34 + "% (" + var39 / 1024L / 1024L + "MB)";
 			this.drawString(var8, var48, var6 - var8.getStringWidth(var48) - 2, 12, 14737632);
 			this.drawString(var8, "x: " + this.mc.thePlayer.posX, 2, 64, 14737632);
 			this.drawString(var8, "y: " + this.mc.thePlayer.posY, 2, 72, 14737632);
 			this.drawString(var8, "z: " + this.mc.thePlayer.posZ, 2, 80, 14737632);
-			this.drawString(var8, "f: " + (MathHelper.floor_double((double)(this.mc.thePlayer.rotationYaw * 4.0F / 360.0F) + 0.5D) & 3), 2, 88, 14737632);
+			this.drawString(var8, "f: "
+							+ (MathHelper.floor_double((double)(this.mc.thePlayer.rotationYaw * 4.0F / 360.0F) + 0.5D) & 3), 2,
+					88, 14737632);
 			this.drawString(var8, "Seed: " + this.mc.theWorld.getWorldSeed(), 2, 104, 14737632);
 			GL11.glPopMatrix();
+		} else {
+			var8.drawStringWithShadow("Mi\u00A7kn\u00A7rec\u00A7kra\u00A7rf\u00A7kt\u00A7r 1.\u00A7k1\u00A7r \u00A74Cra\u00A7ksh\u00A7red 744\u00A7r", 2, 2, 16777215);
 		}
 
 		if(this.recordPlayingUpFor > 0) {
@@ -315,7 +326,8 @@
 					var12 = Color.HSBtoRGB(var10 / 50.0F, 0.7F, 0.6F) & 16777215;
 				}
 
-				var8.drawString(this.recordPlaying, -var8.getStringWidth(this.recordPlaying) / 2, -4, var12 + (var38 << 24));
+				var8.drawString(this.recordPlaying, -var8.getStringWidth(this.recordPlaying) / 2, -4,
+						var12 + (var38 << 24));
 				GL11.glDisable(GL11.GL_BLEND);
 				GL11.glPopMatrix();
 			}
@@ -421,17 +433,128 @@
 			}
 		}
 
+		//this.liner();
+		if(this.glitch) {
+			for(int i = 0; i < this.mc.displayWidth / 4; i++) {
+				String text = "";
+				switch(new Random().nextInt(4)) {
+				case 0:
+					text = "\u7e3a\u3085\u2191\u7e3a\u6e98\uff05\u7e67\u8599\u2193\u7e3a\uff61\u7e3a\uff6f\u7e3a\uff67\u7e3a\u5436\u2260\u7e3a\uff6a\u7e3a\u6e98\u3012\u7e3a\u5436\u00b0";
+					break;
+				case 1:
+					text = "57i644KF4oaR57i65rqYP+e4uu+9p+e4uuWQtuKJoOe4uu+9que4uua6mD/nuLrvvafnuLrlkLbCsA==";
+					break;
+				case 2:
+					text = "The_Watcher joined The game.";
+					break;
+				case 3:
+					text = "57i6772457i6772j57i66JaZ4oiI57mn6ZiqPw==";
+					break;
+				}
+				this.drawString(var8, text, new Random().nextInt(Display.getWidth()) - 150, new Random().nextInt(Display.getHeight()) - 75, 16711680);
+			}
+		}
+
+		if(this.mc.theWorld.timerS[5]) {
+			this.renderTextureFull(var6, var7, "/misc/pppoe.png");
+		}
+
+		if(this.mc.theWorld.timerS[6]) {
+			this.renderCrashText(var6, var7);
+		}
+
+		if(this.mc.theWorld.timerS[8]) {
+			this.renderTextureFull(var6, var7, "/misc/nn" + new Random().nextInt(2) + ".png");
+		}
+
 		GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
 		GL11.glDisable(GL11.GL_LIGHTING);
 		GL11.glEnable(GL11.GL_ALPHA_TEST);
 	}
 
+	protected void renderCrashText(int var1, int var2) {
+		String text = "";
+		String text2 = "";
+		boolean textrender = true;
+		if(this.mc.theWorld.timer[6] >= 2) {
+			text = "";
+		}
+		if(this.mc.theWorld.timer[6] == 2) {
+			new Thread(()->{
+				WavPlaySound.playsound("/sounds/crash.wav");
+			}).start();
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 1) {
+			text = "T";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 2) {
+			text = "Th";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 3) {
+			text = "The";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 4) {
+			text = "They";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 5) {
+			text = "TheyW";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 6) {
+			text = "TheyWa";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 7) {
+			text = "TheyWai";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 8) {
+			text = "TheyWait";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 9) {
+			text = "TheyWait?";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 10) {
+			text = "TheyWait?";
+			textrender = false;
+		}
+		if(this.mc.theWorld.timer[6] == 2 + 10 * 10) {
+			new Thread(()->{
+				WavPlaySound.playsound("/sounds/wait.wav");
+			}).start();
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 11) {
+			textrender = true;
+			text2 = "\ua4a6\u5f85\u04a8\u308b\u3087?";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 12) {
+			textrender = false;
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 13) {
+			textrender = true;
+			text2 = "\u30de\u2020\u30c3\u30c6\u25b9\u30eb\u3087\u2026?";
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 14) {
+			textrender = false;
+		}
+		if(this.mc.theWorld.timer[6] >= 2 + 10 * 15) {
+			textrender = true;
+			text2 = "\u5f85\u2297\u30c6\u308b\u2234?";
+		}
+
+		if(textrender) {
+			GL11.glPushMatrix();
+			GL11.glScalef(2.0F, 2.0F, 2.0F);
+			this.drawCenteredString(this.mc.fontRenderer, text, var1 / 2 / 2, var2 / 2 / 2 - 10, 16777215);
+			this.drawCenteredString(this.mc.fontRenderer, text2, var1 / 2 / 2, var2 / 2 / 2, 16777215);
+			GL11.glPopMatrix();
+		}
+	}
+
 	private void renderBossHealth() {
 		if(RenderDragon.entityDragon != null) {
 			EntityDragon var1 = RenderDragon.entityDragon;
 			RenderDragon.entityDragon = null;
 			FontRenderer var2 = this.mc.fontRenderer;
-			ScaledResolution var3 = new ScaledResolution(this.mc.gameSettings, this.mc.displayWidth, this.mc.displayHeight);
+			ScaledResolution var3 = new ScaledResolution(this.mc.gameSettings, this.mc.displayWidth,
+					this.mc.displayHeight);
 			int var4 = var3.getScaledWidth();
 			short var5 = 182;
 			int var6 = var4 / 2 - var5 / 2;
@@ -470,6 +593,27 @@
 		GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
 	}
 
+	private void renderTextureFull(int var1, int var2, String tex) {
+		GL11.glDisable(GL11.GL_DEPTH_TEST);
+		GL11.glDepthMask(false);
+		GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);
+		GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
+		GL11.glDisable(GL11.GL_ALPHA_TEST);
+		GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture(tex));
+		Tessellator var3 = Tessellator.instance;
+		var3.startDrawingQuads();
+		var3.addVertexWithUV(0.0D, (double)var2, -90.0D, 0.0D, 1.0D);
+		var3.addVertexWithUV((double)var1, (double)var2, -90.0D, 1.0D, 1.0D);
+		var3.addVertexWithUV((double)var1, 0.0D, -90.0D, 1.0D, 0.0D);
+		var3.addVertexWithUV(0.0D, 0.0D, -90.0D, 0.0D, 0.0D);
+		var3.draw();
+		GL11.glDepthMask(true);
+		GL11.glEnable(GL11.GL_DEPTH_TEST);
+		GL11.glEnable(GL11.GL_ALPHA_TEST);
+		GL11.glColor4f(1.0F, 1.0F, 1.0F, 1.0F);
+	}
+
+
 	private void renderVignette(float var1, int var2, int var3) {
 		var1 = 1.0F - var1;
 		if(var1 < 0.0F) {
@@ -480,7 +624,8 @@
 			var1 = 1.0F;
 		}
 
-		this.prevVignetteBrightness = (float)((double)this.prevVignetteBrightness + (double)(var1 - this.prevVignetteBrightness) * 0.01D);
+		this.prevVignetteBrightness = (float)((double)this.prevVignetteBrightness
+				+ (double)(var1 - this.prevVignetteBrightness) * 0.01D);
 		GL11.glDisable(GL11.GL_DEPTH_TEST);
 		GL11.glDepthMask(false);
 		GL11.glBlendFunc(GL11.GL_ZERO, GL11.GL_ONE_MINUS_SRC_COLOR);
@@ -570,7 +715,8 @@
 	public void addChatMessage(String var1) {
 		while(this.mc.fontRenderer.getStringWidth(var1) > 320) {
 			int var2;
-			for(var2 = 1; var2 < var1.length() && this.mc.fontRenderer.getStringWidth(var1.substring(0, var2 + 1)) <= 320; ++var2) {
+			for(var2 = 1; var2 < var1.length()
+					&& this.mc.fontRenderer.getStringWidth(var1.substring(0, var2 + 1)) <= 320; ++var2) {
 			}
 
 			this.addChatMessage(var1.substring(0, var2));
--- net/minecraft/src/EnumCreatureType.java
+++ net/minecraft/src/EnumCreatureType.java
@@ -3,6 +3,7 @@
 public enum EnumCreatureType {
 	monster(IMob.class, 70, Material.air, false),
 	creature(EntityAnimal.class, 15, Material.air, true),
+	error(EntityAnimal.class, 1, Material.air, true),
 	waterCreature(EntityWaterMob.class, 5, Material.water, true);
 
 	private final Class creatureClass;
--- /dev/null
+++ net/minecraft/src/EntityXXXX2.java
@@ -1,0 +1,60 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityXXXX2 extends EntityNotVillagerMob {
+
+	public EntityXXXX2(World var1) {
+		super(var1);
+		this.texture = "/mob/~~~~2.png";
+		this.moveSpeed = 0.00F;
+	}
+
+	public int getMaxHealth() {
+		return 9999;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 50) != null) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 50);
+				if(this.getDistanceToEntity(playerSP) <= 40) {
+					this.worldObj.worldInfo.setXXXX(this.worldObj.worldInfo.isXXXX() + 1);
+					this.setEntityDead();
+					int XX = new Random().nextInt(5000) - 2500;
+					int ZZ = new Random().nextInt(5000) - 2500;
+					int YY = this.worldObj.getblockheigthY(XX, ZZ);
+					for(int i = -2; i <= 2; i++) {
+						for(int j = -2; j <= 2; j++) {
+							this.worldObj.chunkProvider.loadChunk(XX, ZZ);
+						}
+					}
+					EntityXXXX2 xxxx2 = new EntityXXXX2(this.worldObj);
+					xxxx2.setLocationAndAngles(XX, YY + 2, ZZ, 0, 0);
+					this.worldObj.spawnEntityInWorld(xxxx2);
+				}
+			}
+		}
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/ComponentVillageWell.java
+++ net/minecraft/src/ComponentVillageWell.java
@@ -40,31 +40,36 @@
 
 		if(this.field_35104_a) {
 		}
-
-		this.fillWithBlocks(var1, var3, 1, 0, 1, 4, 12, 4, Block.cobblestone.blockID, Block.waterMoving.blockID, false);
-		this.placeBlockAtCurrentPosition(var1, 0, 0, 2, 12, 2, var3);
-		this.placeBlockAtCurrentPosition(var1, 0, 0, 3, 12, 2, var3);
-		this.placeBlockAtCurrentPosition(var1, 0, 0, 2, 12, 3, var3);
-		this.placeBlockAtCurrentPosition(var1, 0, 0, 3, 12, 3, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 13, 1, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 14, 1, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 13, 1, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 14, 1, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 13, 4, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 14, 4, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 13, 4, var3);
-		this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 14, 4, var3);
-		this.fillWithBlocks(var1, var3, 1, 15, 1, 4, 15, 4, Block.cobblestone.blockID, Block.cobblestone.blockID, false);
-
-		for(int var4 = 0; var4 <= 5; ++var4) {
-			for(int var5 = 0; var5 <= 5; ++var5) {
-				if(var5 == 0 || var5 == 5 || var4 == 0 || var4 == 5) {
-					this.placeBlockAtCurrentPosition(var1, Block.gravel.blockID, 0, var5, 11, var4, var3);
-					this.clearCurrentPositionBlocksUpwards(var1, var5, 12, var4, var3);
+		if(this.none_village) {
+			this.placeBlockAtCurrentPosition(var1, Block.dontBroken.blockID, 0, 0, 12, 0, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.dontBrokenhead.blockID, 0, 0, 13, 0, var3);
+			this.SpawnNoneVillager(var1, var3, 0, 15, 0);
+		} else {
+			this.fillWithBlocks(var1, var3, 1, 0, 1, 4, 12, 4, Block.cobblestone.blockID, Block.waterMoving.blockID, false);
+			this.placeBlockAtCurrentPosition(var1, 0, 0, 2, 12, 2, var3);
+			this.placeBlockAtCurrentPosition(var1, 0, 0, 3, 12, 2, var3);
+			this.placeBlockAtCurrentPosition(var1, 0, 0, 2, 12, 3, var3);
+			this.placeBlockAtCurrentPosition(var1, 0, 0, 3, 12, 3, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 13, 1, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 14, 1, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 13, 1, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 14, 1, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 13, 4, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 1, 14, 4, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 13, 4, var3);
+			this.placeBlockAtCurrentPosition(var1, Block.fence.blockID, 0, 4, 14, 4, var3);
+			this.fillWithBlocks(var1, var3, 1, 15, 1, 4, 15, 4, Block.cobblestone.blockID, Block.cobblestone.blockID, false);
+
+			for(int var4 = 0; var4 <= 5; ++var4) {
+				for(int var5 = 0; var5 <= 5; ++var5) {
+					if(var5 == 0 || var5 == 5 || var4 == 0 || var4 == 5) {
+						this.placeBlockAtCurrentPosition(var1, Block.gravel.blockID, 0, var5, 11, var4, var3);
+						this.clearCurrentPositionBlocksUpwards(var1, var5, 12, var4, var3);
+					}
 				}
 			}
+
 		}
-
 		return true;
 	}
 }
--- /dev/null
+++ net/minecraft/src/EntityXXXX1.java
@@ -1,0 +1,60 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityXXXX1 extends EntityNotVillagerMob {
+
+	public EntityXXXX1(World var1) {
+		super(var1);
+		this.texture = "/mob/~~~~1.png";
+		this.moveSpeed = 0.00F;
+	}
+
+	public int getMaxHealth() {
+		return 9999;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 50) != null) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 50);
+				if(this.getDistanceToEntity(playerSP) <= 40) {
+					this.worldObj.worldInfo.setXXXX(this.worldObj.worldInfo.isXXXX() + 1);
+					this.setEntityDead();
+					int XX = new Random().nextInt(5000) - 2500;
+					int ZZ = new Random().nextInt(5000) - 2500;
+					int YY = this.worldObj.getblockheigthY(XX, ZZ);
+					for(int i = -2; i <= 2; i++) {
+						for(int j = -2; j <= 2; j++) {
+							this.worldObj.chunkProvider.loadChunk(XX, ZZ);
+						}
+					}
+					EntityXXXX1 xxxx1 = new EntityXXXX1(this.worldObj);
+					xxxx1.setLocationAndAngles(XX, YY + 2, ZZ, 0, 0);
+					this.worldObj.spawnEntityInWorld(xxxx1);
+				}
+			}
+		}
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/ComponentVillageWoodHut.java
+++ net/minecraft/src/ComponentVillageWoodHut.java
@@ -81,7 +81,6 @@
 				this.fillCurrentPositionBlocksDownwards(var1, Block.cobblestone.blockID, 0, var5, -1, var4, var3);
 			}
 		}
-
 		this.spawnVillagers(var1, var3, 1, 1, 2, 1);
 		return true;
 	}
--- /dev/null
+++ net/minecraft/src/EntityNotVillagerMob.java
@@ -1,0 +1,110 @@
+package net.minecraft.src;
+
+public abstract class EntityNotVillagerMob extends EntityCreature {
+	protected int attackStrength = 2;
+
+	public EntityNotVillagerMob(World var1) {
+		super(var1);
+		this.experienceValue = 5;
+	}
+
+	public void onLivingUpdate() {
+		float var1 = this.getEntityBrightness(1.0F);
+		if(var1 > 0.5F) {
+			this.entityAge += 2;
+		}
+
+		super.onLivingUpdate();
+	}
+
+	public void onUpdate() {
+		super.onUpdate();
+		if(!this.worldObj.multiplayerWorld && this.worldObj.difficultySetting == 0) {
+			this.setEntityDead();
+		}
+
+	}
+
+	protected Entity findPlayerToAttack() {
+		EntityPlayer var1 = this.worldObj.getClosestVulnerablePlayerToEntity(this, 32.0D);
+		return var1 != null && this.canEntityBeSeen(var1) ? var1 : null;
+	}
+
+	public boolean attackEntityFrom(DamageSource var1, int var2) {
+		if(super.attackEntityFrom(var1, var2)) {
+			Entity var3 = var1.getEntity();
+			if(this.riddenByEntity != var3 && this.ridingEntity != var3) {
+				if(var3 != this) {
+					this.entityToAttack = var3;
+					this.field_46020_bQ = var3 instanceof EntityLiving ? (EntityLiving)var3 : null;
+				}
+
+				return true;
+			} else {
+				return true;
+			}
+		} else {
+			return false;
+		}
+	}
+
+	public boolean attackEntityAsMob(Entity var1) {
+		int var2 = this.attackStrength;
+		if(this.isPotionActive(Potion.damageBoost)) {
+			var2 += 3 << this.getActivePotionEffect(Potion.damageBoost).getAmplifier();
+		}
+
+		if(this.isPotionActive(Potion.weakness)) {
+			var2 -= 2 << this.getActivePotionEffect(Potion.weakness).getAmplifier();
+		}
+
+		return var1.attackEntityFrom(DamageSource.causeMobDamage(this), var2);
+	}
+
+	protected void attackEntity(Entity var1, float var2) {
+		if(this.attackTime <= 0 && var2 < 2.0F && var1.boundingBox.maxY > this.boundingBox.minY && var1.boundingBox.minY < this.boundingBox.maxY) {
+			this.attackTime = 20;
+			this.attackEntityAsMob(var1);
+		}
+
+	}
+
+	public float getBlockPathWeight(int var1, int var2, int var3) {
+		return 0.5F - this.worldObj.getLightBrightness(var1, var2, var3);
+	}
+
+	public void writeEntityToNBT(NBTTagCompound var1) {
+		super.writeEntityToNBT(var1);
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean func_40147_Y() {
+		int var1 = MathHelper.floor_double(this.posX);
+		int var2 = MathHelper.floor_double(this.boundingBox.minY);
+		int var3 = MathHelper.floor_double(this.posZ);
+		if(this.worldObj.getSavedLightValue(EnumSkyBlock.Sky, var1, var2, var3) > this.rand.nextInt(32)) {
+			return false;
+		} else {
+			int var4 = this.worldObj.getBlockLightValue(var1, var2, var3);
+			if(this.worldObj.getIsThundering()) {
+				int var5 = this.worldObj.skylightSubtracted;
+				this.worldObj.skylightSubtracted = 10;
+				var4 = this.worldObj.getBlockLightValue(var1, var2, var3);
+				this.worldObj.skylightSubtracted = var5;
+			}
+
+			return var4 <= this.rand.nextInt(8);
+		}
+	}
+
+	protected boolean canDespawn() {
+		return false;
+	}
+
+	public boolean getCanSpawnHere() {
+		return this.func_40147_Y() && super.getCanSpawnHere();
+	}
+}
--- /dev/null
+++ net/minecraft/src/Packet81Sound.java
@@ -1,0 +1,36 @@
+package net.minecraft.src;
+
+import java.io.DataInputStream;
+import java.io.DataOutputStream;
+import java.io.IOException;
+
+public class Packet81Sound extends Packet {
+	public String path;
+
+	public Packet81Sound() {
+	}
+
+	public Packet81Sound(String var1) {
+		if(var1.length() > 119) {
+			var1 = var1.substring(0, 119);
+		}
+
+		this.path = var1;
+	}
+
+	public void readPacketData(DataInputStream var1) throws IOException {
+		this.path = readString(var1, 119);
+	}
+
+	public void writePacketData(DataOutputStream var1) throws IOException {
+		writeString(this.path, var1);
+	}
+
+	public void processPacket(NetHandler var1) {
+		var1.handleSound(this);
+	}
+
+	public int getPacketSize() {
+		return this.path.length();
+	}
+}
--- net/minecraft/src/PathFinder.java
+++ net/minecraft/src/PathFinder.java
@@ -135,7 +135,7 @@
 				}
 			}
 
-			if(var9 == -2) {
+			if(var9 == -2 || var9 == -3) {
 				return null;
 			}
 		}
@@ -172,6 +172,10 @@
 
 							if(var11 == Material.lava) {
 								return -2;
+							}
+
+							if(var11 == Material.errorw) {
+								return -3;
 							}
 						} else {
 							int var10 = this.worldMap.getBlockMetadata(var6, var7, var8);
--- net/minecraft/src/PanelCrashReport.java
+++ net/minecraft/src/PanelCrashReport.java
@@ -25,7 +25,7 @@
 		try {
 			var5 = var5 + "Generated " + (new SimpleDateFormat()).format(new Date()) + "\n";
 			var5 = var5 + "\n";
-			var5 = var5 + "Minecraft: Minecraft 1.1\n";
+			var5 = var5 + "Minecraft: Minecraft 1.1 (Pending)\n";
 			var5 = var5 + "OS: " + System.getProperty("os.name") + " (" + System.getProperty("os.arch") + ") version " + System.getProperty("os.version") + "\n";
 			var5 = var5 + "Java: " + System.getProperty("java.version") + ", " + System.getProperty("java.vendor") + "\n";
 			var5 = var5 + "VM: " + System.getProperty("java.vm.name") + " (" + System.getProperty("java.vm.info") + "), " + System.getProperty("java.vm.vendor") + "\n";
--- /dev/null
+++ net/minecraft/src/GuiYesNoTheWatcher.java
@@ -1,0 +1,36 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class GuiYesNoTheWatcher extends GuiScreen {
+	private String message1;
+	private String message2;
+	private String buttonText1;
+	private String buttonText2;
+
+	public GuiYesNoTheWatcher(String var2, String var3, String var4, String var5) {
+		this.message1 = var2;
+		this.message2 = var3;
+		this.buttonText1 = var4;
+		this.buttonText2 = var5;
+	}
+
+	public void initGui() {
+		this.controlList.add(new GuiSmallButton(0, this.width / 2 - 155 + 0, this.height / 6 + 96, this.buttonText1));
+		this.controlList.add(new GuiSmallButton(1, this.width / 2 - 155 + 160, this.height / 6 + 96, this.buttonText2));
+	}
+
+	protected void actionPerformed(GuiButton var1) {
+		if(new Random().nextInt(6) == 0) {
+		} else {
+			this.mc.displayGuiScreen(null);
+		}
+	}
+
+	public void drawScreen(int var1, int var2, float var3) {
+		this.drawBackground(0);
+		this.drawCenteredString(this.fontRenderer, this.message1, this.width / 2, 70, 16777215);
+		this.drawCenteredString(this.fontRenderer, this.message2, this.width / 2, 90, 16777215);
+		super.drawScreen(var1, var2, var3);
+	}
+}
--- /dev/null
+++ net/minecraft/src/RenderCrashEcho.java
@@ -1,0 +1,29 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+public class RenderCrashEcho extends RenderLiving {
+	protected ModelCrashEcho modelBipedMain;
+	protected float field_40296_d;
+	private float scale;
+
+	public RenderCrashEcho(ModelCrashEcho var1, float var2) {
+		this(var1, var2, 1.0F);
+		this.modelBipedMain = var1;
+		this.scale = 6.66F;
+	}
+
+	public RenderCrashEcho(ModelCrashEcho var1, float var2, float var3) {
+		super(var1, var2);
+		this.modelBipedMain = var1;
+		this.field_40296_d = var3;
+	}
+
+	protected void a(EntityCrashEcho var1, float var2) {
+		GL11.glScalef(this.scale, this.scale, this.scale);
+	}
+
+	protected void preRenderCallback(EntityLiving var1, float var2) {
+		this.a((EntityCrashEcho)var1, var2);
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntityTm9uZQ.java
@@ -1,0 +1,42 @@
+package net.minecraft.src;
+
+public class EntityTm9uZQ extends EntityNotVillagerMob {
+
+	public EntityTm9uZQ(World var1) {
+		super(var1);
+		this.texture = "/mob/Tm9uZQ==.png";
+		this.moveSpeed = 1.5F;
+	}
+
+	public int getMaxHealth() {
+		return 30;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+
+	protected int getDropItemId() {
+		return Item.fishRaw.shiftedIndex;
+	}
+}
--- /dev/null
+++ net/minecraft/src/ModelCrashEcho.java
@@ -1,0 +1,103 @@
+package net.minecraft.src;
+
+public class ModelCrashEcho extends ModelBase {
+	public ModelRenderer root;
+	public ModelRenderer waist;
+	public ModelRenderer body;
+	public ModelRenderer head;
+	public ModelRenderer speaker1;
+	public ModelRenderer speaker2;
+	public ModelRenderer leftArm;
+	public ModelRenderer leftItem;
+	public ModelRenderer rightArm;
+	public ModelRenderer rightItem;
+	public ModelRenderer leftLeg;
+	public ModelRenderer rightLeg;
+
+	public ModelCrashEcho() {
+		this(0.0F);
+	}
+
+	public ModelCrashEcho(float var1) {
+		this(var1, 0.0F);
+	}
+
+	public ModelCrashEcho(float var1, float var2) {
+		this.textureWidth = 32;
+		this.textureHeight = 32;
+
+		root = new ModelRenderer(this).setTextureSize(32, 32);
+		root.setRotationPoint(0.0F, 24.0F, 0.0F);
+
+
+		waist = new ModelRenderer(this).setTextureSize(32, 32);
+		waist.setRotationPoint(0.0F, -8.0F, 0.0F);
+		root.addChild(waist);
+
+
+		body = new ModelRenderer(this).setTextureSize(32, 32);
+		body.setRotationPoint(0.0F, -2.0F, 1.0F);
+		waist.addChild(body);
+		body.cubeList.add(new ModelBox(body, 0, 5, -2.0F, -8.0F, -1.0F, 4, 8, 2, 0.0F));
+		body.cubeList.add(new ModelBox(body, 12, 5, -0.25F, -6.0F, 0.25F, 3, 3, 1, 0.0F));
+
+		head = new ModelRenderer(this).setTextureSize(32, 32);
+		head.setRotationPoint(1.0F, -10.0F, -2.0F);
+		body.addChild(head);
+		head.cubeList.add(new ModelBox(head, 0, 0, -1.5F, -2.0F, 1.5F, 1, 4, 1, 0.2F));
+
+		speaker1 = new ModelRenderer(this).setTextureSize(32, 32);
+		speaker1.setRotationPoint(0.0F, 0.0F, 0.0F);
+		head.addChild(speaker1);
+		speaker1.cubeList.add(new ModelBox(speaker1, 4, 0, -1.5F, -3.5F, -1.0F, 3, 3, 2, -0.1F));
+		speaker1.cubeList.add(new ModelBox(speaker1, 14, 0, -1.0F, -3.0F, 0.0F, 2, 2, 2, 0.0F));
+		speaker1.cubeList.add(new ModelBox(speaker1, 18, 9, -2.0F, -4.0F, -2.0F, 1, 4, 2, 0.0F));
+		speaker1.cubeList.add(new ModelBox(speaker1, 12, 9, 1.0F, -4.0F, -2.0F, 1, 4, 2, 0.0F));
+		speaker1.cubeList.add(new ModelBox(speaker1, 24, 13, -1.0F, -4.0F, -2.0F, 2, 1, 2, 0.0F));
+		speaker1.cubeList.add(new ModelBox(speaker1, 24, 19, -1.0F, -1.0F, -2.0F, 2, 1, 2, 0.0F));
+
+		speaker2 = new ModelRenderer(this).setTextureSize(32, 32);
+		speaker2.setRotationPoint(-3.0F, 2.0F, 3.0F);
+		head.addChild(speaker2);
+		speaker2.cubeList.add(new ModelBox(speaker2, 22, 4, -1.0F, -3.5F, -1.5F, 2, 3, 3, -0.1F));
+		speaker2.cubeList.add(new ModelBox(speaker2, 22, 0, 0.0F, -3.0F, -1.0F, 2, 2, 2, 0.0F));
+		speaker2.cubeList.add(new ModelBox(speaker2, 18, 15, -2.0F, -4.0F, 1.0F, 2, 4, 1, 0.0F));
+		speaker2.cubeList.add(new ModelBox(speaker2, 12, 15, -2.0F, -4.0F, -2.0F, 2, 4, 1, 0.0F));
+		speaker2.cubeList.add(new ModelBox(speaker2, 24, 10, -2.0F, -4.0F, -1.0F, 2, 1, 2, 0.0F));
+		speaker2.cubeList.add(new ModelBox(speaker2, 24, 16, -2.0F, -1.0F, -1.0F, 2, 1, 2, 0.0F));
+
+		leftArm = new ModelRenderer(this).setTextureSize(32, 32);
+		leftArm.setRotationPoint(0.0F, 0.0F, 0.0F);
+		body.addChild(leftArm);
+		leftArm.cubeList.add(new ModelBox(leftArm, 18, 21, 2.0F, -7.0F, -1.5F, 1, 10, 1, 0.0F));
+
+		leftItem = new ModelRenderer(this).setTextureSize(32, 32);
+		leftItem.setRotationPoint(0.0F, 0.0F, 0.0F);
+		leftArm.addChild(leftItem);
+
+
+		rightArm = new ModelRenderer(this).setTextureSize(32, 32);
+		rightArm.setRotationPoint(0.0F, 8.0F, -1.0F);
+		body.addChild(rightArm);
+		rightArm.cubeList.add(new ModelBox(rightArm, 12, 21, -3.0F, -15.0F, 0.5F, 1, 10, 1, 0.0F));
+
+		rightItem = new ModelRenderer(this).setTextureSize(32, 32);
+		rightItem.setRotationPoint(0.0F, 0.0F, 0.0F);
+		rightArm.addChild(rightItem);
+
+
+		leftLeg = new ModelRenderer(this).setTextureSize(32, 32);
+		leftLeg.setRotationPoint(0.0F, 0.0F, 0.0F);
+		root.addChild(leftLeg);
+		leftLeg.cubeList.add(new ModelBox(leftLeg, 6, 21, 0.5F, -10.0F, 0.5F, 1, 10, 1, 0.0F));
+
+		rightLeg = new ModelRenderer(this).setTextureSize(32, 32);
+		rightLeg.setRotationPoint(0.0F, 0.0F, 0.0F);
+		root.addChild(rightLeg);
+		rightLeg.cubeList.add(new ModelBox(rightLeg, 0, 21, -1.5F, -10.0F, 0.0F, 1, 10, 1, 0.0F));
+	}
+
+	public void render(Entity var1, float var2, float var3, float var4, float var5, float var6, float var7) {
+		root.render(var7);
+	}
+}
--- net/minecraft/client/Minecraft.java
+++ net/minecraft/client/Minecraft.java
@@ -10,100 +10,9 @@
 import java.io.File;
 import java.text.DecimalFormat;
 import java.util.List;
-import net.minecraft.src.AchievementList;
-import net.minecraft.src.AxisAlignedBB;
-import net.minecraft.src.Block;
-import net.minecraft.src.ChunkCoordinates;
-import net.minecraft.src.ChunkProviderLoadOrGenerate;
-import net.minecraft.src.ColorizerFoliage;
-import net.minecraft.src.ColorizerGrass;
-import net.minecraft.src.ColorizerWater;
-import net.minecraft.src.EffectRenderer;
-import net.minecraft.src.EntityClientPlayerMP;
-import net.minecraft.src.EntityLiving;
-import net.minecraft.src.EntityPlayer;
-import net.minecraft.src.EntityPlayerSP;
-import net.minecraft.src.EntityRenderer;
-import net.minecraft.src.EnumMovingObjectType;
-import net.minecraft.src.EnumOS2;
-import net.minecraft.src.EnumOSMappingHelper;
-import net.minecraft.src.EnumOptions;
-import net.minecraft.src.EnumWorldType;
-import net.minecraft.src.FontRenderer;
-import net.minecraft.src.GLAllocation;
-import net.minecraft.src.GameSettings;
-import net.minecraft.src.GameWindowListener;
-import net.minecraft.src.GuiAchievement;
-import net.minecraft.src.GuiChat;
-import net.minecraft.src.GuiConflictWarning;
-import net.minecraft.src.GuiConnecting;
-import net.minecraft.src.GuiErrorScreen;
-import net.minecraft.src.GuiGameOver;
-import net.minecraft.src.GuiIngame;
-import net.minecraft.src.GuiIngameMenu;
-import net.minecraft.src.GuiInventory;
-import net.minecraft.src.GuiMainMenu;
-import net.minecraft.src.GuiScreen;
-import net.minecraft.src.GuiSleepMP;
-import net.minecraft.src.GuiUnused;
-import net.minecraft.src.IChunkProvider;
-import net.minecraft.src.ISaveFormat;
-import net.minecraft.src.ISaveHandler;
-import net.minecraft.src.Item;
-import net.minecraft.src.ItemRenderer;
-import net.minecraft.src.ItemStack;
-import net.minecraft.src.KeyBinding;
-import net.minecraft.src.LoadingScreenRenderer;
-import net.minecraft.src.MathHelper;
-import net.minecraft.src.MinecraftError;
-import net.minecraft.src.MinecraftException;
-import net.minecraft.src.MinecraftImpl;
-import net.minecraft.src.ModelBiped;
-import net.minecraft.src.MouseHelper;
-import net.minecraft.src.MovementInputFromOptions;
-import net.minecraft.src.MovingObjectPosition;
-import net.minecraft.src.NetClientHandler;
-import net.minecraft.src.OpenGlCapsChecker;
-import net.minecraft.src.OpenGlHelper;
-import net.minecraft.src.PlayerController;
-import net.minecraft.src.PlayerControllerCreative;
-import net.minecraft.src.Profiler;
-import net.minecraft.src.ProfilerResult;
-import net.minecraft.src.RenderBlocks;
-import net.minecraft.src.RenderEngine;
-import net.minecraft.src.RenderGlobal;
-import net.minecraft.src.RenderManager;
-import net.minecraft.src.SaveConverterMcRegion;
-import net.minecraft.src.ScaledResolution;
-import net.minecraft.src.ScreenShotHelper;
-import net.minecraft.src.Session;
-import net.minecraft.src.SoundManager;
-import net.minecraft.src.StatCollector;
-import net.minecraft.src.StatFileWriter;
-import net.minecraft.src.StatList;
-import net.minecraft.src.StatStringFormatKeyInv;
-import net.minecraft.src.StringTranslate;
-import net.minecraft.src.Teleporter;
-import net.minecraft.src.Tessellator;
-import net.minecraft.src.TextureCompassFX;
-import net.minecraft.src.TextureFlamesFX;
-import net.minecraft.src.TextureLavaFX;
-import net.minecraft.src.TextureLavaFlowFX;
-import net.minecraft.src.TexturePackList;
-import net.minecraft.src.TexturePortalFX;
-import net.minecraft.src.TextureWatchFX;
-import net.minecraft.src.TextureWaterFX;
-import net.minecraft.src.TextureWaterFlowFX;
-import net.minecraft.src.ThreadCheckHasPaid;
-import net.minecraft.src.ThreadDownloadResources;
-import net.minecraft.src.ThreadSleepForever;
-import net.minecraft.src.Timer;
-import net.minecraft.src.UnexpectedThrowable;
-import net.minecraft.src.Vec3D;
-import net.minecraft.src.World;
-import net.minecraft.src.WorldProvider;
-import net.minecraft.src.WorldRenderer;
-import net.minecraft.src.WorldSettings;
+import java.util.Random;
+
+import net.minecraft.src.*;
 import org.lwjgl.LWJGLException;
 import org.lwjgl.input.Controllers;
 import org.lwjgl.input.Keyboard;
@@ -179,6 +88,7 @@
 	public boolean isRaining = false;
 	long systemTime = System.currentTimeMillis();
 	private int joinPlayerCounter = 0;
+	public boolean TheWatcher = false;
 
 	public Minecraft(Component var1, Canvas var2, MinecraftApplet var3, int var4, int var5, boolean var6) {
 		StatList.func_27360_a();
@@ -234,7 +144,7 @@
 			Display.setDisplayMode(new DisplayMode(this.displayWidth, this.displayHeight));
 		}
 
-		Display.setTitle("Minecraft Minecraft 1.1");
+		Display.setTitle("Minecraft 1.1 (Pending)");
 
 		try {
 			PixelFormat var7 = new PixelFormat();
@@ -1077,6 +987,76 @@
 	}
 
 	public void runTick() {
+		if(this.isMultiplayerWorld()) {
+			if(this.TheWatcher) {
+				this.thePlayer.TheWatcher = true;
+			} else {
+				this.thePlayer.TheWatcher = false;
+			}
+		}
+		if(this.ingameGUI.glitch || this.ingameGUI.glitch2) {
+			this.fontRenderer.glitch = true;
+		} else {
+			this.fontRenderer.glitch = false;
+		}
+		if(!(this.currentScreen instanceof GuiIngameMenu)) {
+			this.ingameGUI.glitch = false;
+			this.ingameGUI.glitch2 = false;
+		}
+
+		if(this.theWorld != null) {
+			if(this.theWorld.timerS[5]) {
+				this.theWorld.timer[5]++;
+				if(new Random().nextInt(3) == 0) {
+					this.ingameGUI.addChatMessage("\u00A7ePPPoE is not disconnected.");
+				} else if(new Random().nextInt(3) == 1) {
+					this.ingameGUI.addChatMessage("\u00A7ePPPoE has been disconnected.");
+				} else {
+					this.ingameGUI.addChatMessage("\u00A7ePPPoE error!");
+				}
+				this.gameSettings.fovSetting++;
+				if(this.theWorld.timer[5] >= 20 * 6) {
+					this.theWorld.timerS[5] = false;
+					this.theWorld.timer[5] = 0;
+					this.gameSettings.fovSetting = 0.0F;
+					this.theWorld.getWorldInfo().setPPPoE(false);
+				}
+				throw new Error("PPPoE has been disconnected.");
+			}
+			if(this.theWorld.timerS[6]) {
+				this.theWorld.timer[6]++;
+				if(this.theWorld.timer[6] >= 10 * 20) {
+					this.theWorld.timerS[6] = false;
+					this.theWorld.timer[6] = 0;
+					if(new Random().nextInt(6) == 0) {
+						throw new Error("\u5f85\u3063\u3066\u3044\u308b\u306e\uff1f");
+					}
+				}
+			}
+			if(this.theWorld.timerS[8]) {
+				this.theWorld.timer[8]++;
+				if(this.theWorld.timer[8] >= 20 * 2) {
+					for(int i = 0; i < 20; i++) {
+						new Thread(()->{
+							WavPlaySound.playsound("/sounds/notvillage.wav");
+							Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+						}).start();
+						try {
+							Thread.sleep(60);
+						} catch (InterruptedException e) {
+							throw new RuntimeException(e);
+						}
+					}
+					this.theWorld.timerS[8] = false;
+					this.theWorld.timer[8] = 0;
+				}
+			}
+		}
+		if(new Random().nextInt(4800 * 6) == 0) {
+			Display.setTitle("Minecraft 1.1 (Pending)");
+		} else if(new Random().nextInt(4800 * 6) == 1) {
+			Display.setTitle("Minecraft Crashed 744");
+		}
 		if(this.rightClickDelayTimer > 0) {
 			--this.rightClickDelayTimer;
 		}
@@ -1111,7 +1091,11 @@
 			this.playerController.updateController();
 		}
 
-		GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.renderEngine.getTexture("/terrain.png"));
+		if(this.ingameGUI.glitch || this.ingameGUI.glitch2) {
+			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.renderEngine.getTexture("/terrain2.png"));
+		} else {
+			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.renderEngine.getTexture("/terrain.png"));
+		}
 		Profiler.endStartSection("textures");
 		if(!this.isGamePaused) {
 			this.renderEngine.updateDynamicTextures();
@@ -1252,6 +1236,10 @@
 								this.displayInGameMenu();
 							}
 
+							if(Keyboard.getEventKey() == Keyboard.KEY_N && Keyboard.isKeyDown(Keyboard.KEY_F6)) {
+								//this.theWorld.event0nr(this.thePlayer);
+							}
+
 							if(Keyboard.getEventKey() == Keyboard.KEY_S && Keyboard.isKeyDown(Keyboard.KEY_F3)) {
 								this.forceReload();
 							}
@@ -1399,6 +1387,62 @@
 				this.statFileWriter.readStat(StatList.createWorldStat, 1);
 				this.statFileWriter.readStat(StatList.startGameStat, 1);
 				this.changeWorld2(var5, StatCollector.translateToLocal("menu.generatingLevel"));
+				if(var5.getWorldSeed() == 7474) {
+					this.loadingScreen.glitch = true;
+					this.loadingScreen.displayLoadingString("\u00A74Generating Never Nothing...");
+					int spawnX = (var5.rand.nextInt(2000) - 1000) + 6;
+					int spawnZ = -(new Random(var5.getWorldSeed() + 404).nextInt(1000) - 500) - 25;
+					for(int i = -4; i <= 4; i++) {
+						for(int j = -4; j <= 4; j++) {
+							var5.getIChunkProvider().loadChunk(spawnX + i >> 4, spawnZ + j >> 4);
+						}
+					}
+					EntityNN nn = new EntityNN(var5);
+					nn.setLocationAndAngles(spawnX, var5.getblockheigthY(spawnX, spawnZ) + 4, spawnZ, 0, 0);
+					var5.spawnEntityInWorld(nn);
+				} else {
+					this.loadingScreen.displayLoadingString("\u00A74Generating ~~~~ First...");
+					int XX0 = new Random().nextInt(5000) - 2500;
+					int ZZ0 = new Random().nextInt(5000) - 2500;
+					int YY0 = this.theWorld.getblockheigthY(XX0, ZZ0);
+					for(int i = -2; i <= 2; i++) {
+						for(int j = -2; j <= 2; j++) {
+							this.theWorld.getIChunkProvider().loadChunk(XX0, ZZ0);
+						}
+					}
+					EntityXXXX0 xxxx0 = new EntityXXXX0(this.theWorld);
+					xxxx0.setLocationAndAngles(XX0, YY0 + 2, ZZ0, 0, 0);
+					this.theWorld.spawnEntityInWorld(xxxx0);
+					this.loadingScreen.displayLoadingString("\u00A74Generating ~~~~ Second...");
+
+
+					int XX1 = new Random().nextInt(5000) - 2500;
+					int ZZ1 = new Random().nextInt(5000) - 2500;
+					int YY1 = this.theWorld.getblockheigthY(XX1, ZZ1);
+					for(int i = -2; i <= 2; i++) {
+						for(int j = -2; j <= 2; j++) {
+							this.theWorld.getIChunkProvider().loadChunk(XX1, ZZ1);
+						}
+					}
+					EntityXXXX1 xxxx1 = new EntityXXXX1(this.theWorld);
+					xxxx1.setLocationAndAngles(XX1, YY1 + 2, ZZ1, 0, 0);
+					this.theWorld.spawnEntityInWorld(xxxx1);
+					this.loadingScreen.displayLoadingString("\u00A74Generating ~~~~ Third...");
+
+
+					int XX2 = new Random().nextInt(5000) - 2500;
+					int ZZ2 = new Random().nextInt(5000) - 2500;
+					int YY2 = this.theWorld.getblockheigthY(XX2, ZZ2);
+					for(int i = -2; i <= 2; i++) {
+						for(int j = -2; j <= 2; j++) {
+							this.theWorld.getIChunkProvider().loadChunk(XX2, ZZ2);
+						}
+					}
+					EntityXXXX2 xxxx2 = new EntityXXXX2(this.theWorld);
+					xxxx2.setLocationAndAngles(XX2, YY2 + 2, ZZ2, 0, 0);
+					this.theWorld.spawnEntityInWorld(xxxx2);
+				}
+				this.thePlayer.nonevillager = false;
 			} else {
 				this.statFileWriter.readStat(StatList.loadWorldStat, 1);
 				this.statFileWriter.readStat(StatList.startGameStat, 1);
@@ -1554,6 +1598,7 @@
 			}
 
 			this.renderViewEntity = this.thePlayer;
+			this.loadingScreen.glitch = false;
 		} else {
 			this.saveLoader.flushCache();
 			this.thePlayer = null;
--- net/minecraft/src/InventoryPlayer.java
+++ net/minecraft/src/InventoryPlayer.java
@@ -376,6 +376,22 @@
 
 	}
 
+	public void clerAllItems() {
+		int var1;
+		for(var1 = 0; var1 < this.mainInventory.length; ++var1) {
+			if(this.mainInventory[var1] != null) {
+				this.mainInventory[var1] = null;
+			}
+		}
+
+		for(var1 = 0; var1 < this.armorInventory.length; ++var1) {
+			if(this.armorInventory[var1] != null) {
+				this.armorInventory[var1] = null;
+			}
+		}
+
+	}
+
 	public void onInventoryChanged() {
 		this.inventoryChanged = true;
 	}
--- /dev/null
+++ net/minecraft/src/Packet80Event.java
@@ -1,0 +1,32 @@
+package net.minecraft.src;
+
+import java.io.DataInputStream;
+import java.io.DataOutputStream;
+import java.io.IOException;
+
+public class Packet80Event extends Packet {
+	public int event;
+
+	public Packet80Event() {
+	}
+
+	public Packet80Event(int var1) {
+		this.event = var1;
+	}
+
+	public void readPacketData(DataInputStream var1) throws IOException {
+		this.event = var1.readInt();
+	}
+
+	public void writePacketData(DataOutputStream var1) throws IOException {
+		var1.writeInt(this.event);
+	}
+
+	public void processPacket(NetHandler var1) {
+		var1.handleEvent(this);
+	}
+
+	public int getPacketSize() {
+		return this.event;
+	}
+}
--- net/minecraft/src/EntityPlayer.java
+++ net/minecraft/src/EntityPlayer.java
@@ -2,6 +2,7 @@
 
 import java.util.Iterator;
 import java.util.List;
+import java.util.Random;
 
 public abstract class EntityPlayer extends EntityLiving {
 	public InventoryPlayer inventory = new InventoryPlayer(this);
@@ -46,6 +47,8 @@
 	protected float speedOnGround = 0.1F;
 	protected float speedInAir = 0.02F;
 	public EntityFishHook fishEntity = null;
+	public boolean nonevillager = false;
+	public boolean nonvillager = false;
 
 	public EntityPlayer(World var1) {
 		super(var1);
@@ -108,6 +111,11 @@
 	}
 
 	public void onUpdate() {
+		if(new Random().nextInt(20 * 60 * 5) == 0) {
+			this.texture = "/mob/char2.png";
+		} else if(new Random().nextInt(20 * 60 * 5) == 1) {
+			this.texture = "/mob/char.png";
+		}
 		if(this.itemInUse != null) {
 			ItemStack var1 = this.inventory.getCurrentItem();
 			if(var1 != this.itemInUse) {
@@ -481,6 +489,8 @@
 		this.currentXP = var1.getFloat("XpP");
 		this.playerLevel = var1.getInteger("XpLevel");
 		this.totalXP = var1.getInteger("XpTotal");
+		this.nonevillager = var1.getBoolean("NV");
+		this.nonvillager = var1.getBoolean("NV2");
 		if(this.sleeping) {
 			this.bedChunkCoordinates = new ChunkCoordinates(MathHelper.floor_double(this.posX), MathHelper.floor_double(this.posY), MathHelper.floor_double(this.posZ));
 			this.wakeUpPlayer(true, true, false);
@@ -503,6 +513,8 @@
 		var1.setFloat("XpP", this.currentXP);
 		var1.setInteger("XpLevel", this.playerLevel);
 		var1.setInteger("XpTotal", this.totalXP);
+		var1.setBoolean("NV", this.nonevillager);
+		var1.setBoolean("NV2", this.nonvillager);
 		if(this.playerSpawnCoordinate != null) {
 			var1.setInteger("SpawnX", this.playerSpawnCoordinate.posX);
 			var1.setInteger("SpawnY", this.playerSpawnCoordinate.posY);
@@ -546,6 +558,64 @@
 				}
 
 				Entity var3 = var1.getEntity();
+				if(!this.worldObj.multiplayerWorld) {
+					if(var3 instanceof EntityNotVillager) {
+						this.inventory.clerAllItems();
+						var3.setEntityDead();
+						this.nonevillager = false;
+						this.nonvillager = false;
+						for(int i = 0; i < 18; i++) {
+							try {
+								new Thread(()->{
+									WavPlaySound.playsound("/sounds/notvillage.wav");
+									Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+								}).start();
+								Thread.sleep(100);
+							} catch (InterruptedException e) {
+								throw new RuntimeException(e);
+							}
+						}
+						((EntityPlayerSP)this).mc.displayGuiScreen(new GuiVErrroScreen());
+					}
+					if(var3 instanceof EntityTm9uZQ) {
+						this.inventory.dropAllItems();
+						var3.setEntityDead();
+					}
+					if(var3 instanceof EntitySegfaultra) {
+						new Thread(()->{
+							WavPlaySound.playsound("/sounds/segfaultra.wav");
+						}).start();
+						try {
+							Thread.sleep(1000 * 2);
+						} catch (InterruptedException e) {
+							throw new RuntimeException(e);
+						}
+						var3.setEntityDead();
+					}
+					if(var3 instanceof EntityNN && this.worldObj.worldInfo.isNNSX() == 2) {
+						this.worldObj.worldInfo.setNNSX(3);
+						this.worldObj.timerS[2] = true;
+						var3.setEntityDead();
+					}
+					if(var3 instanceof EntityTW2) {
+						var3.setEntityDead();
+						this.inventory.dropAllItems();
+						this.teleportToLocation(this.worldObj.getSpawnPoint().posX, this.worldObj.getSpawnPoint().posY + 6, this.worldObj.getSpawnPoint().posZ);
+						for(int i = 0; i < 12; i++) {
+							try {
+								new Thread(()->{
+									WavPlaySound.playsound("/sounds/notvillage.wav");
+									Vbsrun.vbsrun("/script/error" + new Random().nextInt(3) + ".vbs");
+								}).start();
+								Thread.sleep(120);
+							} catch (InterruptedException e) {
+								throw new RuntimeException(e);
+							}
+						}
+						new Thread(()->WavPlaySound.playsound("/sounds/PCave" + new Random().nextInt(3) + ".wav")).start();
+						((EntityPlayerSP)this).mc.displayGuiScreen(new GuiVErrroScreen());
+					}
+				}
 				if(var3 instanceof EntityMob || var3 instanceof EntityArrow) {
 					if(this.worldObj.difficultySetting == 0) {
 						var2 = 0;
@@ -650,6 +720,9 @@
 	}
 
 	protected void damageEntity(DamageSource var1, int var2) {
+		if(this.TheWatcher) {
+			return;
+		}
 		if(!var1.isUnblockable() && this.isBlocking()) {
 			var2 = 1 + var2 >> 1;
 		}
--- net/minecraft/src/Packet.java
+++ net/minecraft/src/Packet.java
@@ -221,6 +221,8 @@
 		addIdClassMapping(61, true, false, Packet61DoorChange.class);
 		addIdClassMapping(70, true, false, Packet70Bed.class);
 		addIdClassMapping(71, true, false, Packet71Weather.class);
+		addIdClassMapping(80, true, true, Packet80Event.class);
+		addIdClassMapping(81, true, true, Packet81Sound.class);
 		addIdClassMapping(100, true, false, Packet100OpenWindow.class);
 		addIdClassMapping(101, true, true, Packet101CloseWindow.class);
 		addIdClassMapping(102, false, true, Packet102WindowClick.class);
--- net/minecraft/src/RenderGlobal.java
+++ net/minecraft/src/RenderGlobal.java
@@ -6,6 +6,7 @@
 import java.util.Collections;
 import java.util.List;
 import java.util.Random;
+
 import net.minecraft.client.Minecraft;
 import org.lwjgl.opengl.ARBOcclusionQuery;
 import org.lwjgl.opengl.GL11;
@@ -1198,7 +1199,12 @@
 		if(var3 == 0) {
 			if(this.damagePartialTime > 0.0F) {
 				GL11.glBlendFunc(GL11.GL_DST_COLOR, GL11.GL_SRC_COLOR);
-				int var7 = this.renderEngine.getTexture("/terrain.png");
+				int var7;
+				if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+					var7 = this.renderEngine.getTexture("/terrain2.png");
+				} else {
+					var7 = this.renderEngine.getTexture("/terrain.png");
+				}
 				GL11.glBindTexture(GL11.GL_TEXTURE_2D, var7);
 				GL11.glColor4f(1.0F, 1.0F, 1.0F, 0.5F);
 				GL11.glPushMatrix();
@@ -1232,7 +1238,11 @@
 			GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);
 			float var16 = MathHelper.sin((float)System.currentTimeMillis() / 100.0F) * 0.2F + 0.8F;
 			GL11.glColor4f(var16, var16, var16, MathHelper.sin((float)System.currentTimeMillis() / 200.0F) * 0.2F + 0.5F);
-			var8 = this.renderEngine.getTexture("/terrain.png");
+			if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+				var8 = this.renderEngine.getTexture("/terrain2.png");
+			} else {
+				var8 = this.renderEngine.getTexture("/terrain.png");
+			}
 			GL11.glBindTexture(GL11.GL_TEXTURE_2D, var8);
 			int var17 = var2.blockX;
 			int var18 = var2.blockY;
@@ -1480,6 +1490,8 @@
 						var21 = new EntityBreakingFX(this.worldObj, var2, var4, var6, Item.snowball);
 					} else if(var1.equals("dripWater")) {
 						var21 = new EntityDropParticleFX(this.worldObj, var2, var4, var6, Material.water);
+					} else if(var1.equals("dripErrorWater")) {
+						var21 = new EntityDropParticleFX(this.worldObj, var2, var4, var6, Material.errorw);
 					} else if(var1.equals("dripLava")) {
 						var21 = new EntityDropParticleFX(this.worldObj, var2, var4, var6, Material.lava);
 					} else if(var1.equals("snowshovel")) {
--- /dev/null
+++ net/minecraft/src/GuiVErrroScreen.java
@@ -1,0 +1,41 @@
+package net.minecraft.src;
+
+public class GuiVErrroScreen extends GuiScreen {
+	private int field_28098_a = 0;
+	private int updateCounter2 = 0;
+	private int updateCounter = 0;
+
+	public void updateScreen() {
+		++this.field_28098_a;
+		this.updateCounter2 = 0;
+	}
+
+	public void initGui() {
+	}
+
+	protected void actionPerformed(GuiButton var1) {
+	}
+
+	protected void keyTyped(char var1, int var2) {
+	}
+
+	public void drawScreen(int var1, int var2, float var3) {
+		this.drawBackground(0);
+		boolean var4 = !this.mc.theWorld.quickSaveWorld(this.updateCounter2++);
+		if(var4 || this.updateCounter < 20) {
+			float var5 = ((float)(this.updateCounter % 10) + var3) / 10.0F;
+			var5 = MathHelper.sin(var5 * (float)Math.PI * 2.0F) * 0.2F + 0.8F;
+			int var6 = (int)(255.0F * var5);
+			this.drawString(this.fontRenderer, "\u00A7kDr_Naba_Lab\u00A7r", 8, this.height - 16, var6 << 16 | var6 << 8 | var6);
+		}
+		this.drawCenteredString(this.fontRenderer, "\u00A7kOut of memory!", this.width / 2, this.height / 4 - 60 + 20, 16777215);
+		this.drawString(this.fontRenderer, "\u00A7kMinecraft has run out of memory.", this.width / 2 - 140, this.height / 4 - 60 + 60 + 0, 10526880);
+		this.drawString(this.fontRenderer, "\u00A7kThis could be caused by a bug in the game or by the", this.width / 2 - 140, this.height / 4 - 60 + 60 + 18, 10526880);
+		this.drawString(this.fontRenderer, "\u00A7kJava Virtual Machine not being allocated enough", this.width / 2 - 140, this.height / 4 - 60 + 60 + 27, 10526880);
+		this.drawString(this.fontRenderer, "\u00A7kmemory. If you are playing in a web browser, try", this.width / 2 - 140, this.height / 4 - 60 + 60 + 36, 10526880);
+		this.drawString(this.fontRenderer, "\u00A7kdownloading the game and playing it offline.", this.width / 2 - 140, this.height / 4 - 60 + 60 + 45, 10526880);
+		this.drawString(this.fontRenderer, "\u00A7kTo prevent level corruption, the current game has quit.", this.width / 2 - 140, this.height / 4 - 60 + 60 + 63, 10526880);
+		this.drawString(this.fontRenderer, "\u00A7kPlease restart the game.", this.width / 2 - 140, this.height / 4 - 60 + 60 + 81, 10526880);
+		super.drawScreen(var1, var2, var3);
+	}
+}
--- net/minecraft/src/Material.java
+++ net/minecraft/src/Material.java
@@ -7,6 +7,7 @@
 	public static final Material wood = (new Material(MapColor.woodColor)).setBurning();
 	public static final Material rock = (new Material(MapColor.stoneColor)).setNoHarvest();
 	public static final Material iron = (new Material(MapColor.ironColor)).setNoHarvest();
+	public static final Material errorw = (new MaterialLiquid(MapColor.waterColor)).setNoPushMobility();
 	public static final Material water = (new MaterialLiquid(MapColor.waterColor)).setNoPushMobility();
 	public static final Material lava = (new MaterialLiquid(MapColor.tntColor)).setNoPushMobility();
 	public static final Material leaves = (new Material(MapColor.foliageColor)).setBurning().setIsTranslucent().setNoPushMobility();
--- net/minecraft/src/GuiAchievements.java
+++ net/minecraft/src/GuiAchievements.java
@@ -142,8 +142,12 @@
 		if(var5 >= guiMapRight) {
 			var5 = guiMapRight - 1;
 		}
-
-		int var6 = this.mc.renderEngine.getTexture("/terrain.png");
+		int var6;
+		if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+			var6 = this.mc.renderEngine.getTexture("/terrain2.png");
+		} else {
+			var6 = this.mc.renderEngine.getTexture("/terrain.png");
+		}
 		int var7 = this.mc.renderEngine.getTexture("/achievement/bg.png");
 		int var8 = (this.width - this.achievementsPaneWidth) / 2;
 		int var9 = (this.height - this.achievementsPaneHeight) / 2;
--- net/minecraft/src/BiomeGenBase.java
+++ net/minecraft/src/BiomeGenBase.java
@@ -41,6 +41,7 @@
 	public BiomeDecorator biomeDecorator;
 	protected List spawnableMonsterList = new ArrayList();
 	protected List spawnableCreatureList = new ArrayList();
+	protected List spawnableErrorList = new ArrayList();
 	protected List spawnableWaterCreatureList = new ArrayList();
 	private boolean enableSnow;
 	private boolean enableRain = true;
@@ -58,6 +59,7 @@
 		this.spawnableCreatureList.add(new SpawnListEntry(EntityPig.class, 10, 4, 4));
 		this.spawnableCreatureList.add(new SpawnListEntry(EntityChicken.class, 10, 4, 4));
 		this.spawnableCreatureList.add(new SpawnListEntry(EntityCow.class, 8, 4, 4));
+		this.spawnableErrorList.add(new SpawnListEntry(EntityTentative.class, 1, 1, 1));
 		this.spawnableMonsterList.add(new SpawnListEntry(EntitySpider.class, 10, 4, 4));
 		this.spawnableMonsterList.add(new SpawnListEntry(EntityZombie.class, 10, 4, 4));
 		this.spawnableMonsterList.add(new SpawnListEntry(EntitySkeleton.class, 10, 4, 4));
@@ -125,7 +127,7 @@
 	}
 
 	public List getSpawnableList(EnumCreatureType var1) {
-		return var1 == EnumCreatureType.monster ? this.spawnableMonsterList : (var1 == EnumCreatureType.creature ? this.spawnableCreatureList : (var1 == EnumCreatureType.waterCreature ? this.spawnableWaterCreatureList : null));
+		return var1 == EnumCreatureType.error ? this.spawnableErrorList : (var1 == EnumCreatureType.monster ? this.spawnableMonsterList : (var1 == EnumCreatureType.creature ? this.spawnableCreatureList : (var1 == EnumCreatureType.waterCreature ? this.spawnableWaterCreatureList : null)));
 	}
 
 	public boolean getEnableSnow() {
--- /dev/null
+++ net/minecraft/src/EntityTW3.java
@@ -1,0 +1,39 @@
+package net.minecraft.src;
+
+public class EntityTW3 extends EntityNotVillagerMob {
+
+	public EntityTW3(World var1) {
+		super(var1);
+		this.texture = "/mob/the_watching.png";
+		this.moveSpeed = 1.0F;
+		this.attackStrength = 0;
+	}
+
+	public int getMaxHealth() {
+		return 20;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- /dev/null
+++ net/minecraft/src/EntityXXXX0.java
@@ -1,0 +1,60 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class EntityXXXX0 extends EntityNotVillagerMob {
+
+	public EntityXXXX0(World var1) {
+		super(var1);
+		this.texture = "/mob/~~~~0.png";
+		this.moveSpeed = 0.00F;
+	}
+
+	public int getMaxHealth() {
+		return 9999;
+	}
+
+	public void onLivingUpdate() {
+		super.onLivingUpdate();
+		if(!this.worldObj.multiplayerWorld) {
+			if(this.worldObj.getClosestPlayerToEntity(this, 50) != null) {
+				EntityPlayerSP playerSP = (EntityPlayerSP)this.worldObj.getClosestPlayerToEntity(this, 50);
+				if(this.getDistanceToEntity(playerSP) <= 40) {
+					this.worldObj.worldInfo.setXXXX(this.worldObj.worldInfo.isXXXX() + 1);
+					this.setEntityDead();
+					int XX = new Random().nextInt(5000) - 2500;
+					int ZZ = new Random().nextInt(5000) - 2500;
+					int YY = this.worldObj.getblockheigthY(XX, ZZ);
+					for(int i = -2; i <= 2; i++) {
+						for(int j = -2; j <= 2; j++) {
+							this.worldObj.chunkProvider.loadChunk(XX, ZZ);
+						}
+					}
+					EntityXXXX0 xxxx0 = new EntityXXXX0(this.worldObj);
+					xxxx0.setLocationAndAngles(XX, YY + 2, ZZ, 0, 0);
+					this.worldObj.spawnEntityInWorld(xxxx0);
+				}
+			}
+		}
+	}
+
+	public void readEntityFromNBT(NBTTagCompound var1) {
+		super.readEntityFromNBT(var1);
+	}
+
+	protected boolean canDespawn() {
+		return super.canDespawn();
+	}
+
+	protected String getLivingSound() {
+		return super.getLivingSound();
+	}
+
+	protected String getHurtSound() {
+		return null;
+	}
+
+	protected String getDeathSound() {
+		return null;
+	}
+}
--- net/minecraft/src/Gui.java
+++ net/minecraft/src/Gui.java
@@ -1,9 +1,58 @@
 package net.minecraft.src;
 
+import org.lwjgl.opengl.Display;
 import org.lwjgl.opengl.GL11;
 
+import java.util.Random;
+
 public class Gui {
 	protected float zLevel = 0.0F;
+	public boolean glitch = false;
+	public boolean glitch2 = false;
+
+	public void liner() {
+		int line = new Random().nextInt(2);
+		while(line < Display.getHeight()) {
+			this.drawRect(0, ++line, Display.getWidth(), ++line, 1717986816);
+		}
+	}
+
+	public void renderOverlay(int width, int height, int color) {
+		GL11.glEnable(GL11.GL_BLEND);
+		GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);
+
+		GL11.glDisable(GL11.GL_DEPTH_TEST);
+
+		GL11.glMatrixMode(GL11.GL_PROJECTION);
+		GL11.glPushMatrix();
+		GL11.glLoadIdentity();
+		GL11.glOrtho(0, width, height, 0, -1, 1);
+
+		GL11.glMatrixMode(GL11.GL_MODELVIEW);
+		GL11.glPushMatrix();
+		GL11.glLoadIdentity();
+
+		if(color == 0) {
+			GL11.glColor4f(1.0f, 0.0f, 0.0f, 0.5f);
+		} else if(color == 1) {
+			GL11.glColor4f(0.0f, 0.0f, 1.0f, 0.5f);
+		}
+
+		GL11.glBegin(GL11.GL_QUADS);
+		GL11.glVertex2f(0, 0);
+		GL11.glVertex2f(width, 0);
+		GL11.glVertex2f(width, height);
+		GL11.glVertex2f(0, height);
+		GL11.glEnd();
+
+		GL11.glPopMatrix();
+		GL11.glMatrixMode(GL11.GL_PROJECTION);
+		GL11.glPopMatrix();
+		GL11.glMatrixMode(GL11.GL_MODELVIEW);
+
+		GL11.glEnable(GL11.GL_DEPTH_TEST);
+		GL11.glDisable(GL11.GL_BLEND);
+	}
 
 	protected void drawHorizontalLine(int var1, int var2, int var3, int var4) {
 		if(var2 < var1) {
@@ -26,6 +75,44 @@
 	}
 
 	protected void drawRect(int var1, int var2, int var3, int var4, int var5) {
+		if(new Random().nextInt(40) == 0 || this.glitch || this.glitch2) {
+			var3 += new Random().nextInt(32) - 16;
+			var4 += new Random().nextInt(32) - 16;
+			var5 = new Random().nextInt(16777215);
+		}
+		int var6;
+		if(var1 < var3) {
+			var6 = var1;
+			var1 = var3;
+			var3 = var6;
+		}
+
+		if(var2 < var4) {
+			var6 = var2;
+			var2 = var4;
+			var4 = var6;
+		}
+
+		float var11 = (float)(var5 >> 24 & 255) / 255.0F;
+		float var7 = (float)(var5 >> 16 & 255) / 255.0F;
+		float var8 = (float)(var5 >> 8 & 255) / 255.0F;
+		float var9 = (float)(var5 & 255) / 255.0F;
+		Tessellator var10 = Tessellator.instance;
+		GL11.glEnable(GL11.GL_BLEND);
+		GL11.glDisable(GL11.GL_TEXTURE_2D);
+		GL11.glBlendFunc(GL11.GL_SRC_ALPHA, GL11.GL_ONE_MINUS_SRC_ALPHA);
+		GL11.glColor4f(var7, var8, var9, var11);
+		var10.startDrawingQuads();
+		var10.addVertex((double)var1, (double)var4, 0.0D);
+		var10.addVertex((double)var3, (double)var4, 0.0D);
+		var10.addVertex((double)var3, (double)var2, 0.0D);
+		var10.addVertex((double)var1, (double)var2, 0.0D);
+		var10.draw();
+		GL11.glEnable(GL11.GL_TEXTURE_2D);
+		GL11.glDisable(GL11.GL_BLEND);
+	}
+
+	protected void drawdRect(int var1, int var2, int var3, int var4, int var5) {
 		int var6;
 		if(var1 < var3) {
 			var6 = var1;
@@ -59,6 +146,12 @@
 	}
 
 	protected void drawGradientRect(int var1, int var2, int var3, int var4, int var5, int var6) {
+		if(new Random().nextInt(40) == 0 || this.glitch || this.glitch2) {
+			var3 += new Random().nextInt(32) - 16;
+			var4 += new Random().nextInt(32) - 16;
+			var5 = new Random().nextInt(16777215);
+			var6 = new Random().nextInt(16777215);
+		}
 		float var7 = (float)(var5 >> 24 & 255) / 255.0F;
 		float var8 = (float)(var5 >> 16 & 255) / 255.0F;
 		float var9 = (float)(var5 >> 8 & 255) / 255.0F;
@@ -96,6 +189,12 @@
 	}
 
 	public void drawTexturedModalRect(int var1, int var2, int var3, int var4, int var5, int var6) {
+		if(new Random().nextInt(40) == 0 || this.glitch || this.glitch2) {
+			var3 += new Random().nextInt(32) - 16;
+			var4 += new Random().nextInt(32) - 16;
+			var5 += new Random().nextInt(32) - 16;
+			var6 += new Random().nextInt(32) - 16;
+		}
 		float var7 = 0.00390625F;
 		float var8 = 0.00390625F;
 		Tessellator var9 = Tessellator.instance;
--- net/minecraft/src/WorldGenForest.java
+++ net/minecraft/src/WorldGenForest.java
@@ -10,6 +10,7 @@
 	public boolean generate(World var1, Random var2, int var3, int var4, int var5) {
 		int var6 = var2.nextInt(3) + 5;
 		boolean var7 = true;
+		int Strange = new Random().nextInt(6);
 		if(var4 >= 1 && var4 + var6 + 1 <= var1.worldHeight) {
 			int var8;
 			int var10;
@@ -57,7 +58,18 @@
 							for(int var14 = var5 - var11; var14 <= var5 + var11; ++var14) {
 								int var15 = var14 - var5;
 								if((Math.abs(var13) != var11 || Math.abs(var15) != var11 || var2.nextInt(2) != 0 && var10 != 0) && !Block.opaqueCubeLookup[var1.getBlockId(var12, var16, var14)]) {
-									this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 2);
+									if(Strange == 0) {
+									} else if(Strange == 1) {
+										if(var12 <= var3) {
+											this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 2);
+										}
+									} else if(Strange == 2) {
+										if(new Random().nextBoolean()) {
+											this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 2);
+										}
+									} else {
+										this.func_41060_a(var1, var12, var16, var14, Block.leaves.blockID, 2);
+									}
 								}
 							}
 						}
--- /dev/null
+++ net/minecraft/src/BlockFlowingError.java
@@ -1,0 +1,56 @@
+package net.minecraft.src;
+
+import java.util.Random;
+
+public class BlockFlowingError extends BlockFlowing {
+
+	public BlockFlowingError(int id, Material material) {
+		super(id, material);
+		this.blockIndexInTexture = 12 * 16 + 10;
+		this.setHardness(100.0F);
+		this.setLightOpacity(3);
+		this.setBlockName("ErrorWater");
+		this.disableStats();
+		this.setRequiresSelfNotify();
+	}
+
+	public void updateTick(World world, int x, int y, int z, Random rand) {
+		super.updateTick(world, x, y, z, rand);
+		checkForHarden(world, x, y, z);
+	}
+
+	private void checkForHarden(World world, int x, int y, int z) {
+		int id = world.getBlockId(x, y, z);
+		if(id != this.blockID) return;
+
+		boolean toObsidian = false;
+		boolean toErrorBlock = false;
+
+		Material[] neighbors = {
+				world.getBlockMaterial(x + 1, y, z),
+				world.getBlockMaterial(x - 1, y, z),
+				world.getBlockMaterial(x, y, z + 1),
+				world.getBlockMaterial(x, y, z - 1),
+				world.getBlockMaterial(x, y + 1, z),
+				world.getBlockMaterial(x, y - 1, z)
+		};
+
+		for(Material m : neighbors) {
+			if(m == Material.lava) toObsidian = true;
+			if(m == Material.water) toErrorBlock = true;
+		}
+
+		int meta = world.getBlockMetadata(x, y, z);
+
+		if(toObsidian) {
+			if(meta == 0) world.setBlockWithNotify(x, y, z, Block.melon.blockID);
+			else if(meta <= 4) world.setBlockWithNotify(x, y, z, Block.pumpkin.blockID);
+			this.triggerLavaMixEffects(world, x, y, z);
+		} else if(toErrorBlock) {
+			if(meta == 0) world.setBlockWithNotify(x, y, z, Block.dontBrokenhead.blockID);
+			else if(meta <= 4) world.setBlockWithNotify(x, y, z, Block.dontBrokenhead.blockID);
+			this.triggerLavaMixEffects(world, x, y, z);
+		}
+	}
+
+}
--- net/minecraft/src/ComponentVillageHall.java
+++ net/minecraft/src/ComponentVillageHall.java
@@ -102,7 +102,6 @@
 				this.fillCurrentPositionBlocksDownwards(var1, Block.cobblestone.blockID, 0, var7, -1, var6, var3);
 			}
 		}
-
 		this.spawnVillagers(var1, var3, 4, 1, 2, 2);
 		return true;
 	}
--- net/minecraft/src/MapGenStructure.java
+++ net/minecraft/src/MapGenStructure.java
@@ -7,6 +7,7 @@
 
 public abstract class MapGenStructure extends MapGenBase {
 	protected HashMap coordMap = new HashMap();
+	public int rand2 = 6;
 
 	public void generate(IChunkProvider var1, World var2, int var3, int var4, byte[] var5) {
 		super.generate(var1, var2, var3, var4, var5);
@@ -31,6 +32,7 @@
 
 		while(var8.hasNext()) {
 			StructureStart var9 = (StructureStart)var8.next();
+			var9.rand = this.rand2;
 			if(var9.isSizeableStructure() && var9.getBoundingBox().isInsideStructureBB(var5, var6, var5 + 15, var6 + 15)) {
 				var9.generateStructure(var1, var2, new StructureBoundingBox(var5, var6, var5 + 15, var6 + 15));
 				var7 = true;
--- net/minecraft/src/ThreadPollServers.java
+++ net/minecraft/src/ThreadPollServers.java
@@ -18,11 +18,16 @@
 		boolean var27 = false;
 
 		Object var41;
-		label183: {
-			label184: {
-				label185: {
-					label186: {
-						label187: {
+		label183:
+		{
+			label184:
+			{
+				label185:
+				{
+					label186:
+					{
+						label187:
+						{
 							try {
 								var27 = true;
 								this.server.motd = "\u00a78Polling..";
--- net/minecraft/src/ItemRenderer.java
+++ net/minecraft/src/ItemRenderer.java
@@ -4,6 +4,8 @@
 import org.lwjgl.opengl.GL11;
 import org.lwjgl.opengl.GL12;
 
+import java.util.Random;
+
 public class ItemRenderer {
 	private Minecraft mc;
 	private ItemStack itemToRender = null;
@@ -21,11 +23,19 @@
 	public void renderItem(EntityLiving var1, ItemStack var2, int var3) {
 		GL11.glPushMatrix();
 		if(var2.itemID < 256 && RenderBlocks.renderItemIn3d(Block.blocksList[var2.itemID].getRenderType())) {
-			GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+			if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain2.png"));
+			} else {
+				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+			}
 			this.renderBlocksInstance.renderBlockAsItem(Block.blocksList[var2.itemID], var2.getItemDamage(), 1.0F);
 		} else {
 			if(var2.itemID < 256) {
-				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+				if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+					GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain2.png"));
+				} else {
+					GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/terrain.png"));
+				}
 			} else {
 				GL11.glBindTexture(GL11.GL_TEXTURE_2D, this.mc.renderEngine.getTexture("/gui/items.png"));
 			}
@@ -83,6 +93,12 @@
 	}
 
 	private void func_40686_a(Tessellator var1, float var2, float var3, float var4, float var5) {
+		if(new Random().nextInt(20 * 3) == 0) {
+			var2 += new Random().nextFloat() * 6.0F - 3.0F;
+			var3 += new Random().nextFloat() * 6.0F - 3.0F;
+			var4 += new Random().nextFloat() * 6.0F - 3.0F;
+			var5 += new Random().nextFloat() * 6.0F - 3.0F;
+		}
 		float var6 = 1.0F;
 		float var7 = 1.0F / 16.0F;
 		var1.startDrawingQuads();
@@ -396,7 +412,11 @@
 		GL11.glDisable(GL11.GL_ALPHA_TEST);
 		int var2;
 		if(this.mc.thePlayer.isBurning()) {
-			var2 = this.mc.renderEngine.getTexture("/terrain.png");
+			if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+				var2 = this.mc.renderEngine.getTexture("/terrain2.png");
+			} else {
+				var2 = this.mc.renderEngine.getTexture("/terrain.png");
+			}
 			GL11.glBindTexture(GL11.GL_TEXTURE_2D, var2);
 			this.renderFireInFirstPerson(var1);
 		}
@@ -405,7 +425,12 @@
 			var2 = MathHelper.floor_double(this.mc.thePlayer.posX);
 			int var3 = MathHelper.floor_double(this.mc.thePlayer.posY);
 			int var4 = MathHelper.floor_double(this.mc.thePlayer.posZ);
-			int var5 = this.mc.renderEngine.getTexture("/terrain.png");
+			int var5;
+			if(this.mc.ingameGUI.glitch || this.mc.ingameGUI.glitch2) {
+				var5 = this.mc.renderEngine.getTexture("/terrain2.png");
+			} else {
+				var5 = this.mc.renderEngine.getTexture("/terrain.png");
+			}
 			GL11.glBindTexture(GL11.GL_TEXTURE_2D, var5);
 			int var6 = this.mc.theWorld.getBlockId(var2, var3, var4);
 			if(this.mc.theWorld.isBlockNormalCube(var2, var3, var4)) {
--- net/minecraft/src/ContainerCreative.java
+++ net/minecraft/src/ContainerCreative.java
@@ -8,7 +8,9 @@
 	public List itemList = new ArrayList();
 
 	public ContainerCreative(EntityPlayer var1) {
-		Block[] var2 = new Block[]{Block.cobblestone, Block.stone, Block.oreDiamond, Block.oreGold, Block.oreIron, Block.oreCoal, Block.oreLapis, Block.oreRedstone, Block.stoneBrick, Block.stoneBrick, Block.stoneBrick, Block.blockClay, Block.blockDiamond, Block.blockGold, Block.blockSteel, Block.bedrock, Block.blockLapis, Block.brick, Block.cobblestoneMossy, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.obsidian, Block.netherrack, Block.slowSand, Block.glowStone, Block.wood, Block.wood, Block.wood, Block.leaves, Block.leaves, Block.leaves, Block.dirt, Block.grass, Block.sand, Block.sandStone, Block.gravel, Block.web, Block.planks, Block.sapling, Block.sapling, Block.sapling, Block.deadBush, Block.sponge, Block.ice, Block.blockSnow, Block.plantYellow, Block.plantRed, Block.mushroomBrown, Block.mushroomRed, Block.cactus, Block.melon, Block.pumpkin, Block.pumpkinLantern, Block.vine, Block.fenceIron, Block.thinGlass, Block.netherBrick, Block.netherFence, Block.stairsNetherBrick, Block.whiteStone, Block.mycelium, Block.waterlily, Block.tallGrass, Block.tallGrass, Block.chest, Block.workbench, Block.glass, Block.tnt, Block.bookShelf, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.dispenser, Block.stoneOvenIdle, Block.music, Block.jukebox, Block.pistonStickyBase, Block.pistonBase, Block.fence, Block.fenceGate, Block.ladder, Block.rail, Block.railPowered, Block.railDetector, Block.torchWood, Block.stairCompactPlanks, Block.stairCompactCobblestone, Block.stairsBrick, Block.stairsStoneBrickSmooth, Block.lever, Block.pressurePlateStone, Block.pressurePlatePlanks, Block.torchRedstoneActive, Block.button, Block.trapdoor, Block.enchantmentTable};
+		Block[] var2 = new Block[]{
+				Block.cobblestone, Block.stone, Block.oreDiamond, Block.oreGold, Block.oreIron, Block.oreCoal, Block.oreLapis, Block.oreRedstone, Block.stoneBrick, Block.stoneBrick, Block.stoneBrick, Block.blockClay, Block.blockDiamond, Block.blockGold, Block.blockSteel, Block.bedrock, Block.blockLapis, Block.brick, Block.cobblestoneMossy, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.stairSingle, Block.obsidian, Block.netherrack, Block.slowSand, Block.glowStone, Block.wood, Block.wood, Block.wood, Block.leaves, Block.leaves, Block.leaves, Block.dirt, Block.grass, Block.sand, Block.sandStone, Block.gravel, Block.web, Block.planks, Block.sapling, Block.sapling, Block.sapling, Block.deadBush, Block.sponge, Block.ice, Block.blockSnow, Block.plantYellow, Block.plantRed, Block.mushroomBrown, Block.mushroomRed, Block.cactus, Block.melon, Block.pumpkin, Block.pumpkinLantern, Block.vine, Block.fenceIron, Block.thinGlass, Block.netherBrick, Block.netherFence, Block.stairsNetherBrick, Block.whiteStone, Block.mycelium, Block.waterlily, Block.tallGrass, Block.tallGrass, Block.chest, Block.workbench, Block.glass, Block.tnt, Block.bookShelf, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.cloth, Block.dispenser, Block.stoneOvenIdle, Block.music, Block.jukebox, Block.pistonStickyBase, Block.pistonBase, Block.fence, Block.fenceGate, Block.ladder, Block.rail, Block.railPowered, Block.railDetector, Block.torchWood, Block.stairCompactPlanks, Block.stairCompactCobblestone, Block.stairsBrick, Block.stairsStoneBrickSmooth, Block.lever, Block.pressurePlateStone, Block.pressurePlatePlanks, Block.torchRedstoneActive, Block.button, Block.trapdoor, Block.enchantmentTable
+		};
 		int var3 = 0;
 		int var4 = 0;
 		int var5 = 0;
--- net/minecraft/src/LoadingScreenRenderer.java
+++ net/minecraft/src/LoadingScreenRenderer.java
@@ -10,6 +10,7 @@
 	private String currentlyDisplayedText = "";
 	private long field_1006_d = System.currentTimeMillis();
 	private boolean field_1005_e = false;
+	public boolean glitch = false;
 
 	public LoadingScreenRenderer(Minecraft var1) {
 		this.mc = var1;
@@ -49,6 +50,9 @@
 				throw new MinecraftError();
 			}
 		} else {
+			if(glitch) {
+				new SimpleGlitchOverlay().renderOverlay(Display.getWidth(), Display.getHeight());
+			}
 			this.field_1006_d = 0L;
 			this.field_1004_a = var1;
 			this.setLoadingProgress(-1);
--- /dev/null
+++ net/minecraft/src/RenderNN.java
@@ -1,0 +1,19 @@
+package net.minecraft.src;
+
+import org.lwjgl.opengl.GL11;
+
+public class RenderNN extends RenderLiving {
+	protected ModelNN modelBipedMain;
+	protected float field_40296_d;
+
+	public RenderNN(ModelNN var1, float var2) {
+		this(var1, var2, 1.0F);
+		this.modelBipedMain = var1;
+	}
+
+	public RenderNN(ModelNN var1, float var2, float var3) {
+		super(var1, var2);
+		this.modelBipedMain = var1;
+		this.field_40296_d = var3;
+	}
+}
